---
phase: 02-risk-scoring-engine
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/risk_engine.py
  - main.py
autonomous: true
requirements:
  - RISK-01
  - RISK-02
  - RISK-03
  - RISK-04
  - RISK-05
  - RISK-06
  - RISK-07
  - RISK-08
user_setup: []

must_haves:
  truths:
    - "score_risk(df) on the Phase 1 ingest() output returns a DataFrame with 11 added columns: attendance_rate, avg_practice_questions, trend_direction, days_since_last_note, attendance_component, practice_component, trend_component, notes_component, risk_score, risk_level, recommended_action"
    - "A worst-case student (attendance_days=0, practice_total_q=0, declining session series, latest_note_date=NaT) receives risk_score == 100.0 and risk_level == 'CRITICAL' under @freeze_time('2026-05-23')"
    - "A perfect student (attendance_days=14, practice_total_q=210, improving session series, latest_note_date=today) receives risk_score == 0.0 and risk_level == 'LOW' under @freeze_time('2026-05-23')"
    - "score_risk is a pure function: caller's df columns are unchanged after call AND `result is not df` (different DataFrame object)"
    - "df.attrs['data_quality_warnings'] survives the score_risk transform (Phase 4 depends on this side-channel)"
    - "No bare snake_case column-name strings appear in src/risk_engine.py outside the allowed set {declining, improving, stable, attendance_days, practice_total_q, daily_session_series, latest_note_date}"
    - "logger.info from score_risk does not emit student_name or parent_phone (PII discipline V7)"
    - "main.py orchestrator wires score_risk(df) after ingest(df); `py -3.12 main.py` exits 0 and logs 'Scored {N} students' aggregate counts"
    - "All 14+ tests in tests/test_risk_engine.py pass; Phase 1's 25 tests still pass"
    - "D-05: risk_score = round(attendance_component × 0.35 + practice_component × 0.30 + trend_component × 0.20 + notes_component × 0.15, 2) — weights sourced from cfg.WEIGHT_* constants, not hardcoded floats"
    - "D-06: risk_level assigned via pd.cut(risk_score, bins=[-inf,25,50,75,inf], labels=['LOW','MEDIUM','HIGH','CRITICAL'], right=False) — thresholds from cfg.RISK_THRESHOLD_* constants"
    - "D-07: trend_direction column contains string labels 'declining' / 'stable' / 'improving' (not numeric) — output of _trend_component_and_direction helper"
    - "D-08: recommended_action column populated from _ACTION_BY_LEVEL dict mapping CRITICAL→'Contact parent immediately', HIGH→'Schedule check-in this week', MEDIUM→'Monitor closely', LOW→'On track'"
  artifacts:
    - path: "src/risk_engine.py"
      provides: "Pure function score_risk implementing D-01..D-09 + RISK-01..RISK-08"
      contains: "def score_risk(df: pd.DataFrame) -> pd.DataFrame"
      min_lines: 120
    - path: "main.py"
      provides: "Pipeline wiring — ingest() output flows into score_risk() before Phase 3 stub"
      contains: "from src.risk_engine import score_risk"
  key_links:
    - from: "src/risk_engine.py"
      to: "src.config (cfg.WEIGHT_*, cfg.RISK_THRESHOLD_*, cfg.COL_*)"
      via: "from src import config as cfg"
      pattern: "from src import config as cfg"
    - from: "src/risk_engine.py"
      to: "pd.cut threshold ladder"
      via: "pd.cut(scores, bins=[-inf, 25, 50, 75, inf], labels=[LOW, MEDIUM, HIGH, CRITICAL], right=False)"
      pattern: "pd\\.cut\\("
    - from: "main.py"
      to: "src.risk_engine.score_risk"
      via: "df = score_risk(df) after ingest(df)"
      pattern: "score_risk\\(df\\)"
---

<objective>
Wave 1 — implement `src/risk_engine.py` as a pure function delivering D-01 through D-09 and RISK-01 through RISK-08, then wire it into `main.py` between ingestion and the Phase 3 stub. This is the body of Phase 2. The failing tests written in Plan 02-01 must all turn GREEN.

Purpose: After Plan 02-02 completes, every student in the merged DataFrame carries a deterministic risk_score, risk_level, four component scores, four RISK-07 domain columns, and a rule-based recommended_action — all computed by a single pure function that performs no I/O. Phase 3 (LLM enrichment) can consume CRITICAL/HIGH-flagged students directly from this output.

Output: 2 modified files (src/risk_engine.py replaces stub body; main.py uncomments + extends the Phase 2 wiring). `py -3.12 -m pytest tests/ -q` green (Phase 1's 25 + Phase 2's 14+ = ~39 tests). `py -3.12 main.py` runs end-to-end through Phase 2.

**Threshold semantics reminder (carried from Plan 02-01 objective):** risk_score is a "risk magnitude" — higher score = more at-risk. Worst student → 100 → CRITICAL. Perfect student → 0 → LOW. This is consistent with both ROADMAP success criteria and CONTEXT.md D-06 (no contradiction exists once you trace through the D-01..D-04 component formulas which invert raw activity).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-risk-scoring-engine/02-CONTEXT.md
@.planning/phases/02-risk-scoring-engine/02-RESEARCH.md
@.planning/phases/02-risk-scoring-engine/02-PATTERNS.md
@.planning/phases/02-risk-scoring-engine/02-VALIDATION.md
@.planning/phases/02-risk-scoring-engine/02-01-SUMMARY.md
@src/config.py
@src/ingestion.py
@src/risk_engine.py
@main.py
@tests/test_risk_engine.py
@CLAUDE.md

<interfaces>
Extracted from src/ingestion.py (L254-389) — `ingest()` returns a DataFrame with these columns that score_risk MUST consume:
- `student_id` (StringDtype), `student_name` (StringDtype), `campus_id` (StringDtype), `parent_phone` (StringDtype), `facilitator_email` (StringDtype) — metadata, passed through unchanged
- `session_total_min` (Float64) — sum across 14 days; not used directly by score_risk
- `practice_total_q` (Float64) — used by D-02 formula
- `attendance_days` (Float64) — count of days where session_attended_min > 0; used by D-01 formula
- `daily_session_series` (Python list of np.float64 per row, length ≤14, OR float NaN for students with no metrics rows — Pitfall 4 in RESEARCH.md)
- `daily_practice_series` (Python list or NaN) — not used by score_risk
- `daily_dates` (Python list[str] or NaN) — not used by score_risk
- `latest_note_date` (datetime64[ns] or NaT) — used by D-04 formula
- `latest_note_text` (StringDtype or NA) — not used by score_risk; passed through

Locked signature (STATE.md L110): `score_risk(df: pd.DataFrame) -> pd.DataFrame`. Current stub: src/risk_engine.py L13-29 raises `NotImplementedError("Phase 2")`. Replace the function body; keep the import block at L1-10 intact (already has `import logging`, `import pandas as pd`, `logger = logging.getLogger(__name__)`).

Config constants available after Plan 02-01:
- Weights (must sum to 1.0): cfg.WEIGHT_ATTENDANCE (0.35), cfg.WEIGHT_PRACTICE (0.30), cfg.WEIGHT_TREND (0.20), cfg.WEIGHT_NOTES (0.15)
- Thresholds: cfg.RISK_THRESHOLD_CRITICAL (75), cfg.RISK_THRESHOLD_HIGH (50), cfg.RISK_THRESHOLD_MEDIUM (25)
- RISK-07 domain columns: cfg.COL_ATTENDANCE_RATE, cfg.COL_AVG_PRACTICE, cfg.COL_TREND_DIR, cfg.COL_DAYS_SINCE_NOTE, cfg.COL_RISK_SCORE, cfg.COL_RISK_LEVEL, cfg.COL_RECOMMENDED_ACTION
- D-09 component columns: cfg.COL_ATTENDANCE_COMPONENT, cfg.COL_PRACTICE_COMPONENT, cfg.COL_TREND_COMPONENT, cfg.COL_NOTES_COMPONENT

Reference implementation skeleton — see 02-RESEARCH.md L334-486 (full code block). The executor MAY use this as a starting template but must (a) verify every line against the latest CONTEXT.md decisions, (b) use cfg constants for EVERY column reference and EVERY weight/threshold, (c) NOT inline the four input column names as bare strings unless they appear in the test_no_bare_column_strings_in_risk_engine allowed set.

main.py current state (L61-70): Phase 1 ingest is wired; Phase 2 is commented out at L69-70: `# Phase 2: Risk scoring (wired in plan 02-02)\n# df = risk_engine.score_risk(df)`. Replace those two commented lines with a real import (added at top L12-13) plus a real call site.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement src/risk_engine.py — D-01..D-09 pure function with vectorized + .apply hybrid</name>
  <files>src/risk_engine.py</files>
  <read_first>
    src/risk_engine.py (current stub — preserve module docstring intent, import block, logger initialization; replace body of score_risk)
    src/config.py (all 21 COL_* constants, 4 WEIGHT_* constants, 3 RISK_THRESHOLD_* constants — single source of truth for every name/value)
    src/ingestion.py (L254-389 — score_risk input contract; particularly L319-345 groupby agg that produces the list-column structure)
    tests/test_risk_engine.py (created in Plan 02-01 — these tests describe the EXACT expected behavior; implementation must turn them GREEN)
    .planning/phases/02-risk-scoring-engine/02-CONTEXT.md (D-01..D-09 — every formula, edge case, and label)
    .planning/phases/02-risk-scoring-engine/02-RESEARCH.md (Patterns 1-5 starting L176; Pitfalls 1-8 starting L280; Code Examples L334-486 — reference skeleton)
    .planning/phases/02-risk-scoring-engine/02-PATTERNS.md (src/risk_engine.py section L21-145 — exact analog patterns from src/ingestion.py to mirror)
    .planning/phases/02-risk-scoring-engine/02-01-SUMMARY.md (Plan 02-01 deviation log — note any extra cfg constants or fixture decisions)
    CLAUDE.md (type hints, docstrings, zero print statements, RISK-08 column discipline, pandas 2.2.3 CoW)
  </read_first>
  <behavior>
    src/risk_engine.py must implement `score_risk(df)` such that every test in tests/test_risk_engine.py passes. Specifically:

    Module-level constants (private, leading underscore, with type annotation, per src/ingestion.py L31-53 pattern):
    - `_ACTION_BY_LEVEL: dict[str, str]` mapping each of "CRITICAL"/"HIGH"/"MEDIUM"/"LOW" to its D-08 string ("Contact parent immediately" / "Schedule check-in this week" / "Monitor closely" / "On track")
    - `_RISK_BINS: list` = [-np.inf, cfg.RISK_THRESHOLD_MEDIUM, cfg.RISK_THRESHOLD_HIGH, cfg.RISK_THRESHOLD_CRITICAL, np.inf]
    - `_RISK_LABELS: list[str]` = ["LOW", "MEDIUM", "HIGH", "CRITICAL"]
    - `_WINDOW_DAYS: int = 14` (D-01/D-02 fixed denominator)
    - `_PRACTICE_CAP: float = 15.0` (D-02 cap — 15 questions/day)
    - `_NOTES_MAX_DAYS: int = 30` (D-04 cap — 30 days max penalty)
    - `_TREND_NEUTRAL: float = 50.0` (D-03 fallback for series with < 3 values)

    Private helpers (all with type hints + minimal docstring citing decision ID):
    - `_attendance_component(df: pd.DataFrame) -> pd.Series` — vectorized D-01: `((1 - df[\"attendance_days\"].astype(float) / 14) * 100).clip(0, 100)`
    - `_practice_component(df: pd.DataFrame) -> pd.Series` — vectorized D-02: `avg = df[\"practice_total_q\"].astype(float) / 14`; then `((1 - (avg / 15)).clip(lower=0) * 100).clip(0, 100)`
    - `_trend_component_and_direction(df: pd.DataFrame) -> tuple[pd.Series, pd.Series]` — one .apply pass over `df["daily_session_series"]`; inner function guards `not isinstance(series, list) or len(series) < 3 → (50.0, "stable")`; uses `np.nanmean(series[-3:])` and `np.nanmean(series[:11])`; returns (100.0, "declining") if last3 < first11, (0.0, "improving") if last3 > first11, (0.0, "stable") if equal. Returns two Series: component (float) and direction (string).
    - `_days_since_last_note(df: pd.DataFrame, today: pd.Timestamp) -> pd.Series` — vectorized D-04 helper: `(today - df["latest_note_date"]).dt.days.fillna(30).clip(lower=0, upper=30).astype(float)`. `today` is a parameter so tests can inject (Assumption A2 in RESEARCH.md). Default behavior: caller passes `pd.Timestamp.now().normalize()` from score_risk body.
    - `_notes_component(days_since: pd.Series) -> pd.Series` — vectorized D-04: `(days_since.clip(upper=30) / 30 * 100).clip(0, 100)`

    Public `score_risk(df: pd.DataFrame) -> pd.DataFrame` body:
    1. `df = df.copy()` at function entry (purity guarantee — preserves df.attrs in pandas 2.2.3 per Pitfall 8)
    2. `today = pd.Timestamp.now().normalize()` — called ONCE (Pitfall 7 — never inside .apply)
    3. Compute RISK-07 domain columns and assign to df:
       - `df[cfg.COL_ATTENDANCE_RATE] = df["attendance_days"].astype(float) / 14`
       - `df[cfg.COL_AVG_PRACTICE] = df["practice_total_q"].astype(float) / 14`
       - `df[cfg.COL_DAYS_SINCE_NOTE] = _days_since_last_note(df, today)`
    4. Compute D-09 component scores and assign:
       - `df[cfg.COL_ATTENDANCE_COMPONENT] = _attendance_component(df)`
       - `df[cfg.COL_PRACTICE_COMPONENT] = _practice_component(df)`
       - `(trend_c, trend_d) = _trend_component_and_direction(df)`; `df[cfg.COL_TREND_COMPONENT] = trend_c`; `df[cfg.COL_TREND_DIR] = trend_d`
       - `df[cfg.COL_NOTES_COMPONENT] = _notes_component(df[cfg.COL_DAYS_SINCE_NOTE])`
    5. Compute D-05 weighted sum: `df[cfg.COL_RISK_SCORE] = (df[cfg.COL_ATTENDANCE_COMPONENT] * cfg.WEIGHT_ATTENDANCE + df[cfg.COL_PRACTICE_COMPONENT] * cfg.WEIGHT_PRACTICE + df[cfg.COL_TREND_COMPONENT] * cfg.WEIGHT_TREND + df[cfg.COL_NOTES_COMPONENT] * cfg.WEIGHT_NOTES).round(2).clip(0, 100)`
    6. Compute D-06 risk level via `pd.cut(df[cfg.COL_RISK_SCORE], bins=_RISK_BINS, labels=_RISK_LABELS, right=False).astype("string")`
    7. Compute D-08 recommended action via `df[cfg.COL_RISK_LEVEL].map(_ACTION_BY_LEVEL).astype("string")`
    8. Emit ONE aggregate-only logger.info line: `f"Scored {len(df)} students — CRITICAL={N}, HIGH={N}, MEDIUM={N}, LOW={N}"` where Ns are `(df[cfg.COL_RISK_LEVEL] == 'CRITICAL').sum()` etc. NO student-level identifiers in log output.
    9. Return df.

    Docstring on score_risk must (a) cite decision IDs D-01..D-09, (b) enumerate every column added (11 columns), (c) note the wall-clock dependence on pd.Timestamp.now() and recommend freezegun in tests, (d) state "Pure function — caller's DataFrame is never mutated".

    Column-name discipline (RISK-08):
    - EVERY output column reference uses a cfg.COL_* constant.
    - Input columns "attendance_days", "practice_total_q", "daily_session_series", "latest_note_date" are bare strings (they are Phase 1 internal names not exposed via cfg; they appear in the test_no_bare_column_strings_in_risk_engine allowed set).
    - D-07 string labels "declining", "improving", "stable" are bare strings in the inner _compute function (also in the allowed set — they ARE the value, not column names).
    - The four risk_level labels "CRITICAL", "HIGH", "MEDIUM", "LOW" appear in _RISK_LABELS list and _ACTION_BY_LEVEL keys; the source-scan regex `r'"([a-z][a-z0-9_]{3,})"'` is case-sensitive on the first character (lowercase only), so UPPERCASE labels do not match — they pass the scan without needing to be in the allowed set.

    Code-quality rules (CLAUDE.md):
    - Type hints on EVERY function (public and private).
    - Docstring on `score_risk` (full); private helpers may have one-line docstrings.
    - Zero `print()` statements. Only `logger.info` (no `logger.warning` expected — pure scoring has no quality issues to surface).
    - No file I/O, no network, no os.environ access.
  </behavior>
  <action>
    Open src/risk_engine.py. PRESERVE the existing module docstring (L1-5), import block (L6-9), and `logger = logging.getLogger(__name__)` line (L10) — but EXTEND the docstring to add a "Patterns applied (02-RESEARCH.md)" pointer block matching src/ingestion.py L1-22 style. ADD `import numpy as np` (between `import pandas as pd` and the logger line) and `from src import config as cfg` (after numpy). The existing `import pandas as pd` stays.

    REPLACE the body of `score_risk` (currently L13-29 raising NotImplementedError) with the full implementation per <behavior>. Add the 5 private helpers and 7 module-level private constants ABOVE the public `score_risk` function. Follow the section-comment delimiter pattern from src/ingestion.py:

    `# ------------------------------------------------------------------`
    `# D-XX <heading>`
    `# ------------------------------------------------------------------`

    Use these delimiters to separate: module-level constants block, helper function block, public function block, and (inside score_risk body) the RISK-07 column block / D-09 component block / D-05 weighted sum / D-06 level / D-08 action.

    Use cfg constants for EVERY output column name. Use cfg constants for EVERY weight and threshold. Use the four allowed-set bare strings for INPUT columns ("attendance_days", "practice_total_q", "daily_session_series", "latest_note_date") and for D-07 labels ("declining", "improving", "stable"). Do not introduce any other bare snake_case strings — the RISK-08 source-scan test will fail if you do.

    The `_days_since_last_note` helper takes `today` as a parameter (not internally calling `pd.Timestamp.now()`) so tests can inject a fixed timestamp under @freeze_time. Inside `score_risk`, call `pd.Timestamp.now().normalize()` ONCE at the top of the function and pass that to `_days_since_last_note`.

    Final aggregate log line: `logger.info(f"Scored {len(df)} students — CRITICAL={(df[cfg.COL_RISK_LEVEL] == 'CRITICAL').sum()}, HIGH={(df[cfg.COL_RISK_LEVEL] == 'HIGH').sum()}, MEDIUM={(df[cfg.COL_RISK_LEVEL] == 'MEDIUM').sum()}, LOW={(df[cfg.COL_RISK_LEVEL] == 'LOW').sum()}")`. Do NOT include any student_name, student_id, or parent_phone in this or any other log message.

    Do NOT add any pd.concat, pd.merge, or pd.melt calls — they may drop df.attrs (Pitfall 8). The function only assigns columns to a copied df.
  </action>
  <verify>
    <automated>py -3.12 -m pytest tests/test_risk_engine.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/risk_engine.py contains `def score_risk(df: pd.DataFrame) -> pd.DataFrame:` (signature unchanged from stub)
    - src/risk_engine.py contains `from src import config as cfg`
    - src/risk_engine.py contains `import numpy as np`
    - src/risk_engine.py contains `df = df.copy()` (purity guarantee at function entry)
    - src/risk_engine.py contains `pd.cut(` (D-06 threshold ladder via pd.cut, not if/elif chain)
    - src/risk_engine.py contains `right=False` (D-06 left-closed intervals — Pitfall 1)
    - src/risk_engine.py contains `cfg.WEIGHT_ATTENDANCE` AND `cfg.WEIGHT_PRACTICE` AND `cfg.WEIGHT_TREND` AND `cfg.WEIGHT_NOTES` (D-05 weights from config, not hardcoded)
    - src/risk_engine.py contains `cfg.RISK_THRESHOLD_CRITICAL` AND `cfg.RISK_THRESHOLD_HIGH` AND `cfg.RISK_THRESHOLD_MEDIUM` (D-06 thresholds from config)
    - src/risk_engine.py does NOT contain the literal text `raise NotImplementedError` (stub replaced)
    - src/risk_engine.py does NOT contain any `print(` call (CLAUDE.md zero print statements)
    - src/risk_engine.py does NOT contain `open(` or `requests.` or `http` (no I/O, no network)
    - `py -3.12 -m pytest tests/test_risk_engine.py -x -q` exits 0 (ALL ≥14 tests pass; the RED tests from Plan 02-01 are now GREEN)
    - `py -3.12 -m pytest tests/test_risk_engine.py::test_no_bare_column_strings_in_risk_engine -q` exits 0 (RISK-08 source-scan clean)
    - `py -3.12 -m pytest tests/test_risk_engine.py::test_worst_student_is_critical -q` exits 0
    - `py -3.12 -m pytest tests/test_risk_engine.py::test_perfect_student_is_low -q` exits 0
    - `py -3.12 -m pytest tests/test_risk_engine.py::test_pure_function_does_not_mutate_input -q` exits 0
    - `py -3.12 -m pytest tests/test_risk_engine.py::test_pii_safe_logging_in_score_risk -q` exits 0
    - `py -3.12 -m pytest tests/ -q` exits 0 (full suite: Phase 1's 25 + Phase 2's 14+ = ≥39 tests, all green)
  </acceptance_criteria>
  <done>score_risk implemented; ALL tests in tests/test_risk_engine.py pass; full Phase 1 + Phase 2 test suite green; RISK-01..RISK-08 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire score_risk into main.py orchestrator between ingest and Phase 3 stub</name>
  <files>main.py</files>
  <read_first>
    main.py (current state — L12 has `from src.ingestion import ingest`; L61-67 calls ingest; L69-70 has the Phase 2 stub comments to replace)
    src/risk_engine.py (after Task 1 — confirms `score_risk` is importable as a top-level symbol from `src.risk_engine`)
    tests/test_risk_engine.py (confirms tests pass after Task 1 — gate before wiring)
    .planning/phases/02-risk-scoring-engine/02-PATTERNS.md (main.py section L321-353 — exact wiring pattern)
    CLAUDE.md (main.py orchestrator policy: "Pure coordination only — zero business logic")
  </read_first>
  <behavior>
    - main.py imports score_risk via `from src.risk_engine import score_risk` (added alongside existing `from src.ingestion import ingest` at L12)
    - The L69-70 commented stub (`# Phase 2: Risk scoring (wired in plan 02-02)\n# df = risk_engine.score_risk(df)`) is REPLACED with a real call: `df = score_risk(df)` followed by `logger.info(f"Scored {len(df)} students")`
    - The L66 `run_log["data_quality_warnings"] = df.attrs.get("data_quality_warnings", [])` line stays where it is (before Phase 2 call) — Phase 2 preserves df.attrs but the warnings were captured at ingestion time anyway
    - Phase 3 + Phase 4 stub comments at L72-79 remain commented (those phases are not yet implemented)
    - `py -3.12 main.py` exits 0 and stderr log contains both "Ingested {N} students" and "Scored {N} students" lines
  </behavior>
  <action>
    Open main.py. At L12 (currently `from src.ingestion import ingest`), add a new import line directly below it: `from src.risk_engine import score_risk`. Keep the existing import alphabetic / proximity grouping (both are `from src.X import Y` style).

    Replace the two-line commented stub at L69-70:
    Old text: `# Phase 2: Risk scoring (wired in plan 02-02)\n    # df = risk_engine.score_risk(df)`
    New text: `# Phase 2: Risk scoring\n    df = score_risk(df)\n    logger.info(f"Scored {len(df)} students")`

    Match the indentation of the surrounding code (4-space indent inside the `main()` function body). Keep blank lines around the Phase 2 block consistent with the Phase 1 block above (L61-67).

    Do NOT modify any other line in main.py. Do NOT remove the Phase 3 / Phase 4 stub comments at L72-79. Do NOT touch the run_log dict initialization at L44-52. The data_quality_warnings capture at L66 stays exactly where it is (it captures Phase 1 ingestion warnings, which is its only purpose; Phase 2 produces no warnings).

    After saving, run `py -3.12 main.py` once locally to confirm it does not raise. (Note: it requires data/*.csv to exist — Phase 1 plan 01-02 generates these via `py -3.12 -m src.generate_data` if missing. If data/ is empty, the executor MAY skip the run-main check and rely on test green as the gate; pytest does not depend on data files.)
  </action>
  <verify>
    <automated>py -3.12 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - main.py contains the literal text `from src.risk_engine import score_risk`
    - main.py contains the literal text `df = score_risk(df)`
    - main.py contains the literal text `logger.info(f"Scored {len(df)} students")`
    - main.py does NOT contain the literal text `# df = risk_engine.score_risk(df)` (the commented stub is fully removed)
    - main.py does NOT contain the literal text `# Phase 2: Risk scoring (wired in plan 02-02)` (stub comment removed)
    - `py -3.12 -c "import ast; ast.parse(open('main.py').read())"` exits 0 (syntactically valid Python)
    - `py -3.12 -m pytest tests/ -q` exits 0 (full suite green; main.py changes do not regress any test — main.py is not imported by tests, but pytest may collect it via discovery; no failure expected)
    - `py -3.12 main.py` exits 0 IF data/student_metadata.csv exists (skip this check if data/ is empty — pytest gate is sufficient); log output to stderr contains the substrings `Ingested ` AND `Scored ` AND `Pipeline complete`
  </acceptance_criteria>
  <done>main.py wires score_risk after ingest; full pipeline runs end-to-end through Phase 2; full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest() DataFrame → score_risk() | In-process handoff. Phase 1 already sanitized CSV inputs; dtypes are locked. score_risk treats input as trusted. |
| score_risk() return value → logger | Aggregate counts only flow to logger. Per-student data (name, phone) MUST NOT appear in log output (Security V7). |
| score_risk() → caller's DataFrame reference | Purity boundary — caller's df must NOT be mutated. Enforced via df.copy() at function entry and asserted by test_pure_function_does_not_mutate_input. |
| risk_engine module → src/config | Read-only access to constants. No mutation of cfg state. |
| risk_engine module → filesystem / network | NO boundary crossing — pure CPU function. Asserted by code-review test (no `open(`, no `requests.`, no `http`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | score_risk mutates caller's DataFrame (CoW surprise) | mitigate | `df = df.copy()` at function entry; test_pure_function_does_not_mutate_input asserts caller's df columns unchanged AND `result is not df`. Defense-in-depth against pandas 2.2.3 SettingWithCopyWarning bugs. |
| T-02-06 | Tampering | df.attrs["data_quality_warnings"] dropped by score_risk (Phase 4 silently loses data quality log) | mitigate | df.copy() preserves attrs in pandas 2.2.3 (verified via Assumption A3 in RESEARCH.md). test_df_attrs_preserved asserts this; if pandas behavior changes, test goes RED and we add explicit `result.attrs = df.attrs` rescue. |
| T-02-07 | Information Disclosure | logger.info leaks student_name or parent_phone | mitigate | score_risk emits exactly ONE log line at function exit: aggregate counts only (CRITICAL/HIGH/MEDIUM/LOW counts + len(df)). NO per-student loop with logger calls. test_pii_safe_logging_in_score_risk asserts neither student_name nor parent_phone appears in caplog records. |
| T-02-08 | Repudiation | Threshold drift between cfg and risk_engine (e.g., risk_engine hardcodes 75 but cfg drifts to 80) | mitigate | _RISK_BINS list references cfg.RISK_THRESHOLD_* — single source of truth. RISK-08 source-scan test would NOT catch numeric drift (only string drift), but test_risk_threshold_constants (in test_config.py) asserts cfg values are still 25/50/75; if a developer changes cfg, that test breaks first. |
| T-02-09 | Repudiation | Float precision causes boundary flicker (e.g., score 75.0 lands in HIGH instead of CRITICAL) | mitigate | D-05 .round(2) is applied AFTER the weighted sum and BEFORE pd.cut. pd.cut(..., right=False) is left-closed: 75.0 → CRITICAL deterministically. test_risk_level_boundaries (parametrized 8 cases) locks the contract at endpoints. |
| T-02-10 | Denial of Service | .apply on daily_session_series is O(N) Python per row — could be slow at scale | accept | At 300-2000 students (v1 target per RESEARCH.md), .apply completes in < 1 sec. If v2 hits 10k+ students, replace with vectorized 2-D numpy slice (Alternatives Considered in RESEARCH.md). Not blocking for v1. |
| T-02-11 | Repudiation | RISK-08 source-scan false negative (allowlist too permissive, hides real bare strings) | mitigate | Allowed set is exhaustively enumerated and tied to documented exceptions (D-07 labels + Phase 1 internal column names). Each entry in the allowed set has a comment citing its source. Reviewers can spot-check by reading the test. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Plan adds NO new dependencies. pandas==2.2.3, numpy (transitive), freezegun==1.5.5, pytest==8.3.5 are all already pinned in Phase 1's requirements.txt and verified clean by slopcheck on 2026-05-23 (per RESEARCH.md L122-128). No legitimacy checkpoint needed. |
</threat_model>

<verification>
After both tasks in this plan complete:
- `py -3.12 -m pytest tests/test_risk_engine.py -x -q` → exit 0 (ALL ≥14 Phase 2 tests pass)
- `py -3.12 -m pytest tests/test_config.py -x -q` → exit 0 (no regression)
- `py -3.12 -m pytest tests/test_ingestion.py -x -q` → exit 0 (no regression)
- `py -3.12 -m pytest tests/ -q` → exit 0 (full suite ≥39 tests green)
- `py -3.12 -c "from src.risk_engine import score_risk; import inspect; print(inspect.signature(score_risk))"` prints `(df: pandas.core.frame.DataFrame) -> pandas.core.frame.DataFrame` (locked signature preserved)
- `py -3.12 main.py` (with data/*.csv present from Phase 1 plan 01-02) → exit 0; stderr log contains the substrings "Ingested ", "Scored ", "CRITICAL=", "HIGH=", "MEDIUM=", "LOW=", "Pipeline complete"
- `grep -E '"[a-z][a-z0-9_]{3,}"' src/risk_engine.py | grep -v '^\s*#' | grep -v '"""'` returns ONLY the allowed set strings (declining, improving, stable, attendance_days, practice_total_q, daily_session_series, latest_note_date) — manual cross-check on the RISK-08 source-scan.
</verification>

<success_criteria>
- ROADMAP Phase 2 Success Criterion 1: ✓ score_risk(df) returns DataFrame with risk_score, risk_level, attendance_rate, avg_practice_questions, trend_direction, days_since_last_note (asserted by test_required_output_columns_present)
- ROADMAP Phase 2 Success Criterion 2: ✓ Worst student (0 attendance, 0 practice, declining, no note) → risk_score >= 75 AND risk_level == "CRITICAL" (asserted by test_worst_student_is_critical under freeze_time)
- ROADMAP Phase 2 Success Criterion 3: ✓ Perfect student (full attendance, full practice, improving, recent note) → risk_score < 25 AND risk_level == "LOW" (asserted by test_perfect_student_is_low)
- ROADMAP Phase 2 Success Criterion 4: ✓ No bare column strings in src/risk_engine.py (asserted by test_no_bare_column_strings_in_risk_engine)
- RISK-01 through RISK-08: ✓ All 8 requirements satisfied — each has a dedicated test function
- Pure function discipline: ✓ test_pure_function_does_not_mutate_input + test_df_attrs_preserved both GREEN
- Security V7 PII discipline: ✓ test_pii_safe_logging_in_score_risk asserts no leak
- main.py wiring: ✓ Pipeline runs end-to-end through Phase 2; Phase 3/4 stubs untouched
</success_criteria>

<output>
Create `.planning/phases/02-risk-scoring-engine/02-02-SUMMARY.md` when done documenting:
- Line count of final src/risk_engine.py
- Test result summary: total tests in tests/test_risk_engine.py, all GREEN, full suite count and result
- Any deviation from the reference skeleton in 02-RESEARCH.md (e.g., extra helper, different vectorization approach)
- Any cfg constants added beyond the four from Plan 02-01 (expected: zero — but log if needed)
- Performance: time to run `py -3.12 main.py` on the synthetic 300-student dataset
- Confirmation of D-08 strings matching exactly: "Contact parent immediately", "Schedule check-in this week", "Monitor closely", "On track"
- Risk-level distribution from the synthetic data run (CRITICAL/HIGH/MEDIUM/LOW counts) — informs Phase 3 LLM cost estimate (Open Question in STATE.md L92)
</output>
