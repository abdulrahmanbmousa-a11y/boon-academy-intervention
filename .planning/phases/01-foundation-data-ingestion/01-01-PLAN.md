---
phase: 01-foundation-data-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - requirements-dev.txt
  - .env.example
  - .gitignore
  - Makefile
  - make.ps1
  - README.md
  - src/__init__.py
  - src/config.py
  - src/risk_engine.py
  - src/llm_engine.py
  - src/output_generator.py
  - main.py
  - tests/__init__.py
  - tests/test_config.py
  - tests/test_no_hardcoded_paths.py
  - tests/test_package_structure.py
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-06
  - INFRA-07
  - INFRA-08
  - INFRA-09
tags:
  - scaffolding
  - config
  - env-vars
  - python
user_setup:
  - service: anthropic
    why: "Claude API key is required at config import time (D-08); pipeline will fail loudly on startup if absent. Phase 1 only validates presence — no API call is made until Phase 3."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> https://console.anthropic.com/settings/keys"
    dashboard_config: []
must_haves:
  truths:
    - "D-08: Importing src.config raises KeyError immediately if ANTHROPIC_API_KEY is not in environment (fail-loud via os.environ)"
    - "D-08: Importing src.config with ANTHROPIC_API_KEY set succeeds and exposes DATA_DIR/OUTPUT_DIR/DOCS_DIR Path objects (paths use os.getenv with defaults — NOT fail-loud)"
    - "D-07: Importing src.config exposes COL_STUDENT_ID, COL_PARENT_PHONE, COL_RISK_SCORE, RISK_THRESHOLD_CRITICAL, WEIGHT_ATTENDANCE and 18 other constants required by Phases 2-8 (all constants defined in config.py from day 1: column names + risk thresholds + weights)"
    - "D-05: src/output_generator.py stub exposes the locked signature write_outputs(df, output_dir) -> dict[str, Path] so the Phase 4 run_log.json full schema (run_timestamp, students_processed, api_calls_made, tokens_used, errors_encountered, fallbacks_triggered, data_quality_warnings) can be wired in via that boundary from day 1"
    - "D-06: main.py orchestrator scaffolds an in-memory log dict pattern (built throughout the run, written once at pipeline end via output_generator) — no per-step log file writes anywhere in Phase 1"
    - "Running `python main.py` with empty data/ logs 'Starting boon-academy-intervention pipeline' and exits cleanly (or raises FileNotFoundError from ingest only)"
    - "`pip install -r requirements.txt -r requirements-dev.txt --dry-run` resolves with zero conflicts"
    - "make demo / make test / make clean targets exist in Makefile AND identical targets work via ./make.ps1 on Windows"
    - "README.md is 30 lines or fewer and contains 'Quick Start' and 'ANTHROPIC_API_KEY'"
    - "src is importable as a Python package (src/__init__.py exists)"
    - "Grep for hardcoded 'data', 'outputs', 'docs' string literals in src/*.py (excluding src/config.py) returns zero matches"
  artifacts:
    - path: "src/config.py"
      provides: "Env var loading, fail-loud secret validation, all column-name and risk-threshold constants"
      contains: "ANTHROPIC_API_KEY"
      contains_any: ["COL_STUDENT_ID", "RISK_THRESHOLD_CRITICAL", "WEIGHT_ATTENDANCE"]
    - path: "main.py"
      provides: "Pipeline orchestrator skeleton — pure coordination, logging setup, calls ingest()"
      contains: "logging.basicConfig"
    - path: "requirements.txt"
      provides: "Pinned production dependencies"
      contains: "pandas==2.2.3"
    - path: "requirements-dev.txt"
      provides: "Pinned test dependencies"
      contains: "pytest==8.3.5"
    - path: ".env.example"
      provides: "Documented env var template (committed); .env is gitignored"
      contains: "ANTHROPIC_API_KEY"
    - path: ".gitignore"
      provides: "Ignore .env, outputs/, data/, __pycache__/"
      contains: ".env"
    - path: "Makefile"
      provides: "make demo / make test / make clean targets"
      contains: "demo:"
    - path: "make.ps1"
      provides: "PowerShell fallback mirroring Makefile targets (Windows lacks make)"
      contains: "param"
    - path: "README.md"
      provides: "Fresh-clone install + run instructions, <= 30 lines"
      max_lines: 30
    - path: "src/__init__.py"
      provides: "Marks src/ as a Python package"
    - path: "src/risk_engine.py"
      provides: "Phase 2 stub with locked signature score_risk(df) -> DataFrame"
      contains: "NotImplementedError"
    - path: "src/llm_engine.py"
      provides: "Phase 3 stub with locked signature enrich_with_llm(df, api_key) -> DataFrame"
      contains: "NotImplementedError"
    - path: "src/output_generator.py"
      provides: "Phase 4 stub with locked signature write_outputs(df, output_dir) -> dict[str, Path]"
      contains: "NotImplementedError"
  key_links:
    - from: "main.py"
      to: "src.config"
      via: "from src import config as cfg"
      pattern: "from src import config"
    - from: "main.py"
      to: "src.ingestion"
      via: "from src.ingestion import ingest"
      pattern: "from src\\.ingestion import ingest"
    - from: "src/config.py"
      to: "ANTHROPIC_API_KEY env var"
      via: "os.environ['ANTHROPIC_API_KEY']"
      pattern: "os\\.environ\\[\"ANTHROPIC_API_KEY\"\\]"
---

<objective>
Build the project scaffold and configuration layer for boon-academy-intervention. Deliver `src/config.py` as the single source of truth for env vars, paths, column names, risk thresholds, and weight constants. Deliver `main.py` as a pure orchestrator skeleton. Deliver `requirements.txt`, `requirements-dev.txt`, `.env.example`, `.gitignore`, `Makefile`, `make.ps1`, `README.md`, and the `src/` + `tests/` package markers. Create Phase 2/3/4 stubs with `NotImplementedError` so imports stay clean from day 1.

Purpose: Lock the foundation so plan 02 (synthetic data) and plan 03 (ingestion) can run in parallel against a stable config + import graph. Without this scaffold, every downstream plan would duplicate constants and risk drift.

Output: 17 new files. Zero business logic in `main.py` (CLAUDE.md rule). Zero hardcoded paths outside `src/config.py` (INFRA-07). `ANTHROPIC_API_KEY` fails loudly at config import time (D-08, Pitfall #7).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/01-foundation-data-ingestion/01-CONTEXT.md
@.planning/phases/01-foundation-data-ingestion/01-RESEARCH.md
@.planning/phases/01-foundation-data-ingestion/01-PATTERNS.md
@.planning/phases/01-foundation-data-ingestion/01-VALIDATION.md

<interfaces>
<!-- Locked module signatures from STATE.md L93-96. ALL downstream phases depend on these. -->

From src/ingestion.py (created in plan 03):
```python
def ingest(data_paths: dict[str, Path]) -> pd.DataFrame: ...
```

From src/risk_engine.py (Phase 2 — stub here):
```python
def score_risk(df: pd.DataFrame) -> pd.DataFrame: ...
```

From src/llm_engine.py (Phase 3 — stub here):
```python
def enrich_with_llm(df: pd.DataFrame, api_key: str) -> pd.DataFrame: ...
```

From src/output_generator.py (Phase 4 — stub here):
```python
def write_outputs(df: pd.DataFrame, output_dir: Path) -> dict[str, Path]: ...
```

Locked column-name constants from 01-PATTERNS.md L86-110 (must all appear in src/config.py):
- Metadata: COL_STUDENT_ID, COL_STUDENT_NAME, COL_CAMPUS_ID, COL_PARENT_PHONE, COL_FACILITATOR_EMAIL
- Metrics: COL_METRIC_DATE, COL_SESSION_MIN, COL_PRACTICE_Q
- Notes: COL_NOTE_DATE, COL_NOTE_TEXT
- Derived: COL_ATTENDANCE_RATE, COL_AVG_PRACTICE, COL_TREND_DIR, COL_DAYS_SINCE_NOTE, COL_RISK_SCORE, COL_RISK_LEVEL, COL_RECOMMENDED_ACTION

Locked numeric constants from CONTEXT.md D-07:
- RISK_THRESHOLD_CRITICAL=75, RISK_THRESHOLD_HIGH=50, RISK_THRESHOLD_MEDIUM=25
- WEIGHT_ATTENDANCE=0.35, WEIGHT_PRACTICE=0.30, WEIGHT_TREND=0.20, WEIGHT_NOTES=0.15
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — verify pinned dependencies</name>
  <files>(verification only — no file writes)</files>
  <read_first>
    - .planning/phases/01-foundation-data-ingestion/01-RESEARCH.md (Package Legitimacy Audit section, L168-194)
    - CLAUDE.md (Key Technical Decisions section — confirms pandas 2.2.3, openpyxl, anthropic, respx pins)
  </read_first>
  <what-built>
    Researcher tagged all 13 pinned packages as [ASSUMED] because slopcheck was unavailable. Two name-confusion landmines flagged:
    1. `python-docx` (CORRECT) — NOT `docx` (abandoned, produces broken files)
    2. `python-dotenv` (CORRECT) — NOT `dotenv` (unmaintained)

    Before `pip install` runs (in later task), human must visually confirm each package exists at its pinned version on the official registry.
  </what-built>
  <how-to-verify>
    Open each of these PyPI URLs in a browser and confirm the pinned version appears in the release history:

    1. https://pypi.org/project/pandas/2.2.3/         (expect: pandas — Python Data Analysis Library)
    2. https://pypi.org/project/openpyxl/3.1.5/       (expect: openpyxl — A Python library to read/write Excel)
    3. https://pypi.org/project/python-docx/1.1.2/    (NOT pypi.org/project/docx — confirm the URL has "python-docx")
    4. https://pypi.org/project/anthropic/0.103.1/    (expect: Anthropic, Inc. as author/maintainer)
    5. https://pypi.org/project/python-dotenv/1.2.2/  (NOT pypi.org/project/dotenv)
    6. https://pypi.org/project/tenacity/9.1.4/
    7. https://pypi.org/project/Jinja2/3.1.6/
    8. https://pypi.org/project/pytest/8.3.5/
    9. https://pypi.org/project/pytest-mock/3.15.1/
    10. https://pypi.org/project/pytest-cov/7.1.0/
    11. https://pypi.org/project/respx/0.23.1/
    12. https://pypi.org/project/freezegun/1.5.5/
    13. https://pypi.org/project/coverage/7.14.0/

    Confirm none of the package detail pages show a "deprecated", "yanked", or "compromised" notice.
  </how-to-verify>
  <action>
    Pause execution and present the 13 PyPI URLs to the user. Do NOT proceed with `pip install` (Task 2) until user explicitly approves. This is per the Package Legitimacy Gate (T-01-SC threat) — checkpoints are never auto-approvable for [ASSUMED] packages.
  </action>
  <verify>
    <human-check>User types "approved" after confirming all 13 packages exist at pinned versions on official PyPI URLs (especially python-docx and python-dotenv name confusion landmines).</human-check>
  </verify>
  <acceptance_criteria>
    - User responds "approved" before any `pip install` runs
    - If user reports a package issue, halt and surface the problem rather than proceeding
  </acceptance_criteria>
  <done>Human has visually verified all 13 PyPI URLs and approved proceeding.</done>
  <resume-signal>Type "approved" to proceed to Task 2 (write requirements files + scaffold).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write requirements files, env template, gitignore, README, Makefile, and make.ps1</name>
  <files>
    requirements.txt,
    requirements-dev.txt,
    .env.example,
    .gitignore,
    README.md,
    Makefile,
    make.ps1
  </files>
  <read_first>
    - .planning/phases/01-foundation-data-ingestion/01-RESEARCH.md (Installation L134-153, .env.example L740-756, .gitignore L805-816, Makefile L760-775, make.ps1 L779-801, README L820-847)
    - .planning/phases/01-foundation-data-ingestion/01-PATTERNS.md (sections for each of the 7 files — exact templates with line refs)
    - CLAUDE.md (Quick Start section, Key Technical Decisions — confirms version pins)
  </read_first>
  <action>
    Create all 7 config files using the exact templates from 01-PATTERNS.md.

    `requirements.txt`: pin pandas==2.2.3, openpyxl==3.1.5, python-docx==1.1.2, anthropic==0.103.1, python-dotenv==1.2.2, tenacity==9.1.4, jinja2==3.1.6. One package per line, exact `==` version pins, no ranges. Use `python-docx` and `python-dotenv` — never the bare `docx` or `dotenv` names (Pitfall name-confusion landmines).

    `requirements-dev.txt`: pin pytest==8.3.5, pytest-mock==3.15.1, pytest-cov==7.1.0, respx==0.23.1, freezegun==1.5.5, coverage==7.14.0.

    `.env.example`: include `ANTHROPIC_API_KEY=` (blank value), `DATA_DIR=data`, `OUTPUT_DIR=outputs`, `DOCS_DIR=docs`, `LOG_LEVEL=INFO`, `MAX_STUDENTS_PER_LLM_CALL=10`, `RISK_THRESHOLD_CRITICAL=75`, `RISK_THRESHOLD_HIGH=50`. Add a header comment marking ANTHROPIC_API_KEY as required.

    `.gitignore`: include `.env`, `outputs/`, `data/`, `__pycache__/`, `*.pyc`, `*.egg-info/`, `.pytest_cache/`, `.coverage`, `htmlcov/`, `.DS_Store`.

    `README.md`: 30 lines or fewer. Sections: title, one-paragraph project description, `## Quick Start` with `pip install`/`cp .env.example .env`/`make demo` block, `## What it produces` bullet list of 5 output files (intervention_priority_list.xlsx, facilitator_dashboard_*.xlsx, whatsapp_messages.csv, intervention_report.docx, facilitator_dashboard.html), `## Requirements` noting Python 3.11+ recommended. Use the template from 01-PATTERNS.md L553-580 verbatim.

    `Makefile`: targets `install`, `demo`, `test`, `clean` with `.PHONY` declaration. `demo` runs `python -m src.generate_data` then `python main.py`. `test` runs `pytest tests/ -v`. `clean` removes outputs/, __pycache__, src/__pycache__, .pytest_cache, .coverage. Use tab indentation (Makefile requires tabs, not spaces).

    `make.ps1`: PowerShell switch on `$Target` parameter with `install`, `demo`, `test`, `clean` cases mirroring Makefile. `demo` case must check `$LASTEXITCODE` after `python -m src.generate_data` and exit early on failure. Default case writes error and exits 1.
  </action>
  <verify>
    <automated>python -c "import pathlib; assert all(pathlib.Path(f).exists() for f in ['requirements.txt','requirements-dev.txt','.env.example','.gitignore','README.md','Makefile','make.ps1'])"</automated>
    <automated>python -c "lines=open('README.md').readlines(); assert len(lines) &lt;= 30, f'README has {len(lines)} lines, expected &lt;= 30'"</automated>
    <automated>python -c "txt=open('requirements.txt').read(); assert 'pandas==2.2.3' in txt and 'anthropic==0.103.1' in txt and 'python-dotenv==1.2.2' in txt and 'python-docx==1.1.2' in txt"</automated>
    <automated>python -c "env=open('.env.example').read(); gi=open('.gitignore').read(); assert 'ANTHROPIC_API_KEY' in env and '.env' in gi.split('\n')"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains exactly the 7 pinned production packages listed above
    - requirements-dev.txt contains exactly the 6 pinned test packages listed above
    - Package names use `python-docx` and `python-dotenv` (NOT `docx` or `dotenv`)
    - .env.example contains `ANTHROPIC_API_KEY=` (with no value) plus DATA_DIR, OUTPUT_DIR, DOCS_DIR, LOG_LEVEL, MAX_STUDENTS_PER_LLM_CALL, RISK_THRESHOLD_CRITICAL, RISK_THRESHOLD_HIGH
    - .gitignore contains `.env` on its own line
    - README.md is &lt;= 30 lines (counted with newline)
    - README.md contains the strings "Quick Start" and "ANTHROPIC_API_KEY"
    - Makefile contains `.PHONY: install demo test clean` and targets `demo:`, `test:`, `clean:`
    - Makefile uses tab characters (not spaces) for recipe indentation
    - make.ps1 contains `param([Parameter(Mandatory=$true)][string]$Target)` and a `switch` on `$Target` with `install`, `demo`, `test`, `clean` cases
  </acceptance_criteria>
  <done>All 7 config files exist with correct content. Version pins match RESEARCH.md. README is at or under 30 lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write src/config.py with fail-loud env loading and all constants, plus tests (D-07, D-08)</name>
  <files>
    src/__init__.py,
    src/config.py,
    tests/__init__.py,
    tests/test_config.py,
    tests/test_no_hardcoded_paths.py,
    tests/test_package_structure.py
  </files>
  <read_first>
    - .planning/phases/01-foundation-data-ingestion/01-RESEARCH.md (Pattern 1 L315-374, Pitfall #7 L713-716, Security Domain L961-981)
    - .planning/phases/01-foundation-data-ingestion/01-PATTERNS.md (config.py section L42-115)
    - .planning/phases/01-foundation-data-ingestion/01-CONTEXT.md (D-07, D-08 — locks constant set and fail-loud rule)
    - CLAUDE.md (Critical Pitfalls — os.environ vs os.getenv rule)
    - .planning/REQUIREMENTS.md (INFRA-02, INFRA-07, INFRA-08, INFRA-09)
  </read_first>
  <behavior>
    - Test 1 (test_config.py::test_missing_api_key_raises): With ANTHROPIC_API_KEY removed from environment, `import src.config` raises KeyError. Use `monkeypatch.delenv("ANTHROPIC_API_KEY", raising=False)` and `importlib.reload(src.config)` inside a `pytest.raises(KeyError)` block. (D-08)
    - Test 2 (test_config.py::test_api_key_loads_when_present): With ANTHROPIC_API_KEY set, `src.config.ANTHROPIC_API_KEY` equals that value. (D-08)
    - Test 3 (test_config.py::test_path_defaults): With DATA_DIR/OUTPUT_DIR/DOCS_DIR unset, `src.config.DATA_DIR == Path("data")`, etc. (D-08 — paths use os.getenv with defaults, NOT fail-loud)
    - Test 4 (test_config.py::test_column_constants_defined): assert getattr(cfg, name) is a non-empty str for each of the 17 column constants (COL_STUDENT_ID through COL_RECOMMENDED_ACTION). (D-07)
    - Test 5 (test_config.py::test_risk_threshold_constants): RISK_THRESHOLD_CRITICAL == 75, RISK_THRESHOLD_HIGH == 50, RISK_THRESHOLD_MEDIUM == 25. (D-07)
    - Test 6 (test_config.py::test_weight_constants_sum_to_one): WEIGHT_ATTENDANCE + WEIGHT_PRACTICE + WEIGHT_TREND + WEIGHT_NOTES == 1.0. (D-07)
    - Test 7 (test_no_hardcoded_paths.py::test_no_hardcoded_dirs_in_src): grep src/*.py (excluding src/config.py) for the regex `["\']\b(data|outputs|docs)\b["\']` and assert zero matches. INFRA-07.
    - Test 8 (test_package_structure.py::test_src_is_package): `import src` succeeds and `Path('src/__init__.py').exists()`. INFRA-09.
  </behavior>
  <action>
    Per D-07: ALL constants (column names + risk thresholds + weights) belong in src/config.py from day 1 — single source of truth.
    Per D-08: ONLY `ANTHROPIC_API_KEY` fails loudly via `os.environ[...]`; DATA_DIR/OUTPUT_DIR/DOCS_DIR use `os.getenv(..., default)`.

    First write the 6 test files (RED). Then write src/config.py (GREEN). Both `tests/__init__.py` and `src/__init__.py` are empty files (zero-byte placeholder content is fine — empty marker files are conventional).

    `src/config.py`: Begin with module docstring, then imports: `os`, `pathlib.Path`, `dotenv.load_dotenv`. Call `load_dotenv()` at module level immediately after imports. Then `ANTHROPIC_API_KEY: str = os.environ["ANTHROPIC_API_KEY"]` (fails loudly — D-08, Pitfall #7). Then `DATA_DIR/OUTPUT_DIR/DOCS_DIR` as `Path(os.getenv("...", "default"))` (D-08 — paths use os.getenv with safe string defaults). Then `LOG_LEVEL`, `MAX_STUDENTS_PER_LLM_CALL`. Then risk thresholds per D-07 (RISK_THRESHOLD_CRITICAL=75, RISK_THRESHOLD_HIGH=50 env-overridable; RISK_THRESHOLD_MEDIUM=25 hardcoded). Then weight constants per D-07 (WEIGHT_ATTENDANCE=0.35, WEIGHT_PRACTICE=0.30, WEIGHT_TREND=0.20, WEIGHT_NOTES=0.15). Then all 17 column constants per D-07 grouped by source CSV (metadata, metrics, notes, derived) per 01-PATTERNS.md L86-110. Every constant must have a `: type` annotation per INFRA-08.

    `tests/test_config.py`: Use `monkeypatch` and `importlib.reload(src.config)` to test fail-loud behavior. Set `ANTHROPIC_API_KEY` via fixture for tests that need it loaded.

    `tests/test_no_hardcoded_paths.py`: Walk `src/*.py` (NOT including src/config.py), open each file as text, regex search for quoted literal `"data"`, `"outputs"`, or `"docs"` as standalone tokens. Assert no matches. Use `re.compile(r'["\']\b(data|outputs|docs)\b["\']')`.

    `tests/test_package_structure.py`: assert `Path("src/__init__.py").exists()`, `Path("src/__init__.py").is_file()`, and `import src` works.
  </action>
  <verify>
    <automated>pytest tests/test_config.py -x -v</automated>
    <automated>pytest tests/test_no_hardcoded_paths.py -x -v</automated>
    <automated>pytest tests/test_package_structure.py -x -v</automated>
    <automated>python -c "import os; os.environ.setdefault('ANTHROPIC_API_KEY','dummy'); from src import config as c; assert c.RISK_THRESHOLD_CRITICAL==75 and c.WEIGHT_ATTENDANCE==0.35 and c.COL_STUDENT_ID=='student_id'"</automated>
    <automated>python -c "import os; os.environ.pop('ANTHROPIC_API_KEY',None); import importlib, sys; sys.modules.pop('src.config',None); 
try: 
    from src import config 
    raise SystemExit('config imported without API key — FAIL')
except KeyError: pass"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_config.py exits 0 with at least 6 passing tests covering: missing key raises, key loads, path defaults, column constants, risk thresholds, weight sum
    - tests/test_no_hardcoded_paths.py exits 0 — no `"data"`, `"outputs"`, `"docs"` literals outside src/config.py
    - tests/test_package_structure.py exits 0 — src/__init__.py exists as a file
    - src/config.py contains `os.environ["ANTHROPIC_API_KEY"]` (NOT `os.getenv("ANTHROPIC_API_KEY"`) — D-08
    - src/config.py defines all 17 column constants (D-07): COL_STUDENT_ID, COL_STUDENT_NAME, COL_CAMPUS_ID, COL_PARENT_PHONE, COL_FACILITATOR_EMAIL, COL_METRIC_DATE, COL_SESSION_MIN, COL_PRACTICE_Q, COL_NOTE_DATE, COL_NOTE_TEXT, COL_ATTENDANCE_RATE, COL_AVG_PRACTICE, COL_TREND_DIR, COL_DAYS_SINCE_NOTE, COL_RISK_SCORE, COL_RISK_LEVEL, COL_RECOMMENDED_ACTION
    - src/config.py defines RISK_THRESHOLD_CRITICAL=75, RISK_THRESHOLD_HIGH=50, RISK_THRESHOLD_MEDIUM=25 (D-07)
    - src/config.py defines WEIGHT_ATTENDANCE=0.35, WEIGHT_PRACTICE=0.30, WEIGHT_TREND=0.20, WEIGHT_NOTES=0.15 (sum to 1.0) (D-07)
    - src/config.py calls `load_dotenv()` at module level before reading env vars
    - Every constant in src/config.py has a type annotation (e.g. `ANTHROPIC_API_KEY: str = ...`)
    - src/__init__.py and tests/__init__.py exist and are zero-byte or empty files
  </acceptance_criteria>
  <done>Config module is the single source of truth — every env var, path, column name, threshold, and weight is defined here (D-07). Only ANTHROPIC_API_KEY fails loudly (D-08). All 3 test files pass.</done>
</task>

<task type="auto">
  <name>Task 4: Write Phase 2/3/4 stubs and main.py orchestrator skeleton (D-05, D-06)</name>
  <files>
    src/risk_engine.py,
    src/llm_engine.py,
    src/output_generator.py,
    main.py
  </files>
  <read_first>
    - .planning/phases/01-foundation-data-ingestion/01-RESEARCH.md (Pattern 6 orchestrator L617-654, stubs L307)
    - .planning/phases/01-foundation-data-ingestion/01-PATTERNS.md (main.py L351-389, stubs L398-422)
    - .planning/phases/01-foundation-data-ingestion/01-CONTEXT.md (D-05 run_log schema, D-06 in-memory log dict)
    - .planning/STATE.md (Module contracts L93-96 — LOCKED signatures)
    - CLAUDE.md (Project Structure, Code Standards — zero print, zero business logic in main)
  </read_first>
  <action>
    Per D-05: src/output_generator.py stub locks the signature that will write run_log.json with its full schema (run_timestamp, students_processed, api_calls_made, tokens_used {input, output}, errors_encountered, fallbacks_triggered, data_quality_warnings) — the schema lives in CONTEXT.md D-05; the Phase 4 implementation will materialize it.
    Per D-06: main.py orchestrator scaffolds an in-memory log dict pattern that is built throughout the pipeline run and written once at pipeline end via output_generator — NO incremental file writes. The Phase 1 skeleton wires the call site (even though the call body is NotImplementedError); the in-memory pattern is the convention enforced from this plan forward.

    Create three stub modules in src/ with the locked signatures from STATE.md. Each stub: module docstring, `import pandas as pd` (and `from pathlib import Path` for output_generator), function with full type-hinted signature, docstring noting which phase implements it, body `raise NotImplementedError("Phase N")` where N is 2/3/4. Do NOT add any logic beyond the raise.

    src/risk_engine.py exports `score_risk(df: pd.DataFrame) -> pd.DataFrame` raising `NotImplementedError("Phase 2")`.

    src/llm_engine.py exports `enrich_with_llm(df: pd.DataFrame, api_key: str) -> pd.DataFrame` raising `NotImplementedError("Phase 3")`.

    src/output_generator.py exports `write_outputs(df: pd.DataFrame, output_dir: Path) -> dict[str, Path]` raising `NotImplementedError("Phase 4")`. The docstring on this function MUST reference D-05 / D-06 explicitly: "Writes 6 output files including run_log.json built in-memory by main.py per CONTEXT.md D-05 and D-06."

    main.py: import `logging`, `sys`, `from src import config as cfg`, `from src.ingestion import ingest`. Define `setup_logging() -> None` that calls `logging.basicConfig(level=cfg.LOG_LEVEL, format="%(asctime)s [%(levelname)s] %(name)s: %(message)s", stream=sys.stderr)`. Define `main() -> int` that: calls setup_logging, gets logger via `logging.getLogger("main")`, logs "Starting boon-academy-intervention pipeline", initializes an in-memory `run_log: dict[str, object]` matching D-05 schema (run_timestamp, students_processed=0, api_calls_made=0, tokens_used={"input":0,"output":0}, errors_encountered=[], fallbacks_triggered=0, data_quality_warnings=[]) — per D-06 this dict is built throughout the run and written once at end. Then builds `data_paths = {"metrics": cfg.DATA_DIR / "student_daily_metrics.csv", "notes": cfg.DATA_DIR / "facilitator_notes.csv", "metadata": cfg.DATA_DIR / "student_metadata.csv"}`, calls `df = ingest(data_paths)`, sets `run_log["students_processed"] = len(df)`, logs `f"Ingested {len(df)} students"`, includes a commented block showing where Phase 2-4 calls will be wired (`# df = risk_engine.score_risk(df)`, etc.) and a final `# write_outputs(df, cfg.OUTPUT_DIR, run_log=run_log)` placeholder marking the single write-at-end point (D-06), logs "Pipeline complete", returns 0. Add `if __name__ == "__main__": sys.exit(main())`.

    Zero print statements anywhere (CLAUDE.md). main.py is pure coordination — no DataFrame manipulation, no business logic.
  </action>
  <verify>
    <automated>python -c "import os; os.environ.setdefault('ANTHROPIC_API_KEY','dummy'); from src import risk_engine, llm_engine, output_generator; import pandas as pd; 
try: risk_engine.score_risk(pd.DataFrame()); raise SystemExit('risk_engine did not raise')
except NotImplementedError: pass
try: llm_engine.enrich_with_llm(pd.DataFrame(),'k'); raise SystemExit('llm_engine did not raise')
except NotImplementedError: pass"</automated>
    <automated>python -c "import ast; tree=ast.parse(open('main.py').read()); names=[n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]; assert 'main' in names and 'setup_logging' in names"</automated>
    <automated>python -c "src=open('main.py').read(); assert 'print(' not in src, 'main.py contains print() — forbidden'"</automated>
    <automated>python -c "src=open('main.py').read(); assert 'from src import config' in src and 'from src.ingestion import ingest' in src"</automated>
    <automated>python -c "src=open('main.py').read(); assert 'run_log' in src and 'students_processed' in src and 'tokens_used' in src and 'data_quality_warnings' in src, 'main.py must scaffold the D-05 run_log schema in-memory'"</automated>
  </verify>
  <acceptance_criteria>
    - src/risk_engine.py defines `def score_risk(df: pd.DataFrame) -> pd.DataFrame:` and raises NotImplementedError("Phase 2")
    - src/llm_engine.py defines `def enrich_with_llm(df: pd.DataFrame, api_key: str) -> pd.DataFrame:` and raises NotImplementedError("Phase 3")
    - src/output_generator.py defines `def write_outputs(df: pd.DataFrame, output_dir: Path) -> dict[str, Path]:` and raises NotImplementedError("Phase 4")
    - src/output_generator.py docstring references D-05 (run_log.json schema) and D-06 (write-once at end)
    - main.py imports `from src import config as cfg` and `from src.ingestion import ingest`
    - main.py defines `setup_logging() -> None` calling `logging.basicConfig` with format containing `%(asctime)s [%(levelname)s] %(name)s: %(message)s` and stream `sys.stderr`
    - main.py defines `main() -> int` returning 0
    - main.py initializes an in-memory `run_log` dict with the D-05 keys (run_timestamp, students_processed, api_calls_made, tokens_used, errors_encountered, fallbacks_triggered, data_quality_warnings) — per D-06 it is built in memory and never flushed mid-run
    - main.py contains `if __name__ == "__main__": sys.exit(main())`
    - main.py contains zero `print(` calls
    - main.py builds `data_paths` dict with keys "metrics", "notes", "metadata"
    - All four files use `logging.getLogger` only — no `print` statements
    - Every function in all four files has type-hinted parameters and return type, plus a docstring (INFRA-08)
  </acceptance_criteria>
  <done>main.py is a pure orchestrator with the D-05 run_log schema scaffolded in memory and the D-06 write-once-at-end pattern wired (placeholder call). Phase 2/3/4 stubs raise NotImplementedError with their phase number. Import graph is complete; the only piece still missing is src/ingestion.py (delivered in plan 03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment → Process | OS environment variables cross into Python process at config import time |
| Filesystem → Process | .env file (when present) is read by python-dotenv at module load |
| Developer machine → Repo | Developer runs `pip install` against pinned requirements (supply-chain surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | src/config.py — ANTHROPIC_API_KEY handling | mitigate | Loaded via `os.environ["ANTHROPIC_API_KEY"]` only; never logged, never written to disk, never f-stringed into messages. Tested by tests/test_config.py::test_missing_api_key_raises (asserts KeyError surfaces var name only, not value). |
| T-01-02 | Information Disclosure | .env file at repo root | mitigate | `.gitignore` includes `.env` on its own line; only `.env.example` (template with empty values) is committed. Verified by inspecting `.gitignore`. |
| T-01-03 | Tampering | requirements.txt — package supply chain | mitigate | All 13 packages [ASSUMED] approved per 01-RESEARCH.md Package Legitimacy Audit. Name-confusion landmines (python-docx vs docx, python-dotenv vs dotenv) flagged. Task 1 is a blocking-human checkpoint requiring visual PyPI verification before any `pip install` runs. |
| T-01-04 | Tampering | DATA_DIR / OUTPUT_DIR / DOCS_DIR env vars (path traversal) | accept | Single-operator batch CLI; the only actor who sets these env vars is the developer running the pipeline. No multi-tenancy, no untrusted input. Documented as out-of-scope for v1 in 01-RESEARCH.md Security Domain L979. |
| T-01-05 | Repudiation | Pipeline run logging | mitigate | INFRA-01 mandates `logging` module throughout; main.py configures logging.basicConfig with ISO timestamps. Every pipeline run produces auditable stderr trail. |
| T-01-06 | Information Disclosure | Logging convention | mitigate | Lock convention in plan 01: log only `student_id`, never `student_name`/`parent_phone`/`note_text`. Pattern enforced by code review and 01-RESEARCH.md Security Domain L980. Phase 3 will add masking layer; Phase 1 prevents leakage by never logging PII in the first place. |
| T-01-SC | Tampering | npm/pip/cargo install surface | mitigate | Task 1 is `checkpoint:human-verify` with `gate="blocking-human"`. User must visually confirm 13 PyPI URLs before pip install. Auto-advance is ignored for this checkpoint. |
</threat_model>

<verification>
After all 4 tasks complete:

1. **Fail-loud check** (Pitfall #7): `python -c "import os; os.environ.pop('ANTHROPIC_API_KEY', None); from src import config"` MUST raise KeyError. Run again with the env var set — must succeed.
2. **Stub imports**: `python -c "import os; os.environ.setdefault('ANTHROPIC_API_KEY','x'); from src import risk_engine, llm_engine, output_generator"` succeeds without raising.
3. **main.py runs (smoke)**: With `ANTHROPIC_API_KEY=dummy` and empty `data/` directory, `python main.py` exits with FileNotFoundError from ingest (NOT a config error). This proves config and main.py imports work even before ingestion exists.
4. **Hardcoded paths**: `pytest tests/test_no_hardcoded_paths.py -x` exits 0.
5. **Test suite passes**: `pytest tests/ -x -v` exits 0 (8+ tests across 3 files).
6. **Dependency resolution**: `pip install -r requirements.txt -r requirements-dev.txt --dry-run` reports no conflicts.
7. **README line count**: `python -c "print(sum(1 for _ in open('README.md')))"` prints a number &lt;= 30.
</verification>

<success_criteria>
- All 17 files in `files_modified` exist on disk
- `pytest tests/ -x -v` exits 0
- `src.config` raises KeyError when ANTHROPIC_API_KEY is absent; loads cleanly when present
- `python main.py` imports succeed; pipeline starts and logs "Starting boon-academy-intervention pipeline" before hitting a missing-data error
- No hardcoded `"data"`, `"outputs"`, `"docs"` literals exist in `src/*.py` outside `src/config.py`
- README.md is 30 lines or fewer
- All 13 pinned packages were visually verified on PyPI by the human before Task 2 ran
- All 9 INFRA-* requirements addressed (INFRA-01 main.py skeleton, INFRA-02 config.py, INFRA-03 requirements.txt, INFRA-04 .env.example, INFRA-05 Makefile + make.ps1, INFRA-06 README, INFRA-07 no hardcoded paths verified by test, INFRA-08 type hints + docstrings, INFRA-09 src/__init__.py)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-ingestion/01-01-SUMMARY.md` when done.
</output>
