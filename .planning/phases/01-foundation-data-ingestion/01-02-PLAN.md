---
phase: 01-foundation-data-ingestion
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/generate_data.py
  - tests/test_generate_data.py
  - tests/fixtures/student_metadata_happy.csv
  - tests/fixtures/student_daily_metrics_happy.csv
  - tests/fixtures/facilitator_notes_happy.csv
  - tests/fixtures/student_metadata_with_dupes.csv
  - tests/fixtures/student_daily_metrics_missing_numeric.csv
  - tests/fixtures/student_daily_metrics_bad_dates.csv
  - tests/fixtures/student_metadata_type_mismatch.csv
  - tests/fixtures/empty.csv
  - tests/conftest.py
autonomous: true
requirements:
  - DATA-01
tags:
  - synthetic-data
  - csv-generation
  - test-fixtures
  - numpy-rng
must_haves:
  truths:
    - "D-01: Running `python -m src.generate_data` writes exactly 3 CSV files to data/: student_metadata.csv, student_daily_metrics.csv, facilitator_notes.csv"
    - "D-01: Generated student_metadata.csv contains exactly 300 rows (20 campuses x 15 students)"
    - "D-01: Generated student_daily_metrics.csv contains approximately 4200 rows (300 students x 14 days), allowing for ~5% missing numeric, 3% duplicate ID, 2% type mismatch edge cases"
    - "D-02: Running the generator twice with seed=42 (numpy.random.default_rng(42)) produces byte-identical CSVs (deterministic / reproducible across runs)"
    - "D-03: Edge case densities — ~5% missing numeric values, ~3% duplicate student_id rows, ~2% type mismatches"
    - "D-04: Risk-tier distribution baked into metrics — cohort assignments target ~15% CRITICAL, ~25% HIGH, ~40% MEDIUM, ~20% LOW"
    - "Test fixture CSVs cover happy path, missing numeric, bad dates, duplicate IDs, type mismatch, and empty file scenarios"
  artifacts:
    - path: "src/generate_data.py"
      provides: "Standalone deterministic synthetic CSV generator (invoked by make demo / python -m src.generate_data)"
      contains: "numpy.random.default_rng"
      min_lines: 100
    - path: "tests/test_generate_data.py"
      provides: "Verifies generator produces 3 files with expected row counts and edge-case density"
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures — sample_csv_paths fixture pointing at tests/fixtures/"
    - path: "tests/fixtures/student_metadata_happy.csv"
      provides: "Minimal clean metadata CSV for happy-path ingestion tests"
    - path: "tests/fixtures/student_metadata_with_dupes.csv"
      provides: "Metadata CSV with duplicate student_id rows for DATA-04 testing"
    - path: "tests/fixtures/student_daily_metrics_missing_numeric.csv"
      provides: "Metrics CSV with blank session_attended_min / practice_questions cells for DATA-03 testing"
    - path: "tests/fixtures/student_daily_metrics_bad_dates.csv"
      provides: "Metrics CSV with unparseable date strings for D-11 / DATA-05 testing"
    - path: "tests/fixtures/student_metadata_type_mismatch.csv"
      provides: "Metadata CSV with string values where numeric expected for DATA-05 testing"
    - path: "tests/fixtures/empty.csv"
      provides: "Header-only CSV for empty-file edge case (TEST-02 forward reference)"
  key_links:
    - from: "src/generate_data.py"
      to: "src.config (column constants + DATA_DIR)"
      via: "from src import config as cfg"
      pattern: "from src import config"
    - from: "src/generate_data.py"
      to: "data/ directory"
      via: "cfg.DATA_DIR / 'student_metadata.csv' (and 2 siblings)"
      pattern: "to_csv\\(.*DATA_DIR"
    - from: "tests/conftest.py"
      to: "tests/fixtures/"
      via: "sample_csv_paths fixture returns dict of fixture file Path objects"
      pattern: "sample_csv_paths"
---

<objective>
Build the standalone synthetic data generator `src/generate_data.py` that creates 3 deterministic CSV files in `data/`, plus the test fixture suite under `tests/fixtures/` that downstream plans (03 + Phase 7) consume.

Purpose: DATA-01 is the only requirement covered by this plan, but the test fixtures unblock plan 03's ingestion tests. Both deliverables must land before any ingestion code runs because plan 03 ingestion tests assert against these fixtures and `make demo` requires the generator.

Output: `src/generate_data.py` (deterministic, seed=42, 300 students across 20 campuses, 14 days, edge cases baked in per D-03), 7 fixture CSVs covering happy path + each edge case, `tests/conftest.py` with shared fixture, `tests/test_generate_data.py` verifying generator output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/01-foundation-data-ingestion/01-CONTEXT.md
@.planning/phases/01-foundation-data-ingestion/01-RESEARCH.md
@.planning/phases/01-foundation-data-ingestion/01-PATTERNS.md
@.planning/phases/01-foundation-data-ingestion/01-VALIDATION.md
@.planning/phases/01-foundation-data-ingestion/01-01-SUMMARY.md

<interfaces>
<!-- All column constants needed below come from src/config.py (created in plan 01). -->

From src/config.py:
```python
COL_STUDENT_ID, COL_STUDENT_NAME, COL_CAMPUS_ID, COL_PARENT_PHONE, COL_FACILITATOR_EMAIL
COL_METRIC_DATE, COL_SESSION_MIN, COL_PRACTICE_Q
COL_NOTE_DATE, COL_NOTE_TEXT
DATA_DIR  # Path('data') by default
```

Locked constants for this plan (from 01-RESEARCH.md L562-570):
```python
SEED = 42
N_CAMPUSES = 20
STUDENTS_PER_CAMPUS = 15
N_DAYS = 14
PCT_MISSING_NUMERIC = 0.05
PCT_DUPLICATE_ID = 0.03
PCT_TYPE_MISMATCH = 0.02
```

Risk-tier cohort distribution (D-04):
- 15% CRITICAL students: attendance_rate near 0, practice near 0, declining or flat
- 25% HIGH: attendance ~40-60%, practice low, mixed trend
- 40% MEDIUM: attendance ~70-85%, practice moderate
- 20% LOW: attendance &gt; 90%, practice high, improving trend
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test fixtures, conftest, and test_generate_data.py (RED)</name>
  <files>
    tests/conftest.py,
    tests/fixtures/student_metadata_happy.csv,
    tests/fixtures/student_daily_metrics_happy.csv,
    tests/fixtures/facilitator_notes_happy.csv,
    tests/fixtures/student_metadata_with_dupes.csv,
    tests/fixtures/student_daily_metrics_missing_numeric.csv,
    tests/fixtures/student_daily_metrics_bad_dates.csv,
    tests/fixtures/student_metadata_type_mismatch.csv,
    tests/fixtures/empty.csv,
    tests/test_generate_data.py
  </files>
  <read_first>
    - .planning/phases/01-foundation-data-ingestion/01-VALIDATION.md (Wave 0 Requirements L63-74, Per-Task Verification Map L40-58)
    - .planning/phases/01-foundation-data-ingestion/01-RESEARCH.md (Pattern 5 L553-609, Wave 0 Gaps L944-953)
    - .planning/phases/01-foundation-data-ingestion/01-CONTEXT.md (D-01 through D-04 — generator profile)
    - .planning/phases/01-foundation-data-ingestion/01-01-SUMMARY.md (confirm src/config.py constants are available)
    - src/config.py (after plan 01) — exact column constant string values
  </read_first>
  <behavior>
    - test_generate_data.py::test_three_files_created — After `src.generate_data.main()` runs into a tmp DATA_DIR, exactly 3 files exist: student_metadata.csv, student_daily_metrics.csv, facilitator_notes.csv. (D-01)
    - test_generate_data.py::test_metadata_row_count_300 — student_metadata.csv has exactly 300 rows (excluding header). Tolerance: 300 unique student_ids; total row count may be up to 309 due to 3% duplicate edge cases (which add rows). (D-01: 20 campuses x 15 students)
    - test_generate_data.py::test_metrics_approximate_row_count — student_daily_metrics.csv has between 3990 and 4410 data rows (300 students x 14 days = 4200 ± 5% for edge cases). (D-01)
    - test_generate_data.py::test_deterministic_across_runs — Running main() twice produces byte-identical student_metadata.csv content (D-02: numpy.random.default_rng(42) seed=42 reproducibility).
    - test_generate_data.py::test_campus_count — student_metadata['campus_id'].nunique() == 20. (D-01)
    - test_generate_data.py::test_phone_format — every parent_phone string starts with '0' (preserves leading zero — Pitfall #3 forward defense).
    - test_generate_data.py::test_edge_cases_injected — student_metadata.csv contains at least one duplicate student_id (3% of 300 ~= 9 duplicates expected per D-03); student_daily_metrics.csv contains at least one blank session_attended_min cell.
  </behavior>
  <action>
    Create tests/conftest.py first. Define a fixture `sample_csv_paths(tmp_path, request)` that copies fixture files from `tests/fixtures/` into tmp_path and returns the dict `{"metrics": ..., "notes": ..., "metadata": ...}` with paths into tmp_path. Also define a parameterized fixture `csv_scenario` keyed on scenario name ("happy", "with_dupes", "missing_numeric", "bad_dates", "type_mismatch", "empty") so plan 03 ingestion tests can request a specific scenario.

    Create the 8 fixture CSVs by hand (small — 5-10 rows each):

    `tests/fixtures/student_metadata_happy.csv`: Header `student_id,student_name,campus_id,parent_phone,facilitator_email`. 5 rows. Example row: `S0101,Student S0101,C01,0501123456,facilitator.c01@boon.academy`. All clean values, no duplicates.

    `tests/fixtures/student_daily_metrics_happy.csv`: Header `student_id,metric_date,session_attended_min,practice_questions`. 10 rows (2 students x 5 days). Dates in `YYYY-MM-DD` format. All numeric cells populated.

    `tests/fixtures/facilitator_notes_happy.csv`: Header `student_id,note_date,note_text`. 5 rows. ISO dates, plain note text.

    `tests/fixtures/student_metadata_with_dupes.csv`: Like happy but include 2 rows with the same student_id (S0101 appears twice with different student_name values — to test "keep=last" wins deterministically).

    `tests/fixtures/student_daily_metrics_missing_numeric.csv`: 6 rows; 2 rows have blank `session_attended_min` value (empty string between commas); 1 row has blank `practice_questions`.

    `tests/fixtures/student_daily_metrics_bad_dates.csv`: 5 rows; include 1 row with `metric_date=not-a-date`, 1 row with `metric_date=2026/13/45` (invalid month/day), 1 row with `metric_date=` (empty).

    `tests/fixtures/student_metadata_type_mismatch.csv`: Like happy but include 1 row where `parent_phone` is the literal string `abc` (not parseable as digits — exercises DATA-05 type safety).

    `tests/fixtures/empty.csv`: Header line only, no data rows: `student_id,student_name,campus_id,parent_phone,facilitator_email`.

    Then create `tests/test_generate_data.py` with all 7 tests listed in `<behavior>`. Tests must temporarily redirect `cfg.DATA_DIR` to `tmp_path` via `monkeypatch.setattr` so the generator writes into the test directory, not the real `data/`. After each test, no real data/ files are touched.
  </action>
  <verify>
    <automated>python -c "import pathlib; fixtures=['student_metadata_happy.csv','student_daily_metrics_happy.csv','facilitator_notes_happy.csv','student_metadata_with_dupes.csv','student_daily_metrics_missing_numeric.csv','student_daily_metrics_bad_dates.csv','student_metadata_type_mismatch.csv','empty.csv']; missing=[f for f in fixtures if not pathlib.Path(f'tests/fixtures/{f}').exists()]; assert not missing, f'missing fixtures: {missing}'"</automated>
    <automated>python -c "import pathlib; assert pathlib.Path('tests/conftest.py').exists() and pathlib.Path('tests/test_generate_data.py').exists()"</automated>
    <automated>python -c "import csv; rows=list(csv.reader(open('tests/fixtures/empty.csv'))); assert len(rows)==1, f'empty.csv must have only header, got {len(rows)} rows'"</automated>
    <automated>python -c "import csv; rows=list(csv.reader(open('tests/fixtures/student_metadata_with_dupes.csv'))); ids=[r[0] for r in rows[1:]]; assert len(ids)!=len(set(ids)), 'with_dupes.csv must contain duplicate student_id'"</automated>
    <!-- test_generate_data.py is expected to FAIL here (RED) — generator does not exist yet -->
  </verify>
  <acceptance_criteria>
    - All 8 fixture CSV files exist under tests/fixtures/
    - tests/conftest.py defines a `sample_csv_paths` fixture returning a dict with keys "metrics", "notes", "metadata"
    - tests/test_generate_data.py defines all 7 test functions listed in `<behavior>`
    - tests/fixtures/student_metadata_with_dupes.csv has at least one duplicate student_id value across rows
    - tests/fixtures/student_daily_metrics_missing_numeric.csv has at least 2 rows with empty `session_attended_min`
    - tests/fixtures/student_daily_metrics_bad_dates.csv has at least 1 row with an unparseable date
    - tests/fixtures/empty.csv contains exactly 1 line (header only)
    - All fixture CSVs use UTF-8 encoding and LF or CRLF line endings (no BOM)
    - tests/test_generate_data.py uses `monkeypatch.setattr(cfg, 'DATA_DIR', tmp_path)` to isolate generator output from real data/
  </acceptance_criteria>
  <done>Test fixtures and the test file exist. test_generate_data.py is expected to FAIL because src/generate_data.py is not yet implemented — this is the RED phase of TDD.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/generate_data.py (GREEN) — D-01 (20 campuses x 15 students x 3 CSVs), D-02 (numpy.random.default_rng(42))</name>
  <files>src/generate_data.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-ingestion/01-RESEARCH.md (Pattern 5 L553-611 — exact template; Open Question #2 L878-885 — daily series storage)
    - .planning/phases/01-foundation-data-ingestion/01-PATTERNS.md (generate_data.py section L284-344)
    - .planning/phases/01-foundation-data-ingestion/01-CONTEXT.md (D-01, D-02, D-03, D-04 — all locked decisions)
    - src/config.py (column constants + DATA_DIR — created in plan 01)
    - tests/test_generate_data.py (created in Task 1 — defines exact contract this file must satisfy)
  </read_first>
  <action>
    Implement src/generate_data.py as a standalone script per D-01 (20 campuses x 15 students = 300 total; outputs 3 CSVs) and D-02 (seeded random — numpy.random.default_rng(42) for reproducibility across runs).

    Top of file: module docstring noting it is invoked by `make demo` and `python -m src.generate_data`, NOT imported by `main.py`. Imports: `numpy as np`, `pandas as pd`, `pathlib.Path`, `datetime`, `timedelta`, `from src import config as cfg`.

    Module-level constants (per D-01, D-02, D-03): SEED=42 (D-02), N_CAMPUSES=20 (D-01), STUDENTS_PER_CAMPUS=15 (D-01), N_DAYS=14, PCT_MISSING_NUMERIC=0.05 (D-03), PCT_DUPLICATE_ID=0.03 (D-03), PCT_TYPE_MISMATCH=0.02 (D-03). Use BASE_DATE = `datetime(2026, 5, 1)` as anchor; metric_date values are BASE_DATE + 0..13 days.

    Function `_assign_risk_cohort(student_idx: int, total: int) -> str` returns one of "CRITICAL"/"HIGH"/"MEDIUM"/"LOW" based on index buckets per D-04 percentages (15/25/40/20). Drive deterministic cohort assignment by student_idx ranges so D-02 seed=42 produces identical tier counts on every run.

    Function `generate_metadata(rng: np.random.Generator) -> pd.DataFrame`: build 300 rows (20 campuses x 15 students per D-01) using the template from 01-PATTERNS.md L309-323. student_id format `S{c:02d}{s:02d}`, campus_id format `C{c:02d}`, parent_phone format `0501{rng.integers(100000,999999):06d}` (note the leading `0501` keeps the leading-zero realistic). facilitator_email format `facilitator.{campus_id.lower()}@boon.academy`. Use column constants from cfg, NOT hardcoded strings.

    Function `generate_metrics(rng: np.random.Generator, students: list[tuple[str, str]]) -> pd.DataFrame` (where students is list of `(student_id, cohort)`): for each student, generate N_DAYS rows. Per cohort distribution:
      - CRITICAL: session_min ~ Poisson(2), practice_q ~ Poisson(1) — near-zero engagement
      - HIGH: session_min ~ Normal(15, 5), practice_q ~ Poisson(3) — clipped to >= 0
      - MEDIUM: session_min ~ Normal(35, 8), practice_q ~ Poisson(8)
      - LOW: session_min ~ Normal(50, 5), practice_q ~ Poisson(15)
    Round session_min to nearest int; clip to [0, 90]. metric_date column uses BASE_DATE + day_index in YYYY-MM-DD format.

    Function `generate_notes(rng: np.random.Generator, students: list[tuple[str, str]]) -> pd.DataFrame`: for ~70% of students, generate 1-3 facilitator notes with `note_date` somewhere in the 14-day window (ISO format), `note_text` from a small canned list of realistic note strings ("Missed class", "Strong performance this week", "Parent meeting scheduled", "Disengaged in group work", "Submitted late assignment"). The remaining 30% of students have no notes (their student_id never appears in the notes CSV) — this exercises the "max days_since_last_note penalty" path in Phase 2.

    Function `inject_edge_cases(metadata: pd.DataFrame, metrics: pd.DataFrame, rng: np.random.Generator) -> tuple[pd.DataFrame, pd.DataFrame]`: implement the 3 edge-case injections per D-03:
      - Duplicate IDs (3% per D-03): pick `int(0.03 * len(metadata))` random rows from metadata, duplicate them (append copies with same student_id, possibly slightly different student_name like "{name} DUPE"). Append to metadata.
      - Missing numeric (5% per D-03): pick `int(0.05 * len(metrics))` random rows in metrics, randomly choose session_attended_min OR practice_questions, set that cell to pd.NA / NaN.
      - Type mismatches (2% per D-03): pick `int(0.02 * len(metrics))` random rows, set practice_questions to a non-numeric string ("abc", "many", "?") in CSV (will read as object/string and be caught by ingestion's coerce-to-numeric step).

    Function `main() -> None`: call `cfg.DATA_DIR.mkdir(parents=True, exist_ok=True)`, create `rng = np.random.default_rng(SEED)` (D-02 — use default_rng not legacy np.random.seed), generate all 3 DataFrames + apply edge cases, write each via `.to_csv(path, index=False, encoding='utf-8')` (D-01: outputs exactly 3 CSVs — student_metadata.csv, student_daily_metrics.csv, facilitator_notes.csv). End with a `print(f"Generated synthetic data: {len(metadata)} students, {len(metrics)} metric rows, {len(notes)} notes in {cfg.DATA_DIR}")`. `print` is permitted here per 01-RESEARCH.md L611 (utility script, not pipeline). Bottom of file: `if __name__ == "__main__": main()`.

    Every function gets a type-hinted signature and a docstring (INFRA-08).
  </action>
  <verify>
    <automated>pytest tests/test_generate_data.py -x -v</automated>
    <automated>python -c "import os, tempfile, pathlib; os.environ.setdefault('ANTHROPIC_API_KEY','dummy'); 
with tempfile.TemporaryDirectory() as td:
    os.environ['DATA_DIR']=td
    import importlib, sys
    sys.modules.pop('src.config', None); sys.modules.pop('src.generate_data', None)
    from src import generate_data
    generate_data.main()
    files = sorted(p.name for p in pathlib.Path(td).iterdir())
    assert files == ['facilitator_notes.csv','student_daily_metrics.csv','student_metadata.csv'], files"</automated>
    <automated>python -c "import os, tempfile, pathlib, hashlib
os.environ.setdefault('ANTHROPIC_API_KEY','dummy')
def run_once():
    with tempfile.TemporaryDirectory() as td:
        os.environ['DATA_DIR']=td
        import importlib, sys
        for m in ['src.config','src.generate_data']: sys.modules.pop(m, None)
        from src import generate_data; generate_data.main()
        return hashlib.sha256(pathlib.Path(td,'student_metadata.csv').read_bytes()).hexdigest()
h1 = run_once(); h2 = run_once()
assert h1 == h2, f'non-deterministic output: {h1} vs {h2}'"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_generate_data.py -x -v` exits 0 (all 7 tests pass)
    - src/generate_data.py defines `main() -> None` callable as the script entry point
    - src/generate_data.py uses `np.random.default_rng(SEED)` with SEED=42 per D-02 (NOT global `np.random.seed`)
    - src/generate_data.py contains module-level constants SEED, N_CAMPUSES, STUDENTS_PER_CAMPUS, N_DAYS, PCT_MISSING_NUMERIC, PCT_DUPLICATE_ID, PCT_TYPE_MISMATCH with values from D-01 through D-03 (D-01: N_CAMPUSES=20, STUDENTS_PER_CAMPUS=15; D-02: SEED=42)
    - src/generate_data.py outputs exactly 3 CSVs (D-01): student_metadata.csv, student_daily_metrics.csv, facilitator_notes.csv
    - src/generate_data.py imports column names from `cfg` (no hardcoded "student_id", "campus_id", etc.)
    - src/generate_data.py uses `cfg.DATA_DIR` for output path (no hardcoded "data" string)
    - Running the generator into the same DATA_DIR twice produces byte-identical student_metadata.csv (sha256 match — D-02 reproducibility)
    - Generated student_metadata.csv has &gt;= 300 unique student_ids and &lt;= 320 total rows (300 base per D-01 + ~3% dupe injection per D-03)
    - Generated student_daily_metrics.csv has between 3990 and 4410 data rows
    - At least one row in student_daily_metrics.csv has an empty session_attended_min or practice_questions cell (edge case injection succeeded)
    - parent_phone strings in student_metadata.csv all start with the character '0' (leading-zero realism preserved)
    - src/generate_data.py has type hints on every function signature and docstrings on every function
  </acceptance_criteria>
  <done>Synthetic data generator is deterministic (D-02 seed=42), hits the locked profile (D-01: 300 students = 20 campuses x 15 students, 3 CSVs, 14 days, edge cases at D-03 densities), and all 7 tests in test_generate_data.py pass. `make demo` (or `./make.ps1 demo`) can now successfully populate data/ before main.py runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator → Filesystem | src/generate_data.py writes CSVs to cfg.DATA_DIR; if DATA_DIR is set to a sensitive path, files would be overwritten |
| Test fixtures → Repo | Committed CSV fixtures contain synthetic data only — no real PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | DATA_DIR env var (path traversal) | accept | Inherited from plan 01 (T-01-04). Single-operator batch CLI; only the developer running `python -m src.generate_data` controls DATA_DIR. Documented as out-of-scope for v1. |
| T-02-02 | Information Disclosure | Test fixtures containing PII-like data | mitigate | All fixture CSVs use synthetic placeholder names ("Student S0101"), synthetic phone numbers (`0501XXXXXX`), and a single facilitator email pattern (`facilitator.cNN@boon.academy`). No real student data appears in committed fixtures. |
| T-02-03 | Tampering | Non-deterministic synthetic data | mitigate | `np.random.default_rng(42)` provides reproducibility (D-02). test_deterministic_across_runs asserts byte-identical output across two runs. Prevents flaky tests downstream. |
| T-02-04 | Information Disclosure | Generator log output | accept | Generator emits a single summary `print` line containing counts only, no individual student data. Documented exception to "no print" rule (01-RESEARCH.md L611 — utility script). |
| T-02-05 | Repudiation | Generator side effects on real data/ during tests | mitigate | All test_generate_data.py tests use `monkeypatch.setattr(cfg, 'DATA_DIR', tmp_path)` to ensure tests never write to the real `data/` directory. Verified by `tests/test_generate_data.py` fixture setup. |
</threat_model>

<verification>
After both tasks complete:

1. **Generator runs cleanly**: `python -m src.generate_data` from repo root exits 0 and writes exactly 3 files to `data/`.
2. **Determinism**: Run the generator twice; sha256 of student_metadata.csv matches across runs.
3. **All 7 generator tests pass**: `pytest tests/test_generate_data.py -x -v` exits 0.
4. **Fixture inventory complete**: All 8 fixture CSVs exist under tests/fixtures/.
5. **Edge case densities hit targets**: open generated metrics CSV, count blank session_attended_min cells, confirm ~5% (4200 × 0.05 = ~210 cells).
6. **Distribution sanity** (manual spot check): cohort assignment hits D-04 percentages within ±2% tolerance.
</verification>

<success_criteria>
- `python -m src.generate_data` writes 3 CSV files (student_metadata.csv, student_daily_metrics.csv, facilitator_notes.csv) to cfg.DATA_DIR (D-01)
- Generated student_metadata.csv has 300 base students across 20 campuses (D-01: 20 x 15) with up to ~9 duplicate rows injected per D-03
- Output is deterministic — same SEED=42 (D-02), same output bytes
- 8 test fixture CSVs exist under tests/fixtures/ covering happy + 6 edge cases + empty
- tests/conftest.py defines the `sample_csv_paths` fixture consumed by plan 03 ingestion tests
- `pytest tests/test_generate_data.py -x -v` exits 0
- DATA-01 requirement is fully satisfied
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-ingestion/01-02-SUMMARY.md` when done.
</output>
