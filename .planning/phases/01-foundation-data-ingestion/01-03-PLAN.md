---
phase: 01-foundation-data-ingestion
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/ingestion.py
  - tests/test_ingestion.py
autonomous: true
requirements:
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07
  - DATA-08
  - INFRA-01
tags:
  - ingestion
  - pandas
  - csv-cleaning
  - per-row-error-containment
must_haves:
  truths:
    - "ingest({metrics, notes, metadata}) returns a single pd.DataFrame with one row per student_id"
    - "Every read_csv call inside ingestion uses an explicit dtype= dict (no inferred types)"
    - "parent_phone column in the returned DataFrame retains leading-zero strings (e.g. '0501123456' — NOT 501123456 or 5.01123e+09)"
    - "student_id column in the returned DataFrame is string dtype (NOT float64 or int64)"
    - "D-09: Missing session_attended_min or practice_questions cells are filled with 0 and a WARNING is logged per affected student_id (missing_numeric + type_mismatch warning types)"
    - "D-10: Missing or unparseable student_id / campus_id values are assigned auto-incremented placeholder UNKNOWN_NNN and a WARNING is logged per affected row (missing_id warning type)"
    - "Duplicate student_id rows in metadata are deduplicated (keep='last') with a WARNING logged per duplicate"
    - "Unparseable dates in metric_date or note_date are coerced to NaT with a WARNING logged per affected row"
    - "Type mismatches (string-where-numeric-expected) do not raise — pd.to_numeric(..., errors='coerce') applies, NaN is filled with 0, warning logged"
    - "ingest() returns successfully on empty.csv inputs (header only, no data rows) — does not raise"
    - "Returned DataFrame has df.attrs['data_quality_warnings'] populated as list[dict] entries with 'type' key"
    - "Logging messages contain only student_id, never student_name / parent_phone / note_text (PII safety, Security V7)"
    - "Running `python main.py` on synthetic data (after `python -m src.generate_data`) exits 0 and logs 'Ingested 300 students'"
  artifacts:
    - path: "src/ingestion.py"
      provides: "ingest(data_paths) -> DataFrame — locked signature from STATE.md L93; encapsulates all read_csv, type coercion, dedup, merge logic"
      contains: "def ingest"
      exports: ["ingest"]
      min_lines: 120
    - path: "tests/test_ingestion.py"
      provides: "Unit tests covering DATA-02 through DATA-08 (8 distinct test cases per VALIDATION.md Per-Task Map)"
      min_test_count: 8
  key_links:
    - from: "src/ingestion.py"
      to: "src.config"
      via: "from src import config as cfg (column constants + DATA_DIR)"
      pattern: "from src import config"
    - from: "src/ingestion.py"
      to: "data_quality_warnings side channel"
      via: "df.attrs['data_quality_warnings'] = warnings (list[dict] accumulator)"
      pattern: "df\\.attrs\\[.data_quality_warnings.\\]"
    - from: "main.py"
      to: "src.ingestion.ingest"
      via: "from src.ingestion import ingest; df = ingest(data_paths)"
      pattern: "ingest\\(data_paths\\)"
    - from: "src/ingestion.py read_csv calls"
      to: "dtype dicts (DTYPE_METRICS, DTYPE_NOTES, DTYPE_META)"
      via: "pd.read_csv(path, dtype=DTYPE_*, na_values=[...], encoding='utf-8')"
      pattern: "pd\\.read_csv\\([^)]*dtype="
---

<objective>
Implement `src/ingestion.py` as the canonical CSV-to-DataFrame contract for the pipeline. The locked signature `ingest(data_paths: dict[str, Path]) -> pd.DataFrame` (STATE.md L93) is the boundary between Phase 1 and Phases 2-8 — the DataFrame schema it returns is frozen here. Cover all DATA-02 through DATA-08 requirements with per-row error containment so no single bad record halts the pipeline.

Purpose: This is the most pitfall-dense module in the project. CLAUDE.md Critical Pitfalls #1 (dtype float promotion), #3 (phone leading zeros), #4 (silent duplicates), #5 (missing imputation), #6 (date ambiguity) all manifest here if implemented carelessly. The patterns in 01-RESEARCH.md L385-538 pre-empt every one — they must be followed verbatim.

Output: src/ingestion.py implementing Patterns 2 (dtype-locked read), 3 (per-row error containment), and 4 (three-CSV merge), plus tests/test_ingestion.py with 8+ tests proving every DATA-* requirement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/01-foundation-data-ingestion/01-CONTEXT.md
@.planning/phases/01-foundation-data-ingestion/01-RESEARCH.md
@.planning/phases/01-foundation-data-ingestion/01-PATTERNS.md
@.planning/phases/01-foundation-data-ingestion/01-VALIDATION.md
@.planning/phases/01-foundation-data-ingestion/01-01-SUMMARY.md

<interfaces>
<!-- LOCKED signature from STATE.md L93 — must not change. -->

```python
def ingest(data_paths: dict[str, Path]) -> pd.DataFrame:
    """data_paths has keys "metrics", "notes", "metadata" mapping to Path objects.
    Returns one row per student_id with columns:
      student_id, student_name, campus_id, parent_phone, facilitator_email,
      session_total_min, practice_total_q, attendance_days,
      daily_session_series (list[float]), daily_practice_series (list[float]), daily_dates (list[str]),
      latest_note_date, latest_note_text
    df.attrs["data_quality_warnings"]: list[dict] populated with cleaning events.
    """
```

From src/config.py (created in plan 01):
- COL_STUDENT_ID, COL_STUDENT_NAME, COL_CAMPUS_ID, COL_PARENT_PHONE, COL_FACILITATOR_EMAIL
- COL_METRIC_DATE, COL_SESSION_MIN, COL_PRACTICE_Q
- COL_NOTE_DATE, COL_NOTE_TEXT

Dtype dictionaries from 01-PATTERNS.md L137-167 (must be used verbatim):
```python
DTYPE_METRICS = {
    cfg.COL_STUDENT_ID:   "string",
    cfg.COL_METRIC_DATE:  "string",   # parse to datetime AFTER load
    cfg.COL_SESSION_MIN:  "Float64",  # nullable, capital F
    cfg.COL_PRACTICE_Q:   "Float64",
}
DTYPE_NOTES = {
    cfg.COL_STUDENT_ID: "string",
    cfg.COL_NOTE_DATE:  "string",
    cfg.COL_NOTE_TEXT:  "string",
}
DTYPE_META = {
    cfg.COL_STUDENT_ID:        "string",
    cfg.COL_STUDENT_NAME:      "string",
    cfg.COL_CAMPUS_ID:         "string",
    cfg.COL_PARENT_PHONE:      "string",   # CRITICAL — Pitfall #3
    cfg.COL_FACILITATOR_EMAIL: "string",
}
```

Warning entry schema:
- `{"type": "missing_numeric", "column": str, "student_id": str}`
- `{"type": "duplicate_id", "student_id": str}`
- `{"type": "bad_date", "column": str, "student_id": str}`
- `{"type": "missing_id", "column": str, "row_index": int, "assigned": str}`
- `{"type": "type_mismatch", "column": str, "student_id": str, "raw_value": str}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/test_ingestion.py covering DATA-02 through DATA-08 (RED)</name>
  <files>tests/test_ingestion.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-ingestion/01-VALIDATION.md (Per-Task Verification Map L40-58 — exact test names)
    - .planning/phases/01-foundation-data-ingestion/01-RESEARCH.md (Patterns 2-4 L385-538 — expected behavior)
    - .planning/phases/01-foundation-data-ingestion/01-PATTERNS.md (ingestion section L117-281)
    - .planning/REQUIREMENTS.md (DATA-02 through DATA-08 exact text)
    - tests/conftest.py (sample_csv_paths fixture — created in plan 02)
    - tests/fixtures/*.csv (fixture inventory — created in plan 02)
  </read_first>
  <behavior>
    Implement these 9 tests against the locked signature `ingest(data_paths) -> pd.DataFrame`:

    - test_phone_stays_string (DATA-02): Load happy fixtures. Assert `df[cfg.COL_PARENT_PHONE].dtype == pd.StringDtype()` (or equivalent). Assert every value starts with the character "0". This pre-empts Pitfall #3.

    - test_student_id_is_string (DATA-02): Assert `df[cfg.COL_STUDENT_ID].dtype == pd.StringDtype()`. Pre-empts Pitfall #1.

    - test_missing_numeric_filled_with_zero (DATA-03): Load with metrics=student_daily_metrics_missing_numeric.csv. Assert `df['session_total_min'].notna().all()` and `df['practice_total_q'].notna().all()`. Assert at least one warning of `type='missing_numeric'` exists in df.attrs['data_quality_warnings'].

    - test_duplicate_ids_deduped (DATA-04): Load with metadata=student_metadata_with_dupes.csv. Assert `df[cfg.COL_STUDENT_ID].is_unique`. Assert at least one warning of `type='duplicate_id'` exists.

    - test_type_mismatch_safe_default (DATA-05): Load with metadata=student_metadata_type_mismatch.csv (one row has `parent_phone='abc'`). Assert ingest() does not raise. Assert the row appears in the output (preserved per D-10 spirit). Assert no exception.

    - test_merge_one_row_per_student (DATA-06): Load happy fixtures. Compute expected unique student_ids in metadata fixture. Assert `len(df) == expected_count` and `df[cfg.COL_STUDENT_ID].is_unique`.

    - test_warnings_attached_to_df (DATA-07): Load any fixture with edge cases. Assert `'data_quality_warnings' in df.attrs`, `isinstance(df.attrs['data_quality_warnings'], list)`, and every entry has a `'type'` key with value in {'missing_numeric','duplicate_id','bad_date','missing_id','type_mismatch'}.

    - test_bad_record_does_not_crash (DATA-08): Load metrics=student_daily_metrics_bad_dates.csv. Assert ingest() returns a DataFrame (does not raise). Assert at least one `type='bad_date'` warning exists.

    - test_empty_csv_handled (DATA-08 / TEST-02 forward ref): Load all three paths pointing at empty.csv. Assert ingest() does not raise; returns a DataFrame with 0 rows.

    Bonus PII-safety assertion in any test: capture caplog during ingestion; assert that captured log messages do NOT contain `student_name` values like "Student S0101" — only student_id (e.g., "S0101"). This proves Security V7 (PII-safe logging) is honored.
  </behavior>
  <action>
    Write tests/test_ingestion.py. Use `pathlib.Path("tests/fixtures") / "filename"` to point at committed fixtures. For tests that mix fixtures (e.g., test_phone_stays_string uses all-happy; test_duplicate_ids_deduped uses with_dupes metadata + happy metrics + happy notes), build the `data_paths` dict inline:
      `data_paths = {"metadata": FIXTURES_DIR / "student_metadata_with_dupes.csv", "metrics": FIXTURES_DIR / "student_daily_metrics_happy.csv", "notes": FIXTURES_DIR / "facilitator_notes_happy.csv"}`

    Use `caplog` fixture from pytest for the PII-safety assertion. Use `pytest.fixture(autouse=True)` to set `ANTHROPIC_API_KEY='dummy'` if needed for config import.

    Define `FIXTURES_DIR = pathlib.Path(__file__).parent / "fixtures"` at the top of the file. Import `from src.ingestion import ingest` and `from src import config as cfg`.

    Each test gets a one-line docstring stating which DATA-* requirement it covers.

    DO NOT mutate the committed fixture files — they are immutable inputs.
  </action>
  <verify>
    <automated>python -c "import pathlib; assert pathlib.Path('tests/test_ingestion.py').exists()"</automated>
    <automated>python -c "src=open('tests/test_ingestion.py').read(); required=['test_phone_stays_string','test_student_id_is_string','test_missing_numeric_filled_with_zero','test_duplicate_ids_deduped','test_type_mismatch_safe_default','test_merge_one_row_per_student','test_warnings_attached_to_df','test_bad_record_does_not_crash','test_empty_csv_handled']; missing=[n for n in required if n not in src]; assert not missing, f'missing tests: {missing}'"</automated>
    <!-- RED: tests will fail because src/ingestion.py does not exist yet -->
  </verify>
  <acceptance_criteria>
    - tests/test_ingestion.py defines all 9 test functions listed in `<behavior>`
    - Every test function has a docstring naming the DATA-* requirement it covers
    - At least one test uses `caplog` to assert no PII (student_name strings, parent_phone strings) appears in log output
    - Tests import `from src.ingestion import ingest` and `from src import config as cfg`
    - Tests use the committed fixture CSVs under `tests/fixtures/` (no inline CSV generation in tests)
    - File is syntactically valid Python (`python -c "import ast; ast.parse(open('tests/test_ingestion.py').read())"` succeeds)
  </acceptance_criteria>
  <done>Test file exists with 9 tests covering DATA-02 through DATA-08. All tests are expected to FAIL because src/ingestion.py is not yet implemented (RED phase).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/ingestion.py with Patterns 2 + 3 + 4 (GREEN)</name>
  <files>src/ingestion.py</files>
  <read_first>
    - .planning/phases/01-foundation-data-ingestion/01-RESEARCH.md (Patterns 2-4 L385-544, Pitfalls section L682-722, Security Domain L961-981)
    - .planning/phases/01-foundation-data-ingestion/01-PATTERNS.md (ingestion section L117-281 — exact code excerpts)
    - .planning/phases/01-foundation-data-ingestion/01-CONTEXT.md (D-09, D-10, D-11 — error handling rules)
    - src/config.py (column constants — created in plan 01)
    - tests/test_ingestion.py (created in Task 1 — defines exact behavioral contract)
    - CLAUDE.md (Critical Pitfalls — dtype, phone, os.environ, logging conventions)
  </read_first>
  <action>
    Implement src/ingestion.py following Patterns 2, 3, and 4 from 01-RESEARCH.md verbatim. Top of file:

    1. Module docstring describing the function. Imports: `import logging`, `from pathlib import Path`, `import pandas as pd`, `from src import config as cfg`. Module-level `logger = logging.getLogger(__name__)` (per CLAUDE.md "no print" rule).

    2. Module-level dtype dicts: `DTYPE_METRICS`, `DTYPE_NOTES`, `DTYPE_META` per 01-PATTERNS.md L137-167. Use cfg column constants as keys (no hardcoded "student_id" etc). Use pandas nullable types: `"string"` for IDs/names/dates-as-strings/phones; `"Float64"` (capital F) for nullable numerics.

    3. Helper `_read_csv_safe(path: Path, dtype: dict[str, str]) -> pd.DataFrame`: wraps `pd.read_csv(path, dtype=dtype, keep_default_na=True, na_values=["", "N/A", "n/a", "NULL", "null", "-"], encoding="utf-8")`. Catches `FileNotFoundError` as-is (do not suppress — main.py shows it). Catches `pd.errors.EmptyDataError` and returns an empty DataFrame with the expected columns (for empty.csv support — DATA-08).

    4. Helper `_ensure_ids(df: pd.DataFrame, warnings: list[dict], id_col: str = None) -> pd.DataFrame`: scans `cfg.COL_STUDENT_ID` (and `cfg.COL_CAMPUS_ID` if present in df.columns) for NaN/empty. For each missing ID, generate auto-incremented placeholder `f"UNKNOWN_{counter:03d}"`, assign it back to the row, append warning `{"type": "missing_id", "column": ..., "row_index": idx, "assigned": placeholder}`, log WARNING. Use a closure or class-level counter for uniqueness across calls within a single ingest() invocation. Per D-10.

    5. Helper `_coerce_dates(df, date_col, warnings) -> pd.DataFrame`: parse with `pd.to_datetime(df[date_col], errors="coerce", format="%Y-%m-%d")`. Identify rows that had non-null input but parsed to NaT (`bad_mask = parsed.isna() & df[date_col].notna()`). For each affected student_id, log warning and append entry per D-11. Assign parsed back to df[date_col]. Pitfall #6 pre-emption — explicit `format="%Y-%m-%d"`.

    6. Helper `_fill_numeric_with_zero(df, col, warnings) -> pd.DataFrame`: First call `pd.to_numeric(df[col], errors='coerce')` to convert any string-type cells to numeric (DATA-05 type mismatch handling — leaves bad values as NaN, logged as `type_mismatch`). Then identify NaN cells, log warning per affected student_id per D-09, fill with 0, return df with column updated. Append two warning kinds: `type_mismatch` for cells that coerce-failed (string→NaN before fill), `missing_numeric` for cells that were already NaN.

    7. Helper `_dedupe_student_ids(df, warnings) -> pd.DataFrame`: `df[df.duplicated(subset=[cfg.COL_STUDENT_ID], keep=False)]` to find all dupes (including originals). Iterate unique dupe ids, log warning per id, append entry. Return `df.drop_duplicates(subset=[cfg.COL_STUDENT_ID], keep="last")` per D-04.

    8. Main `ingest(data_paths: dict[str, Path]) -> pd.DataFrame` (docstring per 01-PATTERNS.md L260-274): initialize `warnings: list[dict] = []`. Read all 3 CSVs via `_read_csv_safe`. Call `_ensure_ids` on each. Call `_coerce_dates` on metrics (COL_METRIC_DATE) and notes (COL_NOTE_DATE). Call `_fill_numeric_with_zero` on metrics for COL_SESSION_MIN and COL_PRACTICE_Q. Call `_dedupe_student_ids` on metadata.

    Aggregate metrics per-student (Pattern 4 L228-239): groupby `cfg.COL_STUDENT_ID`, agg to `session_total_min=sum(COL_SESSION_MIN)`, `practice_total_q=sum(COL_PRACTICE_Q)`, `attendance_days=lambda s: (s > 0).sum()` on COL_SESSION_MIN, `daily_session_series=list(COL_SESSION_MIN)`, `daily_practice_series=list(COL_PRACTICE_Q)`, `daily_dates=list(COL_METRIC_DATE)`. Reset index.

    Aggregate notes (Pattern 4 L242-247): sort by COL_NOTE_DATE descending, drop_duplicates keep="first" to get latest note per student, rename COL_NOTE_DATE→"latest_note_date", COL_NOTE_TEXT→"latest_note_text", select 3 columns.

    Merge: `df = metadata.merge(metrics_agg, on=cfg.COL_STUDENT_ID, how="left")`; then `df = df.merge(notes_latest, on=cfg.COL_STUDENT_ID, how="left")`. `how="left"` ensures every metadata row appears.

    Attach warnings: `df.attrs["data_quality_warnings"] = warnings`. Log info: `logger.info(f"ingestion complete — {len(df)} students, {len(warnings)} warnings")`.

    Return df.

    PII-SAFETY RULE (Security V7): every `logger.warning` and `logger.info` call must include `student_id` only — never `student_name`, `parent_phone`, or `note_text`. Pattern: `logger.warning(f"missing {col} for student_id={sid} — filled with 0")`. NEVER `logger.warning(f"missing {col} for {row}")` where row could expose other columns.

    Type hints on every function. Docstrings on `ingest()` and every private helper.
  </action>
  <verify>
    <automated>pytest tests/test_ingestion.py -x -v</automated>
    <automated>python -c "import os; os.environ.setdefault('ANTHROPIC_API_KEY','dummy'); from src.ingestion import ingest; import inspect; sig=inspect.signature(ingest); params=list(sig.parameters); assert params==['data_paths'], f'ingest signature drifted: {params}'"</automated>
    <automated>python -c "import os; os.environ.setdefault('ANTHROPIC_API_KEY','dummy'); src=open('src/ingestion.py').read(); 
assert 'pd.read_csv' in src and 'dtype=' in src, 'must call read_csv with explicit dtype'
assert 'student_name' not in [w for w in src.split('logger') if 'warning' in w.lower() or 'info' in w.lower()] or True
import re
# No bare hardcoded path literals
for forbidden in ['\"data/\"', '\"outputs/\"', '\"docs/\"', \"'data/'\", \"'outputs/'\", \"'docs/'\"]: 
    assert forbidden not in src, f'hardcoded path {forbidden!r} found in ingestion.py'"</automated>
    <automated>python -c "import os, ast; os.environ.setdefault('ANTHROPIC_API_KEY','dummy'); 
tree=ast.parse(open('src/ingestion.py').read())
funcs=[n for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
no_hints=[f.name for f in funcs if not f.returns]
assert not no_hints, f'functions missing return annotation: {no_hints}'
no_doc=[f.name for f in funcs if not (f.body and isinstance(f.body[0], ast.Expr) and isinstance(f.body[0].value, (ast.Constant, ast.Str)))]
assert not no_doc, f'functions missing docstring: {no_doc}'"</automated>
    <automated>python -c "import os, sys, tempfile, pathlib
os.environ.setdefault('ANTHROPIC_API_KEY','dummy')
# Make sure config picks up tmp DATA_DIR for full smoke
with tempfile.TemporaryDirectory() as td:
    os.environ['DATA_DIR']=td
    for m in ['src.config','src.generate_data','src.ingestion','main']: sys.modules.pop(m, None)
    from src import generate_data, ingestion, config as cfg
    generate_data.main()
    df = ingestion.ingest({'metrics': cfg.DATA_DIR/'student_daily_metrics.csv','notes': cfg.DATA_DIR/'facilitator_notes.csv','metadata': cfg.DATA_DIR/'student_metadata.csv'})
    assert len(df) &gt;= 300, f'expected at least 300 students, got {len(df)}'
    assert df[cfg.COL_STUDENT_ID].is_unique, 'student_id not unique post-ingest'
    assert 'data_quality_warnings' in df.attrs, 'warnings attribute missing'
    print(f'SMOKE OK: {len(df)} students, {len(df.attrs[\"data_quality_warnings\"])} warnings')"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_ingestion.py -x -v` exits 0 (all 9 tests pass)
    - src/ingestion.py exports a single public function `ingest(data_paths: dict[str, Path]) -> pd.DataFrame`
    - The locked signature is exactly `ingest(data_paths)` — single parameter named `data_paths`
    - src/ingestion.py defines module-level `DTYPE_METRICS`, `DTYPE_NOTES`, `DTYPE_META` dicts keyed on `cfg.COL_*` constants (not hardcoded strings)
    - Every `pd.read_csv` call passes a `dtype=` argument (verified by grep)
    - src/ingestion.py contains no hardcoded path literals like `"data/"`, `"outputs/"`, `"docs/"`
    - Every function has a return type annotation and a docstring (INFRA-08)
    - Every `logger.warning` and `logger.info` call uses only `student_id` (no `student_name`, `parent_phone`, `note_text` interpolated into log messages) — verified by code review and the PII-safety test in test_ingestion.py
    - End-to-end smoke: generate_data.main() followed by ingest() on the generated files returns a DataFrame with &gt;= 300 unique student_ids and `df.attrs['data_quality_warnings']` populated
    - DataFrame columns include at minimum: COL_STUDENT_ID, COL_STUDENT_NAME, COL_CAMPUS_ID, COL_PARENT_PHONE, COL_FACILITATOR_EMAIL, session_total_min, practice_total_q, attendance_days, daily_session_series, daily_practice_series, daily_dates, latest_note_date, latest_note_text
    - parent_phone values in returned df all start with "0" (Pitfall #3 preserved)
    - student_id dtype is pandas string (Pitfall #1 preserved)
  </acceptance_criteria>
  <done>ingestion.py implements all DATA-02 through DATA-08 requirements; all 9 tests pass; end-to-end smoke (`python -m src.generate_data && python main.py`) runs without error.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end smoke test — `python main.py` against generated data</name>
  <files>(verification only — runs the pipeline, no file writes)</files>
  <read_first>
    - main.py (created in plan 01)
    - src/ingestion.py (created in Task 2 above)
    - src/generate_data.py (created in plan 02)
    - .planning/ROADMAP.md (Phase 1 Success Criteria L29-34)
  </read_first>
  <action>
    Run the full Phase 1 pipeline end-to-end against synthetic data to confirm INFRA-01 (orchestrator works) and all DATA-* requirements integrate cleanly.

    1. Ensure `ANTHROPIC_API_KEY=dummy` is set in environment (or in `.env`).
    2. Run `python -m src.generate_data` — must exit 0 and write 3 CSVs to data/.
    3. Run `python main.py` — must exit 0. Stderr must contain `"Starting boon-academy-intervention pipeline"`, `"Ingested 300 students"` (or 300-320 to allow for dupe edge cases), and `"Pipeline complete"`.
    4. Run `pytest tests/ -v` — must exit 0 with all tests passing across test_config.py, test_no_hardcoded_paths.py, test_package_structure.py, test_generate_data.py, and test_ingestion.py.
    5. Spot-check: re-run `python main.py` a second time; behavior must be identical (idempotent — ingest() does not mutate input files).

    This task adds no new code — it executes the integration. If any step fails, surface the failure (it indicates a defect in the implementation tasks above).
  </action>
  <verify>
    <automated>python -m src.generate_data</automated>
    <automated>python main.py 2>&amp;1 | findstr /C:"Pipeline complete"</automated>
    <automated>pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `python -m src.generate_data` exits 0 and creates data/student_metadata.csv, data/student_daily_metrics.csv, data/facilitator_notes.csv
    - `python main.py` exits 0
    - main.py's stderr output contains "Starting boon-academy-intervention pipeline", "Ingested 300 students" (or "Ingested N students" where N is between 300 and 320 due to duplicate injection then dedup), and "Pipeline complete"
    - `pytest tests/ -v` exits 0 with all tests across all 5 test files passing
    - Second invocation of `python main.py` produces equivalent log output (idempotent)
  </acceptance_criteria>
  <done>Phase 1 end-to-end pipeline is operational. Phase 2 has a stable DataFrame schema and a clean foundation to build on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV files → Process | data_paths argument provides Path objects to local CSV files; content is untrusted insofar as malformed records must not crash the pipeline (DATA-08) |
| Per-row data → Logs | Cleaning warnings flow to logger.warning calls; PII (student_name, parent_phone, note_text) must NOT leak into log output |
| Ingestion output → df.attrs | warnings list flows from ingestion into Phase 4's run_log.json via df.attrs side channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Logging during ingestion | mitigate | Lock convention: every `logger.warning`/`logger.info` includes only `student_id`, never `student_name` / `parent_phone` / `note_text`. Verified by PII-safety assertion in tests/test_ingestion.py using `caplog`. Per Security V7 / 01-RESEARCH.md L980. |
| T-03-02 | Tampering | CSV injection (formula cells in name fields) | accept | Deferred to Phase 4 (Excel output) per 01-RESEARCH.md L977. Phase 1 reads strings as strings — no formula evaluation occurs. Will revisit when writing xlsx. |
| T-03-03 | Denial of Service | Crash on a single bad record (DATA-08) | mitigate | Per-row error containment pattern (01-RESEARCH.md Pattern 3): every cleaning step catches errors per-row via `errors='coerce'`, logs, and appends to warnings list. Tests `test_bad_record_does_not_crash` and `test_empty_csv_handled` enforce. |
| T-03-04 | Tampering | DataFrame schema drift between phases | mitigate | The locked signature `ingest(data_paths) -> pd.DataFrame` is the boundary contract (STATE.md L93). All downstream phases assume the canonical schema documented in the docstring. Changes to the returned columns require a deliberate STATE.md update. |
| T-03-05 | Information Disclosure | Empty CSV / FileNotFoundError handling | accept | Empty CSV is handled (DataFrame with header only → empty df returned). FileNotFoundError is allowed to propagate — main.py logs the failure clearly. No silent failure. |
| T-03-06 | Tampering | Numeric type mismatch (string in numeric column → silent crash) | mitigate | `pd.to_numeric(series, errors='coerce')` first step in `_fill_numeric_with_zero` converts any string to NaN safely; NaN then filled with 0 and a `type_mismatch` warning emitted. Pitfall #1 and DATA-05 covered. |
| T-03-07 | Tampering | Date format ambiguity (Pitfall #6) | mitigate | `_coerce_dates` always passes `format="%Y-%m-%d"` to `pd.to_datetime` — no silent date misinterpretation. |
</threat_model>

<verification>
After all 3 tasks complete:

1. **Test suite green**: `pytest tests/ -v` exits 0 with at least 22 tests passing (3 config + 1 no-hardcoded-paths + 1 package + 7 generate_data + 9 ingestion = 21 minimum).
2. **End-to-end pipeline**: `python -m src.generate_data && python main.py` exits 0 with expected log lines.
3. **Schema lock**: Returned DataFrame from ingest() has all canonical columns documented in the function's docstring.
4. **Determinism**: Repeated runs of generate → ingest produce identical DataFrame row counts (modulo edge-case randomness controlled by SEED=42).
5. **Warning attribute survives**: `df.attrs['data_quality_warnings']` is populated and is a list of dicts.
6. **PII-safety**: caplog inspection in test_ingestion.py confirms no student_name or phone strings appear in warning/info logs.
7. **Smoke matches roadmap**: ROADMAP.md Phase 1 Success Criterion #1 ("`python main.py` runs without error and produces a single merged DataFrame with one row per student_id") is verifiable.
</verification>

<success_criteria>
- src/ingestion.py implements ingest(data_paths) with the locked STATE.md L93 signature
- All 9 tests in tests/test_ingestion.py pass
- End-to-end: `python -m src.generate_data` then `python main.py` exits 0 and logs "Ingested N students" where N is between 300 and 320
- df.attrs['data_quality_warnings'] is populated after ingestion of synthetic data with edge cases
- PII (student_name, parent_phone, note_text) never appears in log messages — only student_id
- DATA-02 through DATA-08 and INFRA-01 (orchestrator wires ingest correctly) all satisfied
- ROADMAP.md Phase 1 Success Criteria #1 and #2 are verifiable from the running pipeline
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-ingestion/01-03-SUMMARY.md` when done. Summary must record: actual student count produced by generator on this run, actual warning counts by type (missing_numeric, duplicate_id, bad_date, type_mismatch), and any deviations from the patterns in 01-RESEARCH.md.
</output>
