---
phase: 05-html-dashboard-word-report
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/output_generator.py
  - tests/test_output_generator.py
autonomous: true
requirements:
  - OUT-04
  - OUT-05

must_haves:
  truths:
    - "write_outputs() returns a dict containing both 'dashboard' and 'report' keys"
    - "The Path value for 'dashboard' key points to an existing facilitator_dashboard.html file"
    - "The Path value for 'report' key points to an existing intervention_report.docx file"
    - "Running python main.py produces all 6 output files including both new files"
    - "The integration test asserting 'dashboard' and 'report' keys passes"
    - "HTML file contains const studentsData = asserted by integration test"
  artifacts:
    - path: "src/output_generator.py"
      provides: "write_outputs() extended to call _write_html_dashboard and _write_report"
      contains: "dashboard_path = _write_html_dashboard"
    - path: "tests/test_output_generator.py"
      provides: "Extended integration test asserting 'dashboard' and 'report' keys"
      contains: "dashboard"
  key_links:
    - from: "write_outputs()"
      to: "_write_html_dashboard(df, output_dir)"
      via: "dashboard_path = _write_html_dashboard(df, output_dir); paths['dashboard'] = dashboard_path"
      pattern: "dashboard_path"
    - from: "write_outputs()"
      to: "_write_report(df, run_log, output_dir)"
      via: "report_path = _write_report(df, run_log, output_dir); paths['report'] = report_path"
      pattern: "report_path"
---

<objective>
Wire _write_html_dashboard() and _write_report() into write_outputs(), extend the integration test to assert both new keys, and verify the full pipeline produces all 6 output files.

Purpose: write_outputs() is the single orchestration point for all output files. Adding the two new helpers here ensures main.py (which already calls write_outputs()) automatically produces the HTML dashboard and Word report without any main.py changes.

Output: write_outputs() in src/output_generator.py extended with 2 new calls and 2 new return dict keys. Two integration tests extended/added in tests/test_output_generator.py.
</objective>

<execution_context>
@C:\Users\abdul\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\abdul\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\ROADMAP.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\STATE.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\05-html-dashboard-word-report\05-CONTEXT.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\05-html-dashboard-word-report\05-PATTERNS.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\05-html-dashboard-word-report\05-01-SUMMARY.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\05-html-dashboard-word-report\05-02-SUMMARY.md

<interfaces>
<!-- Key signatures the executor needs. Extracted from codebase after plans 05-01 and 05-02 complete. -->

From src/output_generator.py (write_outputs — current state after phases 1-4, lines 290-341):
  def write_outputs(df: pd.DataFrame, output_dir: Path, run_log: dict) -> dict[str, Path]:
      output_dir.mkdir(parents=True, exist_ok=True)
      paths: dict[str, Path] = {}
      priority_path = _write_priority_list(df, output_dir)
      paths["priority_list"] = priority_path
      campus_paths = _write_campus_dashboards(df, output_dir)
      paths.update(campus_paths)
      whatsapp_path = _write_whatsapp_csv(df, output_dir)
      paths["whatsapp"] = whatsapp_path
      run_log_path = _write_run_log(run_log, output_dir)
      paths["run_log"] = run_log_path
      logger.info("All outputs written to %s — keys: %s", output_dir, list(paths.keys()))
      return paths

New helpers added by 05-01 and 05-02:
  def _write_html_dashboard(df: pd.DataFrame, output_dir: Path) -> Path: ...
  def _write_report(df: pd.DataFrame, run_log: dict, output_dir: Path) -> Path: ...

From tests/test_output_generator.py (lines 559-578 — test to extend):
  def test_write_outputs_returns_all_keys(full_sample_df, sample_run_log_full, tmp_path):
      result = write_outputs(full_sample_df, tmp_path, sample_run_log_full)
      assert "priority_list" in result
      assert len([k for k in result if k.startswith("campus_")]) >= 1
      assert "whatsapp" in result
      assert "run_log" in result
      # Phase 5 adds: assert "dashboard" in result + assert "report" in result
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend write_outputs() to call both new helpers and update docstring</name>
  <files>src/output_generator.py</files>
  <read_first>
    - src/output_generator.py — read lines 290-342 (write_outputs body in its current Phase 4 state — confirm exact line numbers and structure before editing, as 05-01 and 05-02 added new functions above this section)
  </read_first>
  <action>
    In write_outputs(), add the two new helper calls immediately before the final logger.info call (after paths["run_log"] = run_log_path):

      dashboard_path = _write_html_dashboard(df, output_dir)
      paths["dashboard"] = dashboard_path

      report_path = _write_report(df, run_log, output_dir)
      paths["report"] = report_path

    Update the write_outputs() docstring Returns: section to add:
      "dashboard": Path to facilitator_dashboard.html (OUT-05)
      "report": Path to intervention_report.docx (OUT-04)

    Replace the inline comment "D-09: Phase 5 will add _write_report() and _write_html_dashboard() here." with the actual calls (the comment is now obsolete).

    Update the docstring description from "Write all Phase 4 output files" to "Write all output files (Phase 4: Excel/CSV/JSON; Phase 5: HTML dashboard and Word report)".

    No other changes to write_outputs() — the function signature and all existing calls remain identical.
  </action>
  <verify>
    <automated>python -c "
from pathlib import Path
import pandas as pd
from src import config as cfg
from src.output_generator import write_outputs
import inspect
src = inspect.getsource(write_outputs)
assert 'dashboard_path = _write_html_dashboard' in src, 'Missing dashboard_path call'
assert 'report_path = _write_report' in src, 'Missing report_path call'
assert 'paths[\"dashboard\"]' in src or \"paths['dashboard']\" in src, 'Missing dashboard key assignment'
print('write_outputs wiring OK')
"</automated>
  </verify>
  <done>
    - write_outputs() body contains dashboard_path = _write_html_dashboard(df, output_dir) and paths["dashboard"] = dashboard_path
    - write_outputs() body contains report_path = _write_report(df, run_log, output_dir) and paths["report"] = report_path
    - The obsolete D-09 placeholder comment is removed
    - Docstring Returns section lists "dashboard" and "report" keys
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend integration tests and run full test suite</name>
  <files>tests/test_output_generator.py</files>
  <read_first>
    - tests/test_output_generator.py — read lines 559-605 (test_write_outputs_returns_all_keys and test_write_outputs_all_paths_exist — understand exact assertions before extending)
  </read_first>
  <action>
    Extend test_write_outputs_returns_all_keys (lines 559-578): add two assertions after the existing four:
      assert "dashboard" in result, f"Missing key 'dashboard' in result: {list(result.keys())}"
      assert "report" in result, f"Missing key 'report' in result: {list(result.keys())}"

    The existing test_write_outputs_all_paths_exist already iterates all keys and asserts path.exists() for each — this test automatically covers the new "dashboard" and "report" keys without modification.

    Add one new integration test immediately after test_write_outputs_creates_output_dir (the last test in the file):

      def test_write_outputs_html_contains_embedded_json(
          full_sample_df: pd.DataFrame,
          sample_run_log_full: dict,
          tmp_path: Path,
      ) -> None:
          """write_outputs integration: facilitator_dashboard.html contains const studentsData."""
          result = write_outputs(full_sample_df, tmp_path, sample_run_log_full)
          html_path = result["dashboard"]
          content = html_path.read_text(encoding="utf-8")
          assert "const studentsData" in content, (
              f"Expected 'const studentsData' in {html_path.name}, got first 200 chars: {content[:200]}"
          )
          assert "S001" in content, (
              "Expected student_id S001 to appear in embedded JSON"
          )

    Run the full test suite to confirm all tests pass (this is the terminal verification step).
  </action>
  <verify>
    <automated>py -3.12 -m pytest tests/test_output_generator.py -x -q</automated>
  </verify>
  <done>
    - test_write_outputs_returns_all_keys now asserts both "dashboard" and "report" keys exist
    - test_write_outputs_html_contains_embedded_json exists and passes
    - py -3.12 -m pytest tests/test_output_generator.py -x -q exits 0 with all tests passing (99 prior + new Phase 5 tests)
    - py -3.12 -m pytest tests/ -x -q exits 0 (full suite green)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| write_outputs return dict -> main.py | All 6+ output paths returned and logged by main.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | write_outputs orchestrator call order | accept | _write_html_dashboard and _write_report added last — no ordering dependency with existing helpers; output_dir already created by mkdir at top |
| T-05-08 | Denial of Service | _write_report failure halting pipeline | mitigate | Both new helpers follow same pattern as existing helpers — exceptions propagate to write_outputs caller (main.py); consistent with existing behavior for _write_priority_list etc. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies in this wiring plan |
</threat_model>

<verification>
After both tasks complete:
- py -3.12 -m pytest tests/ -x -q exits 0 — full suite green including all Phase 5 tests
- python main.py runs without error and logs keys including "dashboard" and "report"
- outputs/facilitator_dashboard.html and outputs/intervention_report.docx both exist after main.py run
- Opening facilitator_dashboard.html in a browser via file:// shows populated student table
- Opening intervention_report.docx in Word or Google Docs shows all 7 sections without rendering errors
</verification>

<success_criteria>
- write_outputs() return dict contains exactly: "priority_list", "campus_*" per campus, "whatsapp", "run_log", "dashboard", "report"
- All Phase 5 tests pass (HTML tests from 05-01, report tests from 05-02, integration tests from this plan)
- Full test suite (all phases) remains green
- python main.py end-to-end produces 6 output files with both new files present and valid
</success_criteria>

<output>
Create .planning/phases/05-html-dashboard-word-report/05-03-SUMMARY.md when done
</output>
