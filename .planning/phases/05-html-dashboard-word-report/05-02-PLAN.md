---
phase: 05-html-dashboard-word-report
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/output_generator.py
  - tests/test_output_generator.py
autonomous: true
requirements:
  - OUT-04

must_haves:
  truths:
    - "outputs/intervention_report.docx opens in Word and Google Docs without rendering errors"
    - "Cover page shows title, run date, campus count, and total students processed"
    - "Executive summary has a narrative paragraph and a risk breakdown table (risk_level | count | %)"
    - "Top-10 most at-risk students table lists rank, name, campus, risk_score, risk_level"
    - "Campus summary table shows campus_id, total students, critical count, high count, coverage %"
    - "Up to 4 student deep-dive sections (one per risk tier) each show name, campus, risk level, risk score, 4 component scores, facilitator summary, and recommended action"
    - "Data quality notes section lists warnings from run_log['data_quality_warnings']"
    - "Methodology appendix describes the 4 weighted components and risk thresholds using values from cfg.WEIGHT_* and cfg.RISK_THRESHOLD_* constants"
    - "All tables use style='Table Grid' — no custom XML or OxmlElement"
    - "All headings use built-in heading levels (level=0/1/2) — no custom styles"
  artifacts:
    - path: "src/output_generator.py"
      provides: "_write_report() helper function"
      contains: "def _write_report"
    - path: "tests/test_output_generator.py"
      provides: "Unit tests for _write_report"
      contains: "test_report"
  key_links:
    - from: "src/output_generator.py/_write_report"
      to: "run_log['data_quality_warnings']"
      via: "run_log parameter passed to helper"
      pattern: "data_quality_warnings"
    - from: "src/output_generator.py/_write_report"
      to: "cfg.WEIGHT_ATTENDANCE / cfg.RISK_THRESHOLD_CRITICAL"
      via: "methodology appendix f-string"
      pattern: "WEIGHT_ATTENDANCE"
---

<objective>
Implement _write_report() in output_generator.py — a programmatic python-docx builder that produces intervention_report.docx with all 7 required sections — plus unit tests.

Purpose: A facilitator or director can open intervention_report.docx in Word or Google Docs and immediately understand the intervention landscape: who is at risk, by how much, which campuses need focus, and exactly why the four highest-risk students are flagged.

Output: _write_report() function added to src/output_generator.py, 5 test functions added to tests/test_output_generator.py. No new files, no new dependencies.
</objective>

<execution_context>
@C:\Users\abdul\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\abdul\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\ROADMAP.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\STATE.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\05-html-dashboard-word-report\05-CONTEXT.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\05-html-dashboard-word-report\05-PATTERNS.md

<interfaces>
<!-- Key constants and signatures the executor needs. Extracted from codebase. -->

From src/config.py (constants used in _write_report):
  WEIGHT_ATTENDANCE: float = 0.35
  WEIGHT_PRACTICE: float = 0.30
  WEIGHT_TREND: float = 0.20
  WEIGHT_NOTES: float = 0.15
  RISK_THRESHOLD_CRITICAL: int = 75  (env-overridable)
  RISK_THRESHOLD_HIGH: int = 50
  RISK_THRESHOLD_MEDIUM: int = 25
  COL_STUDENT_NAME, COL_CAMPUS_ID, COL_RISK_SCORE, COL_RISK_LEVEL
  COL_ATTENDANCE_RATE, COL_AVG_PRACTICE, COL_TREND_DIR, COL_DAYS_SINCE_NOTE
  COL_FACILITATOR_SUMMARY, COL_RECOMMENDED_ACTION
  COL_ATTENDANCE_COMPONENT, COL_PRACTICE_COMPONENT, COL_TREND_COMPONENT, COL_NOTES_COMPONENT

From src/output_generator.py (lines 155-157, multi-param helper pattern):
  def _write_report(df: pd.DataFrame, run_log: dict, output_dir: Path) -> Path:
  # df_copy = df.copy() at top
  # path = output_dir / "intervention_report.docx"
  # doc.save(str(path))  -- python-docx requires str path on Windows
  # logger.info("Wrote Word report: %s (%d students)", path, len(df_copy))
  # return path

python-docx 1.1.2 constraints (STATE.md):
  - Document() — programmatic build, no binary template
  - add_heading(text, level=0) for title, level=1 for section, level=2 for subsection
  - add_paragraph(text) for narrative
  - add_table(rows, cols, style="Table Grid") for ALL tables
  - NEVER use OxmlElement — known issues in 1.1.2
  - NEVER use custom table styles — use "Table Grid" only
  - doc.save(str(path)) not doc.save(path) — str() required on Windows

From tests/test_output_generator.py (lines 72-83, sample_run_log fixture):
  sample_run_log = {
    "run_timestamp": "2026-01-01T00:00:00+00:00",
    "students_processed": 10,
    "api_calls_made": 2,
    "tokens_used": {"input": 100, "output": 50},
    "errors_encountered": [],
    "fallbacks_triggered": 0,
    "data_quality_warnings": [],
  }
  # sample_df fixture has 4 rows: S001 CRITICAL, S002 HIGH, S003 MEDIUM, S004 LOW
  # COL_FACILITATOR_SUMMARY: ["Alice is at risk", "Bob needs attention", None, None]
  # COL_RECOMMENDED_ACTION: ["Contact parent immediately", "Schedule check-in", ...]
  # COL_ATTENDANCE_COMPONENT, COL_PRACTICE_COMPONENT, etc. are NOT in sample_df
  # _write_report must handle missing component columns gracefully (use getattr/get with default 0.0)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _write_report() in output_generator.py</name>
  <files>src/output_generator.py</files>
  <read_first>
    - src/output_generator.py — read full file (all 342 lines; add _write_report after _write_html_dashboard, before _write_priority_list, maintaining the logical grouping of output helpers)
    - src/config.py — read lines 60-104 (WEIGHT_*, RISK_THRESHOLD_*, COL_ATTENDANCE_COMPONENT through COL_NOTES_COMPONENT — confirm constant names before referencing)
    - .planning/phases/05-html-dashboard-word-report/05-CONTEXT.md — D-07 through D-13 (narrative source, deep-dive content, section order)
  </read_first>
  <behavior>
    Function: _write_report(df: pd.DataFrame, run_log: dict, output_dir: Path) -> Path

    Section 1 — Cover page:
      doc.add_heading("Boon Academy — Student Intervention Report", level=0)
      doc.add_paragraph(f"Run date: {run_log.get('run_timestamp', 'N/A')}")
      campus_count = df_copy[cfg.COL_CAMPUS_ID].nunique()
      doc.add_paragraph(f"Campuses: {campus_count}")
      doc.add_paragraph(f"Students processed: {run_log.get('students_processed', len(df_copy))}")

    Section 2 — Executive summary:
      doc.add_heading("Executive Summary", level=1)
      Compute: total = len(df_copy); critical_count = int((df_copy[COL_RISK_LEVEL]=="CRITICAL").sum()); etc.
      doc.add_paragraph(f"Of {total} students, {critical_count} ({critical_count/total*100:.0f}%) are at CRITICAL risk requiring immediate intervention.")
      Risk breakdown table: 5 rows (header + 4 risk levels), 3 cols (Risk Level | Count | % of Total)
        table = doc.add_table(rows=5, cols=3, style="Table Grid")
        Row 0 headers: "Risk Level", "Count", "% of Total"
        Rows 1-4: CRITICAL, HIGH, MEDIUM, LOW counts and percentages
        Use df_copy[COL_RISK_LEVEL].value_counts() to get counts; fill missing levels with 0

    Section 3 — Top 10 most at-risk:
      doc.add_heading("Top 10 Most At-Risk Students", level=1)
      top10 = df_copy.nlargest(10, cfg.COL_RISK_SCORE) (or all students if fewer than 10)
      Table: len(top10)+1 rows (header + data), 5 cols (Rank | Name | Campus | Risk Score | Risk Level)
        Row 0 headers: "Rank", "Student Name", "Campus", "Risk Score", "Risk Level"
        Data rows: 1-based rank, COL_STUDENT_NAME, COL_CAMPUS_ID, round(COL_RISK_SCORE,1), COL_RISK_LEVEL

    Section 4 — Campus summary:
      doc.add_heading("Campus Summary", level=1)
      Group by COL_CAMPUS_ID, compute total/critical/high/coverage per campus
      Table: campus_count+1 rows, 5 cols (Campus | Total | Critical | High | Coverage %)
        Row 0 headers: "Campus", "Total Students", "Critical", "High", "Intervention Coverage %"

    Section 5 — Student deep-dives:
      doc.add_heading("Student Deep-Dives", level=1)
      For each tier in ["CRITICAL", "HIGH", "MEDIUM", "LOW"]:
        tier_df = df_copy[df_copy[COL_RISK_LEVEL] == tier]
        If tier_df is empty: skip (no section added for that tier — D-08 graceful degradation)
        student = tier_df.nlargest(1, COL_RISK_SCORE).iloc[0]
        doc.add_heading(f"{tier} — {student[COL_STUDENT_NAME]}", level=2)
        doc.add_paragraph(f"Campus: {student[COL_CAMPUS_ID]} | Risk Score: {student[COL_RISK_SCORE]:.1f} | Level: {student[COL_RISK_LEVEL]}")
        Component scores table: 4 rows (one per component) + header, 2 cols (Component | Score)
          headers: "Component", "Score"
          Rows: attendance_rate, avg_practice_questions, trend_direction, days_since_last_note
          Values: student.get(col, "N/A") using dict-style access (use student[col] if col in df_copy.columns else "N/A")
        doc.add_paragraph(f"Facilitator Summary: {student.get(COL_FACILITATOR_SUMMARY, 'N/A') or 'N/A'}")
        doc.add_paragraph(f"Recommended Action: {student.get(COL_RECOMMENDED_ACTION, 'N/A') or 'N/A'}")
        WhatsApp message is NOT included (D-09 spec)

    Section 6 — Data quality notes:
      doc.add_heading("Data Quality Notes", level=1)
      warnings = run_log.get("data_quality_warnings", [])
      If not warnings: doc.add_paragraph("No data quality issues detected in this run.")
      Else: for w in warnings: doc.add_paragraph(f"• {w}")

    Section 7 — Methodology appendix:
      doc.add_heading("Methodology Appendix", level=1)
      doc.add_heading("Risk Score Formula", level=2)
      doc.add_paragraph("Risk score (0-100) is computed as a weighted sum of four components:")
      4-row table, 2 cols (Component | Weight):
        Attendance Rate | cfg.WEIGHT_ATTENDANCE (35%)
        Avg Practice Questions | cfg.WEIGHT_PRACTICE (30%)
        Trend Direction | cfg.WEIGHT_TREND (20%)
        Days Since Last Note | cfg.WEIGHT_NOTES (15%)
      doc.add_heading("Risk Level Thresholds", level=2)
      doc.add_paragraph(f"CRITICAL: score >= {cfg.RISK_THRESHOLD_CRITICAL}")
      doc.add_paragraph(f"HIGH: {cfg.RISK_THRESHOLD_HIGH} <= score < {cfg.RISK_THRESHOLD_CRITICAL}")
      doc.add_paragraph(f"MEDIUM: {cfg.RISK_THRESHOLD_MEDIUM} <= score < {cfg.RISK_THRESHOLD_HIGH}")
      doc.add_paragraph(f"LOW: score < {cfg.RISK_THRESHOLD_MEDIUM}")

    File save: path = output_dir / "intervention_report.docx"; doc.save(str(path))  [str() required on Windows]
  </behavior>
  <action>
    Add from docx import Document to the imports section of src/output_generator.py (after the existing openpyxl imports, grouped with other third-party imports). Add _write_report() immediately after _write_html_dashboard() and before _write_priority_list().

    Full docstring must follow the Args/Returns pattern of existing helpers. Include references to D-10 (programmatic build), D-11 (built-in heading levels), D-12 (Table Grid style), D-13 (section order).

    All column references use cfg.COL_* — no bare strings. All weight/threshold references use cfg.WEIGHT_* and cfg.RISK_THRESHOLD_* — no hardcoded floats/ints.

    Guard against missing component columns: use `student[col] if col in df_copy.columns else "N/A"` pattern (sample_df in tests does not have the component columns COL_ATTENDANCE_COMPONENT etc. — the deep-dive component table shows the 4 output columns, not the internal component columns).

    For the deep-dive component scores table, use the 4 end-user-facing columns: COL_ATTENDANCE_RATE, COL_AVG_PRACTICE, COL_TREND_DIR, COL_DAYS_SINCE_NOTE — these are always present in the enriched DataFrame.
  </action>
  <verify>
    <automated>python -c "from src.output_generator import _write_report; print('_write_report importable OK')"</automated>
  </verify>
  <done>
    - src/output_generator.py contains def _write_report(df: pd.DataFrame, run_log: dict, output_dir: Path) -> Path:
    - from docx import Document is present in imports
    - All 7 document sections are built using only add_heading/add_paragraph/add_table — no OxmlElement
    - All table style arguments are exactly style="Table Grid"
    - doc.save(str(path)) used (not doc.save(path))
    - All column and weight references use cfg.* constants
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _write_report unit tests to test_output_generator.py</name>
  <files>tests/test_output_generator.py</files>
  <read_first>
    - tests/test_output_generator.py — read lines 1-21 (import block) and lines 194-197 (fixture pattern) and lines 559-604 (integration test pattern) — understand what already exists before extending
    - src/output_generator.py — read the completed _write_report() to confirm function signature and output filename
  </read_first>
  <behavior>
    Extend import block (lines 15-21): add _write_report to the from src.output_generator import (...) block.

    Add report_path fixture mirroring priority_list_path:
      @pytest.fixture
      def report_path(sample_df: pd.DataFrame, sample_run_log: dict, tmp_path: Path) -> Path:
          return _write_report(sample_df, sample_run_log, tmp_path)

    Tests (5 functions in section "# _write_report tests (OUT-04)"):

    test_report_returns_path(report_path):
      assert isinstance(report_path, Path)
      assert report_path.exists()
      assert report_path.name == "intervention_report.docx"

    test_report_is_valid_docx(report_path):
      from docx import Document
      doc = Document(str(report_path))
      assert len(doc.paragraphs) > 0, "Expected non-empty Word document"

    test_report_contains_cover_heading(report_path):
      from docx import Document
      doc = Document(str(report_path))
      headings = [p.text for p in doc.paragraphs if p.style.name.startswith("Heading")]
      assert any("Intervention Report" in h for h in headings), f"Cover heading not found. Headings: {headings}"

    test_report_contains_executive_summary(report_path):
      from docx import Document
      doc = Document(str(report_path))
      headings = [p.text for p in doc.paragraphs if p.style.name.startswith("Heading")]
      assert any("Executive Summary" in h for h in headings), f"'Executive Summary' heading not found. Headings: {headings}"

    test_report_contains_tables(report_path):
      from docx import Document
      doc = Document(str(report_path))
      assert len(doc.tables) >= 3, f"Expected at least 3 tables (risk breakdown, top-10, campus summary), got {len(doc.tables)}"

    test_report_data_quality_no_warnings(report_path):
      from docx import Document
      doc = Document(str(report_path))
      all_text = " ".join(p.text for p in doc.paragraphs)
      assert "No data quality issues" in all_text, "Expected 'No data quality issues' paragraph when warnings list is empty"
  </behavior>
  <action>
    Extend tests/test_output_generator.py: add _write_report to the import block. Add report_path fixture immediately after the html_dashboard_path fixture (added by plan 05-01). Add the 6 test functions in a new section. Import from docx import Document inside each test function body (not at module level) to mirror the lazy-import pattern used in test_run_log_default_str_handles_non_serializable.

    Note: sample_df does not have COL_ATTENDANCE_COMPONENT columns — _write_report must handle this gracefully (Task 1 guards against it). Tests do not need to assert component scores specifically; test_report_contains_tables and test_report_is_valid_docx provide the structural guarantee.
  </action>
  <verify>
    <automated>py -3.12 -m pytest tests/test_output_generator.py -k "report" -x -q</automated>
  </verify>
  <done>
    - All 6 report tests pass (py -3.12 -m pytest tests/test_output_generator.py -k "report" exits 0)
    - py -3.12 -m pytest tests/ -x -q exits 0 (all prior tests remain green)
    - test_report_contains_tables asserts >= 3 tables exist in the docx
    - test_report_data_quality_no_warnings asserts correct empty-warnings text
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_log dict -> docx paragraphs | run_log["data_quality_warnings"] contents written as document text |
| DataFrame values -> docx table cells | Student names, campus IDs, scores written verbatim into Word cells |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | intervention_report.docx (contains student names + risk data) | accept | File written to local outputs/ dir; same sensitivity as existing Excel outputs; no additional exposure |
| T-05-05 | Tampering | run_log["data_quality_warnings"] text in docx | accept | run_log is pipeline-internal; only plain warning strings from ingestion; no user-controlled input reaches this field |
| T-05-06 | Denial of Service | doc.add_table with very large top-10 (capped at 10 rows) | mitigate | df.nlargest(10, ...) hard cap — table never exceeds 11 rows regardless of DataFrame size |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies — python-docx 1.1.2 already in requirements.txt |
</threat_model>

<verification>
After both tasks complete:
- py -3.12 -m pytest tests/test_output_generator.py -k "report" -x -q exits 0 (all 6 report tests pass)
- py -3.12 -m pytest tests/ -x -q exits 0 (all prior tests remain green, no regression)
- python -c "from src.output_generator import _write_report; print('OK')" exits 0
- Running python main.py produces outputs/intervention_report.docx — open the file and confirm: cover page heading visible, Executive Summary heading visible, at least 3 tables present, no rendering errors
</verification>

<success_criteria>
- intervention_report.docx opens in Word and Google Docs without errors
- All 7 sections present: cover, exec summary, top-10, campus summary, deep-dives, data quality notes, methodology appendix
- All tables use "Table Grid" style — no OxmlElement used anywhere
- 6 report unit tests pass
- All 99 prior tests remain green
</success_criteria>

<output>
Create .planning/phases/05-html-dashboard-word-report/05-02-SUMMARY.md when done
</output>
