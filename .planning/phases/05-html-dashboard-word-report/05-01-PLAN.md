---
phase: 05-html-dashboard-word-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/output_generator.py
  - src/templates/dashboard.html.j2
  - tests/test_output_generator.py
autonomous: true
requirements:
  - OUT-05

must_haves:
  truths:
    - "Opening outputs/facilitator_dashboard.html via file:// renders a table of students sorted by risk_score descending"
    - "Campus filter <select> shows all campus IDs from the data and filters the table rows"
    - "Risk-level filter buttons (CRITICAL / HIGH / MEDIUM / LOW / ALL) filter the table"
    - "Name search input performs case-insensitive substring match on student_name"
    - "Clicking a student row expands a detail panel showing risk breakdown, facilitator summary, WhatsApp message, and a Copy button"
    - "The Copy button copies WhatsApp message text to clipboard (Clipboard API with execCommand fallback)"
    - "Summary stats at top of page show total students, CRITICAL count, HIGH count, intervention coverage %"
    - "The HTML file has no external network requests — all CSS, JS, and data are inline"
    - "Arabic student names render correctly (UTF-8 encoding, meta charset)"
  artifacts:
    - path: "src/templates/dashboard.html.j2"
      provides: "Jinja2 template with all HTML/CSS/JS inline"
      contains: "studentsData"
    - path: "src/config.py"
      provides: "DISPLAY_COLS_DASHBOARD tuple constant"
      contains: "DISPLAY_COLS_DASHBOARD"
    - path: "src/output_generator.py"
      provides: "_write_html_dashboard() helper function"
      contains: "def _write_html_dashboard"
    - path: "tests/test_output_generator.py"
      provides: "Unit tests for _write_html_dashboard"
      contains: "test_html_dashboard"
  key_links:
    - from: "src/output_generator.py/_write_html_dashboard"
      to: "src/templates/dashboard.html.j2"
      via: "Jinja2 FileSystemLoader from Path(__file__).parent / 'templates'"
      pattern: "FileSystemLoader"
    - from: "src/output_generator.py/_write_html_dashboard"
      to: "src/config.py DISPLAY_COLS_DASHBOARD"
      via: "cfg.DISPLAY_COLS_DASHBOARD column selection"
      pattern: "DISPLAY_COLS_DASHBOARD"
    - from: "dashboard.html.j2 <script>"
      to: "studentsData JS const"
      via: "{{ students_json }} Jinja2 variable injection"
      pattern: "studentsData"
---

<objective>
Create the self-contained HTML dashboard: Jinja2 template file, DISPLAY_COLS_DASHBOARD config constant, and _write_html_dashboard() helper — all tested.

Purpose: Facilitators can open facilitator_dashboard.html directly in any browser (file://) and immediately see all students ranked by risk, filter by campus/level/name, expand a row for detail, and copy the WhatsApp message — with no server and no network requests.

Output: src/templates/dashboard.html.j2, src/config.py extended with DISPLAY_COLS_DASHBOARD, src/output_generator.py extended with _write_html_dashboard(), tests/test_output_generator.py extended with HTML unit tests.
</objective>

<execution_context>
@C:\Users\abdul\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\abdul\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\ROADMAP.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\STATE.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\05-html-dashboard-word-report\05-CONTEXT.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\05-html-dashboard-word-report\05-PATTERNS.md

<interfaces>
<!-- Key constants and signatures the executor needs. Extracted from codebase. -->

From src/config.py (lines 86-130):
  COL_STUDENT_ID = "student_id"
  COL_STUDENT_NAME = "student_name"
  COL_CAMPUS_ID = "campus_id"
  COL_RISK_SCORE = "risk_score"
  COL_RISK_LEVEL = "risk_level"
  COL_ATTENDANCE_RATE = "attendance_rate"
  COL_AVG_PRACTICE = "avg_practice_questions"
  COL_TREND_DIR = "trend_direction"
  COL_DAYS_SINCE_NOTE = "days_since_last_note"
  COL_FACILITATOR_SUMMARY = "facilitator_summary"
  COL_WHATSAPP_MESSAGE = "whatsapp_message"
  COL_GENERATED_BY = "generated_by"
  COLOR_CRITICAL = "FFFFCCCC"   # -> CSS #FFCCCC (strip leading FF)
  COLOR_HIGH     = "FFFFE5CC"   # -> CSS #FFE5CC
  COLOR_MEDIUM   = "FFFFFFCC"   # -> CSS #FFFFCC
  COLOR_LOW      = "FFCCFFCC"   # -> CSS #CCFFCC
  OUTPUT_COLS_PRIORITY: tuple  # 12 cols — reference for column ordering
  OUTPUT_COLS_CAMPUS: tuple    # 15 cols

From src/output_generator.py (lines 25-57):
  logger = logging.getLogger(__name__)  # already at module level — do not re-declare
  # Helper signature pattern (mirrors _write_whatsapp_csv):
  def _write_html_dashboard(df: pd.DataFrame, output_dir: Path) -> Path: ...
  # Purity discipline: df_copy = df.copy() at top of every helper
  # Return pattern: path = output_dir / "filename"; ... ; return path

From tests/test_output_generator.py (lines 24-83):
  # sample_df fixture: 4 rows, student_ids S001-S004, campuses ALPHA/BETA,
  #   all 4 risk levels, COL_FACILITATOR_SUMMARY present (None for MEDIUM/LOW),
  #   COL_WHATSAPP_MESSAGE present (empty string for MEDIUM/LOW)
  # sample_run_log fixture: 7-key dict with run_timestamp, students_processed, etc.
  # full_sample_df fixture: 6 rows, 2 campuses — reuse for integration tests
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DISPLAY_COLS_DASHBOARD to config.py and create src/templates/dashboard.html.j2</name>
  <files>src/config.py, src/templates/dashboard.html.j2</files>
  <read_first>
    - src/config.py — read lines 106-131 (OUTPUT_COLS_PRIORITY and OUTPUT_COLS_CAMPUS patterns to mirror exactly)
    - .planning/phases/05-html-dashboard-word-report/05-CONTEXT.md — D-01 through D-06 (template loading, data injection, CSS palette, JS interactivity spec, copy button code)
  </read_first>
  <behavior>
    - DISPLAY_COLS_DASHBOARD tuple contains exactly these 12 COL_* constants in order:
      COL_STUDENT_ID, COL_STUDENT_NAME, COL_CAMPUS_ID, COL_RISK_SCORE, COL_RISK_LEVEL,
      COL_ATTENDANCE_RATE, COL_AVG_PRACTICE, COL_TREND_DIR, COL_DAYS_SINCE_NOTE,
      COL_FACILITATOR_SUMMARY, COL_WHATSAPP_MESSAGE, COL_GENERATED_BY
      (No COL_RANK — JS sorts client-side. No COL_RECOMMENDED_ACTION per D-02.)
    - dashboard.html.j2 contains: DOCTYPE html, meta charset="utf-8", all CSS inline in <style>, all JS inline in <script>
    - JS const declaration is exactly: const studentsData = {{ students_json }};
    - Campus filter is a <select id="campusFilter"> populated via Jinja2 for-loop over campus_ids variable
    - Risk filter buttons have id attributes: btn-ALL, btn-CRITICAL, btn-HIGH, btn-MEDIUM, btn-LOW
    - Name search is <input id="nameSearch" type="text">
    - Student table has id="studentTable" with a <tbody id="tableBody">
    - Summary stats section has id="statsBar" — computed from studentsData on JS load
    - Expandable row detail panel uses a <tr class="detail-row"> inserted after the clicked row; toggled by click on data row
    - Detail panel contains: risk breakdown (4 component scores), facilitator_summary text, whatsapp_message text, Copy button
    - Copy button logic exactly: try { await navigator.clipboard.writeText(msg); btn.textContent='Copied!'; } catch { textarea execCommand fallback } setTimeout 2000ms reset to 'Copy'
    - CSS row colors: .risk-CRITICAL { background-color: #FFCCCC; } .risk-HIGH { background-color: #FFE5CC; } .risk-MEDIUM { background-color: #FFFFCC; } .risk-LOW { background-color: #CCFFCC; }
    - Default sort on renderTable(): by risk_score descending (highest first)
    - No external URLs anywhere in the file (no CDN links, no external fonts, no image src with http)
  </behavior>
  <action>
    In src/config.py: append DISPLAY_COLS_DASHBOARD tuple immediately after OUTPUT_COLS_CAMPUS (line 130), following the exact comment style: "# OUT-05 HTML dashboard display columns — 12 columns (D-02)". Use COL_* references, not bare strings.

    Create directory src/templates/ (it does not exist yet). Create src/templates/dashboard.html.j2 as a fully self-contained HTML file with all CSS in a single <style> block and all JS in a single <script> block at end of body.

    The Jinja2 template receives three variables from Python: students_json (pre-serialized JSON string, injected as {{ students_json }} inside the const declaration), campus_ids (list of strings, used in a {% for %} loop to build <option> elements), and run_timestamp (string, displayed in page footer).

    Use autoescape=False (injecting pre-serialized JSON, not user HTML) — this is enforced at the Environment level in the Python caller, not in the template itself.

    JS filterAndRender() function: reads campusFilter.value, active risk button's data-level attribute, nameSearch.value; filters studentsData array; calls renderTable(filtered). renderTable() sorts by risk_score descending, clears tableBody, creates one <tr> per student with class="risk-{risk_level}" plus a hidden detail <tr class="detail-row" style="display:none">, appends both to tableBody.

    Row click handler: toggles the sibling detail-row display between "none" and "table-row". Only one detail row open at a time (close others before opening new one).

    Summary stats: computed once on DOMContentLoaded from studentsData — total = studentsData.length, critical = filter by risk_level=="CRITICAL", high = filter by risk_level=="HIGH", coverage = ((critical+high)/total*100).toFixed(1).
  </action>
  <verify>
    <automated>python -c "from src import config as cfg; assert hasattr(cfg, 'DISPLAY_COLS_DASHBOARD'); assert len(cfg.DISPLAY_COLS_DASHBOARD) == 12; print('DISPLAY_COLS_DASHBOARD OK')"</automated>
  </verify>
  <done>
    - src/config.py contains DISPLAY_COLS_DASHBOARD = ( with 12 COL_* entries and no bare strings
    - src/templates/dashboard.html.j2 exists and contains: const studentsData = {{ students_json }};, id="campusFilter", id="nameSearch", id="statsBar", id="tableBody", .risk-CRITICAL, navigator.clipboard.writeText
    - No http:// or https:// URLs appear in dashboard.html.j2
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _write_html_dashboard() in output_generator.py and add unit tests</name>
  <files>src/output_generator.py, tests/test_output_generator.py</files>
  <read_first>
    - src/output_generator.py — read full file (current 342 lines; understand all imports and helper pattern before adding)
    - tests/test_output_generator.py — read lines 1-21 (import block to extend) and lines 194-197 (priority_list_path fixture pattern to mirror)
    - src/templates/dashboard.html.j2 — confirm template variable names before writing Python caller
    - src/config.py — confirm DISPLAY_COLS_DASHBOARD and COL_* names added in Task 1
  </read_first>
  <behavior>
    - _write_html_dashboard signature: (df: pd.DataFrame, output_dir: Path) -> Path
    - Starts with df_copy = df.copy() — never mutates incoming df
    - Loads Jinja2 inside function body (lazy, not at module import): from jinja2 import Environment, FileSystemLoader; template_dir = Path(__file__).parent / "templates"; env = Environment(loader=FileSystemLoader(str(template_dir)), autoescape=False); template = env.get_template("dashboard.html.j2")
    - Column selection: records = df_copy[list(cfg.DISPLAY_COLS_DASHBOARD)].where(df_copy[list(cfg.DISPLAY_COLS_DASHBOARD)].notna(), other=None).to_dict(orient="records")
    - JSON injection: students_json = json.dumps(records).replace("</", "<\\/")  — CLAUDE.md critical pitfall
    - campus_ids: sorted(df_copy[cfg.COL_CAMPUS_ID].dropna().unique().tolist())
    - Template render: html = template.render(students_json=students_json, campus_ids=campus_ids, run_timestamp=str(pd.Timestamp.now().isoformat()))
    - File write: path = output_dir / "facilitator_dashboard.html"; path.write_text(html, encoding="utf-8")
    - Log: logger.info("Wrote HTML dashboard: %s (%d students)", path, len(df_copy))
    - Returns path

    Tests:
    - html_dashboard_path fixture: calls _write_html_dashboard(sample_df, tmp_path) and returns path
    - test_html_dashboard_returns_path: isinstance Path, exists, name == "facilitator_dashboard.html"
    - test_html_dashboard_contains_student_data: content contains "studentsData", contains "S001"
    - test_html_dashboard_contains_campus_ids: content contains "ALPHA" and "BETA" (from sample_df campus_ids)
    - test_html_dashboard_no_external_urls: content does not contain "https://" and does not contain "http://" (confirms file is self-contained)
    - test_html_dashboard_escape_script_tag: inject a student with whatsapp_message containing "</script>" — assert the rendered HTML does not contain literal "</script>" inside the JSON data block (the replace("</", "<\\/") pitfall is enforced)
  </behavior>
  <action>
    In src/output_generator.py: add import for jinja2 is NOT needed at module top — the import is done lazily inside _write_html_dashboard(). json is already imported at line 14. Add _write_html_dashboard() function after _write_whatsapp_csv() (before _write_priority_list at line 82), following the exact docstring style of existing helpers (Args, Returns, description of D-reference).

    Extend test file imports: add _write_html_dashboard to the from src.output_generator import (...) block at lines 15-21.

    Add the html_dashboard_path fixture immediately after the priority_list_path fixture (line 197). Add all 5 test functions in a new section headed "# _write_html_dashboard tests (OUT-05)".

    For test_html_dashboard_escape_script_tag: build a one-row DataFrame with cfg.COL_WHATSAPP_MESSAGE = "</script><script>alert(1)</script>", call _write_html_dashboard, read the HTML, find the index of "const studentsData", extract the substring from there to the next semicolon, assert "</script>" does not appear in that substring.
  </action>
  <verify>
    <automated>py -3.12 -m pytest tests/test_output_generator.py -k "html_dashboard" -x -q</automated>
  </verify>
  <done>
    - _write_html_dashboard() exists in src/output_generator.py with correct signature, lazy Jinja2 load, json.dumps().replace("</", "<\\/"), path.write_text(encoding="utf-8"), logger.info, return path
    - All 5 html_dashboard tests pass (py -3.12 -m pytest tests/test_output_generator.py -k "html_dashboard" exits 0)
    - No print() statements added; all column references use cfg.COL_*
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python data -> HTML script tag | Student data serialized to JSON and embedded in browser-executable JS |
| Template file -> rendered HTML | Jinja2 template on disk rendered with autoescape=False |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | json.dumps in _write_html_dashboard | mitigate | json.dumps().replace("</", "<\\/") prevents script-tag injection per CLAUDE.md pitfall — enforced by test_html_dashboard_escape_script_tag |
| T-05-02 | Information Disclosure | facilitator_dashboard.html (contains all student data) | accept | File is written to local outputs/ dir; distribution is facilitator's responsibility; no PII beyond what's already in Excel outputs |
| T-05-03 | Tampering | dashboard.html.j2 template file | accept | Template is read-only at generation time; no user input flows into template resolution |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added in this plan — jinja2 already in requirements.txt |
</threat_model>

<verification>
After both tasks complete:
- py -3.12 -m pytest tests/test_output_generator.py -k "html_dashboard" -x -q exits 0 (all 5 HTML tests pass)
- py -3.12 -m pytest tests/ -x -q exits 0 (all 99 existing tests still pass, nothing regressed)
- python -c "from src import config as cfg; assert len(cfg.DISPLAY_COLS_DASHBOARD) == 12" exits 0
- Manually open outputs/facilitator_dashboard.html (generated by python main.py) in a browser: table renders, campus filter works, name search works, row expands on click
</verification>

<success_criteria>
- facilitator_dashboard.html can be opened via file:// with no server and no network requests
- The JS const studentsData is populated with all student records from the DataFrame
- Campus filter, risk filter buttons, name search, row expand, copy button all functional
- test_html_dashboard_escape_script_tag confirms </script> injection is prevented
- All prior 99 tests remain green
</success_criteria>

<output>
Create .planning/phases/05-html-dashboard-word-report/05-01-SUMMARY.md when done
</output>
