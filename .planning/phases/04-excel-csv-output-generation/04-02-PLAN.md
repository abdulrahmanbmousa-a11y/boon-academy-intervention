---
phase: 04-excel-csv-output-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/output_generator.py
  - tests/test_output_generator.py
autonomous: true
requirements:
  - OUT-01
  - OUT-02
must_haves:
  truths:
    - "intervention_priority_list.xlsx has all students sorted by risk_score desc with color-coded rows"
    - "Priority list header row is navy background with white bold text, frozen at A2"
    - "Each campus gets its own facilitator_dashboard_{campus_id}.xlsx with 15 columns"
    - "Campus dashboard row 2 is a summary row (total students, CRITICAL count, HIGH count, coverage %)"
    - "Data rows start at row 3 in campus dashboards (row 1=header, row 2=summary)"
    - "MEDIUM/LOW students in campus dashboards have empty cells in the 3 LLM columns"
    - "All column widths are auto-calculated (capped at 60, +2 padding)"
  artifacts:
    - path: "src/output_generator.py"
      provides: "_write_priority_list and _write_campus_dashboards private helpers"
      exports: ["_write_priority_list", "_write_campus_dashboards"]
    - path: "tests/test_output_generator.py"
      provides: "Excel format assertions (colors, freeze, auto-width, summary row, LLM empty cells)"
  key_links:
    - from: "src/output_generator.py"
      to: "src/config.py"
      via: "cfg.COLOR_CRITICAL, cfg.COLOR_HEADER, cfg.FONT_WHITE, cfg.OUTPUT_COLS_PRIORITY, cfg.OUTPUT_COLS_CAMPUS"
      pattern: "cfg\\.COLOR_|cfg\\.FONT_|cfg\\.OUTPUT_COLS_"
    - from: "tests/test_output_generator.py"
      to: "disk (tmp_path)"
      via: "save workbook → reload → assert fgColor.rgb"
      pattern: "load_workbook|fgColor\\.rgb"
---

<objective>
Implement the two openpyxl-based private helpers: _write_priority_list (all-student
ranked Excel) and _write_campus_dashboards (per-campus Excel with summary row and
LLM columns). Add Excel-specific unit tests that save to disk, reload, and assert
on cell properties.

Purpose: Deliver the core Excel output that facilitators open daily. Row colors, frozen
panes, auto-widths, and the campus summary row are the primary value of Phase 4.

Output: Two new private helpers in output_generator.py; expanded test_output_generator.py
with openpyxl round-trip tests for colors, freeze_panes, auto-width, summary row, and
MEDIUM/LOW empty LLM cells.
</objective>

<execution_context>
@C:/Users/abdul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-excel-csv-output-generation/04-CONTEXT.md
@.planning/phases/04-excel-csv-output-generation/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Constants added in Plan 04-01 — executor must import from cfg. -->
<!-- After 04-01 completes, src/config.py contains ALL of these. -->

From src/config.py (added by Plan 04-01):
  COLOR_CRITICAL: str = "FFFFCCCC"
  COLOR_HIGH: str = "FFFFE5CC"
  COLOR_MEDIUM: str = "FFFFFFCC"
  COLOR_LOW: str = "FFCCFFCC"
  COLOR_HEADER: str = "FF1F4E79"
  FONT_WHITE: str = "FFFFFFFF"
  COL_RANK: str = "rank"
  OUTPUT_COLS_PRIORITY: list[str]  # 12 elements — rank through recommended_action
  OUTPUT_COLS_CAMPUS: list[str]    # 15 elements — OUTPUT_COLS_PRIORITY + 3 LLM cols

From tests/test_output_generator.py (added by Plan 04-01):
  fixture sample_df  — 4-row DataFrame (CRITICAL/HIGH/MEDIUM/LOW), all columns present
  fixture sample_run_log — dict matching main.py run_log schema

openpyxl PatternFill invariant (CLAUDE.md pitfall — ALWAYS include fill_type="solid"):
  PatternFill(fill_type="solid", fgColor=cfg.COLOR_CRITICAL)  # NOT PatternFill(fgColor=...)

openpyxl test assertion invariant (CLAUDE.md pitfall — 8-char hex with alpha prefix):
  assert cell.fill.fgColor.rgb == cfg.COLOR_CRITICAL   # "FFFFCCCC" not "FFCCCC"
  NOTE: fgColor.rgb only returns correct value AFTER saving to disk and reloading.
  Do NOT assert on an in-memory workbook — always: wb.save(path) then load_workbook(path).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _write_priority_list with full openpyxl formatting</name>
  <files>src/output_generator.py, tests/test_output_generator.py</files>

  <read_first>
    - src/output_generator.py — read in full (has _write_whatsapp_csv and _write_run_log from 04-01)
    - src/config.py — read in full for all COLOR_*, OUTPUT_COLS_PRIORITY, COL_* constants
    - tests/test_output_generator.py — read in full (has sample_df fixture from 04-01)
    - .planning/phases/04-excel-csv-output-generation/04-CONTEXT.md — D-07, D-08, research findings
    - .planning/phases/04-excel-csv-output-generation/04-RESEARCH.md — auto-width pattern, summary row layout
  </read_first>

  <behavior>
    _write_priority_list tests (all require save-to-disk + reload via load_workbook):
      - test_priority_list_file_exists: calling the helper writes a .xlsx file at
        output_dir / "intervention_priority_list.xlsx"
      - test_priority_list_header_color: ws["A1"].fill.fgColor.rgb == cfg.COLOR_HEADER ("FF1F4E79")
      - test_priority_list_header_font: ws["A1"].font.bold is True AND ws["A1"].font.color.rgb == cfg.FONT_WHITE
      - test_priority_list_freeze_panes: ws.freeze_panes == "A2" after reload
      - test_priority_list_critical_row_color: first data row (A2) where risk_level==CRITICAL has
        fill.fgColor.rgb == cfg.COLOR_CRITICAL ("FFFFCCCC")
      - test_priority_list_sorted_desc: row 2 has higher risk_score than row 3 (sorted desc)
      - test_priority_list_rank_column: ws["A2"].value == 1 (rank starts at 1)
      - test_priority_list_column_count: ws.max_column == 12 (OUTPUT_COLS_PRIORITY length)
  </behavior>

  <action>
  Add `_write_priority_list(df: pd.DataFrame, output_dir: Path) -> Path` to
  output_generator.py. Add these imports if not already present:
    from openpyxl import Workbook
    from openpyxl.styles import PatternFill, Font
    from openpyxl.utils import get_column_letter

  Implementation steps in order:

  1. df_copy = df.copy()

  2. Sort and rank:
       df_sorted = df_copy.sort_values(cfg.COL_RISK_SCORE, ascending=False).reset_index(drop=True)
       df_sorted[cfg.COL_RANK] = df_sorted.index + 1

  3. Select output columns:
       df_out = df_sorted[cfg.OUTPUT_COLS_PRIORITY]

  4. Create workbook and write header row (row 1):
       wb = Workbook()
       ws = wb.active
       ws.title = "Intervention Priority List"
       header_fill = PatternFill(fill_type="solid", fgColor=cfg.COLOR_HEADER)
       header_font = Font(bold=True, color=cfg.FONT_WHITE)
       for col_idx, col_name in enumerate(cfg.OUTPUT_COLS_PRIORITY, start=1):
           cell = ws.cell(row=1, column=col_idx, value=col_name)
           cell.fill = header_fill
           cell.font = header_font

  5. Write data rows starting at row 2. Build fill map once before the loop:
       fill_map = {
           "CRITICAL": PatternFill(fill_type="solid", fgColor=cfg.COLOR_CRITICAL),
           "HIGH":     PatternFill(fill_type="solid", fgColor=cfg.COLOR_HIGH),
           "MEDIUM":   PatternFill(fill_type="solid", fgColor=cfg.COLOR_MEDIUM),
           "LOW":      PatternFill(fill_type="solid", fgColor=cfg.COLOR_LOW),
       }
       for row_idx, (_, row) in enumerate(df_out.iterrows(), start=2):
           risk_level = row[cfg.COL_RISK_LEVEL]
           row_fill = fill_map.get(risk_level)
           for col_idx, col_name in enumerate(cfg.OUTPUT_COLS_PRIORITY, start=1):
               cell = ws.cell(row=row_idx, column=col_idx, value=row[col_name])
               if row_fill:
                   cell.fill = row_fill

  6. Auto column widths (per research findings — is not None guard is mandatory):
       for col_cells in ws.columns:
           max_len = max(
               (len(str(c.value)) for c in col_cells if c.value is not None),
               default=10,
           )
           ws.column_dimensions[get_column_letter(col_cells[0].column)].width = min(max_len, 60) + 2

  7. Freeze panes and save:
       ws.freeze_panes = "A2"
       path = output_dir / "intervention_priority_list.xlsx"
       wb.save(path)
       logger.info("Wrote priority list: %s (%d rows)", path, len(df_out))
       return path

  In tests/test_output_generator.py, add a `@pytest.fixture` named `priority_list_path`
  that calls `_write_priority_list(sample_df, tmp_path)` and returns the path.
  Implement all 8 test functions from the behavior block. Each test must:
    1. Call load_workbook(priority_list_path) — NOT assert on in-memory wb
    2. Assert on ws cell properties after reload

  Import at top of test file: from openpyxl import load_workbook
  Import: from src.output_generator import _write_priority_list
  </action>

  <verify>
    <automated>py -3.12 -m pytest tests/test_output_generator.py -v -k "priority_list" 2>&amp;&amp; py -3.12 -c "from src.output_generator import _write_priority_list; print('import OK')"</automated>
  </verify>

  <acceptance_criteria>
    - _write_priority_list returns a Path to a .xlsx file that opens without error
    - ws["A1"].fill.fgColor.rgb == "FF1F4E79" (navy header) after save+reload
    - ws["A1"].font.bold is True and font.color.rgb == "FFFFFFFF"
    - ws.freeze_panes == "A2" after save+reload
    - CRITICAL student row has fgColor.rgb == "FFFFCCCC" after save+reload
    - ws.max_column == 12 (exactly OUTPUT_COLS_PRIORITY columns)
    - ws["A2"].value == 1 (rank column, first data row)
    - All 8 test_priority_list_* functions pass under py -3.12 -m pytest -v
    - Zero bare string literals for column names in output_generator.py (all cfg.COL_*)
    - PatternFill always called with fill_type="solid"
  </acceptance_criteria>

  <done>_write_priority_list produces a correctly formatted Excel file; 8 openpyxl round-trip assertions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _write_campus_dashboards with summary row and LLM columns</name>
  <files>src/output_generator.py, tests/test_output_generator.py</files>

  <read_first>
    - src/output_generator.py — read in full (has _write_priority_list from Task 1)
    - src/config.py — read in full for OUTPUT_COLS_CAMPUS and all COL_* constants
    - tests/test_output_generator.py — read in full (has existing fixtures and tests)
    - .planning/phases/04-excel-csv-output-generation/04-CONTEXT.md — D-05, D-06 (empty cells for MEDIUM/LOW)
    - .planning/phases/04-excel-csv-output-generation/04-RESEARCH.md — summary row layout (row 1=header, row 2=summary, data starts row 3)
  </read_first>

  <behavior>
    _write_campus_dashboards tests (all save-to-disk + reload required):
      - test_campus_dashboard_files_created: returns a dict; for a df with 2 campuses
        (ALPHA, BETA), dict has keys "campus_ALPHA" and "campus_BETA" and both files exist
      - test_campus_dashboard_header_row: ws["A1"].fill.fgColor.rgb == cfg.COLOR_HEADER
        and ws["A1"].font.bold is True (same header styling as priority list)
      - test_campus_dashboard_freeze_panes: ws.freeze_panes == "A2"
      - test_campus_dashboard_column_count: ws.max_column == 15 (OUTPUT_COLS_CAMPUS)
      - test_campus_dashboard_summary_row: ws["A2"] contains "Summary" text (row 2 is the
        summary stats row — bold, light grey fill)
      - test_campus_dashboard_data_starts_row3: ws["A3"] contains the first student's rank (1)
      - test_campus_dashboard_critical_row_color: the first CRITICAL student data row
        (row 3 or later) has fill.fgColor.rgb == cfg.COLOR_CRITICAL
      - test_campus_dashboard_medium_llm_cells_empty: for a MEDIUM student row, the
        cells in columns 13, 14, 15 (facilitator_summary, whatsapp_message, generated_by)
        have value None (empty — per D-06, NOT "N/A")
      - test_campus_dashboard_excludes_nan_campus: if df has a row with campus_id=NaN,
        no "facilitator_dashboard_nan.xlsx" file is created (dropna=True in groupby)
  </behavior>

  <action>
  Add `_write_campus_dashboards(df: pd.DataFrame, output_dir: Path) -> dict[str, Path]`
  to output_generator.py.

  Implementation steps in order:

  1. df_copy = df.copy()
     results: dict[str, Path] = {}

  2. Group by campus, excluding NaN campus_ids:
       for campus_id, campus_df in df_copy.groupby(cfg.COL_CAMPUS_ID, dropna=True):
           campus_df = campus_df.sort_values(cfg.COL_RISK_SCORE, ascending=False).reset_index(drop=True)
           campus_df[cfg.COL_RANK] = campus_df.index + 1

  3. Inside the loop — compute summary stats:
       total = len(campus_df)
       critical_count = (campus_df[cfg.COL_RISK_LEVEL] == "CRITICAL").sum()
       high_count = (campus_df[cfg.COL_RISK_LEVEL] == "HIGH").sum()
       coverage_pct = round((critical_count + high_count) / total * 100, 1) if total > 0 else 0.0
       df_out = campus_df[cfg.OUTPUT_COLS_CAMPUS]

  4. Create workbook, write header row (row 1) — identical styling to _write_priority_list:
       wb = Workbook()
       ws = wb.active
       ws.title = str(campus_id)
       header_fill = PatternFill(fill_type="solid", fgColor=cfg.COLOR_HEADER)
       header_font = Font(bold=True, color=cfg.FONT_WHITE)
       for col_idx, col_name in enumerate(cfg.OUTPUT_COLS_CAMPUS, start=1):
           cell = ws.cell(row=1, column=col_idx, value=col_name)
           cell.fill = header_fill
           cell.font = header_font

  5. Write summary row (row 2) — bold, light grey fill (FFEEEEEE):
       summary_fill = PatternFill(fill_type="solid", fgColor="FFEEEEEE")
       summary_font = Font(bold=True)
       summary_values = [
           "Summary", "", "", str(campus_id), "", "", "",
           f"Total: {total}", f"CRITICAL: {critical_count}",
           f"HIGH: {high_count}", f"Coverage: {coverage_pct}%", "", "", "", "",
       ]
       for col_idx, val in enumerate(summary_values, start=1):
           cell = ws.cell(row=2, column=col_idx, value=val)
           cell.fill = summary_fill
           cell.font = summary_font

  6. Write data rows starting at row 3. Build fill_map once before inner loop (same as
     _write_priority_list). For MEDIUM/LOW rows, write None for columns 13-15 (LLM cols):
       fill_map = {
           "CRITICAL": PatternFill(fill_type="solid", fgColor=cfg.COLOR_CRITICAL),
           "HIGH":     PatternFill(fill_type="solid", fgColor=cfg.COLOR_HIGH),
           "MEDIUM":   PatternFill(fill_type="solid", fgColor=cfg.COLOR_MEDIUM),
           "LOW":      PatternFill(fill_type="solid", fgColor=cfg.COLOR_LOW),
       }
       llm_col_names = {cfg.COL_FACILITATOR_SUMMARY, cfg.COL_WHATSAPP_MESSAGE, cfg.COL_GENERATED_BY}
       for row_idx, (_, row) in enumerate(df_out.iterrows(), start=3):
           risk_level = row[cfg.COL_RISK_LEVEL]
           row_fill = fill_map.get(risk_level)
           is_llm_eligible = risk_level in ("CRITICAL", "HIGH")
           for col_idx, col_name in enumerate(cfg.OUTPUT_COLS_CAMPUS, start=1):
               if col_name in llm_col_names and not is_llm_eligible:
                   value = None   # D-06: empty cell, not "N/A"
               else:
                   value = row[col_name]
               cell = ws.cell(row=row_idx, column=col_idx, value=value)
               if row_fill:
                   cell.fill = row_fill

  7. Auto-width (same pattern as _write_priority_list), freeze, save:
       for col_cells in ws.columns:
           max_len = max(
               (len(str(c.value)) for c in col_cells if c.value is not None),
               default=10,
           )
           ws.column_dimensions[get_column_letter(col_cells[0].column)].width = min(max_len, 60) + 2
       ws.freeze_panes = "A2"
       path = output_dir / f"facilitator_dashboard_{campus_id}.xlsx"
       wb.save(path)
       logger.info("Wrote campus dashboard: %s (%d students)", path, total)
       results[f"campus_{campus_id}"] = path

  8. return results

  In tests/test_output_generator.py:
    - Add a `@pytest.fixture` named `multi_campus_df` that returns a DataFrame with
      students from 2 campuses (ALPHA: 1 CRITICAL + 1 MEDIUM; BETA: 1 HIGH + 1 LOW).
      All 15 OUTPUT_COLS_CAMPUS columns must be present. MEDIUM and LOW rows have
      None/NaN in facilitator_summary, whatsapp_message, generated_by (per D-06).
      Include one row with campus_id=float("nan") to test dropna=True behavior.
    - Add a `@pytest.fixture` named `campus_dashboard_paths` that calls
      _write_campus_dashboards(multi_campus_df, tmp_path) and returns the result dict.
    - Import: from src.output_generator import _write_campus_dashboards
    - Implement all 9 test functions from the behavior block. Each test reloads the
      workbook from disk via load_workbook before asserting.
  </action>

  <verify>
    <automated>py -3.12 -m pytest tests/test_output_generator.py -v -k "campus_dashboard" 2>&amp;&amp; py -3.12 -c "from src.output_generator import _write_campus_dashboards; print('import OK')"</automated>
  </verify>

  <acceptance_criteria>
    - _write_campus_dashboards returns dict with key pattern "campus_{id}" per campus
    - One .xlsx file created per unique non-NaN campus_id (no facilitator_dashboard_nan.xlsx)
    - ws["A1"].fill.fgColor.rgb == "FF1F4E79" and ws["A1"].font.bold is True (after reload)
    - ws.freeze_panes == "A2" after reload
    - ws.max_column == 15 (OUTPUT_COLS_CAMPUS)
    - ws["A2"].value == "Summary" (summary row in row 2)
    - ws["A3"].value == 1 (first student's rank; data rows start at row 3)
    - CRITICAL student row (row 3+) has fgColor.rgb == "FFFFCCCC" after reload
    - MEDIUM/LOW rows: columns 13, 14, 15 have cell.value is None (not "N/A")
    - All 9 test_campus_dashboard_* functions pass under py -3.12 -m pytest -v
    - dropna=True in groupby (verified by test_campus_dashboard_excludes_nan_campus)
    - PatternFill always called with fill_type="solid"
    - All column references use cfg.COL_* or cfg.OUTPUT_COLS_CAMPUS — no bare strings
  </acceptance_criteria>

  <done>_write_campus_dashboards produces correctly formatted per-campus Excel files; 9 openpyxl round-trip assertions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataFrame → openpyxl Workbook → disk | Internal data only; campus_id from ingestion, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | facilitator_dashboard_{campus}.xlsx | accept | Contains parent_phone by design; file paths only in logger.info |
| T-04-04 | Tampering | campus_id as filename component | mitigate | campus_id comes from ingestion (validated CSV field); dropna=True prevents NaN filename; no path traversal possible from internal enum values |
| T-04-SC | Tampering | pip installs | accept | No new packages in this plan; openpyxl already in requirements.txt |
</threat_model>

<verification>
After both tasks complete:
  py -3.12 -m pytest tests/test_output_generator.py -v
  py -3.12 -c "from src.output_generator import _write_priority_list, _write_campus_dashboards; print('OK')"

All 65 pre-existing tests must still pass:
  py -3.12 -m pytest tests/ -v --tb=short
</verification>

<success_criteria>
- _write_priority_list: sorted by risk_score desc, rank column, navy header, correct row colors, frozen A2, 12 columns, auto-width
- _write_campus_dashboards: per-campus files, summary row at row 2, data at row 3, 15 columns, MEDIUM/LOW LLM cells empty, no NaN file
- 8 priority list assertions pass; 9 campus dashboard assertions pass
- All 65 pre-existing tests still GREEN
- Zero bare string literals for column names in output_generator.py
</success_criteria>

<output>
Create `.planning/phases/04-excel-csv-output-generation/04-02-SUMMARY.md` when done.
</output>
