---
phase: 04-excel-csv-output-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/output_generator.py
  - tests/test_config.py
  - tests/test_output_generator.py
autonomous: true
requirements:
  - OUT-03
  - OUT-06
must_haves:
  truths:
    - "whatsapp_messages.csv exists with correct 8-column schema and only CRITICAL/HIGH rows"
    - "run_log.json exists and contains all 7 required top-level keys"
    - "COLOR_* constants exist in config.py using 8-char openpyxl hex format"
    - "OUTPUT_COLS_PRIORITY has exactly 12 elements; OUTPUT_COLS_CAMPUS has exactly 15"
    - "All new config constants are importable and used via cfg.* in output_generator.py"
  artifacts:
    - path: "src/config.py"
      provides: "COLOR_CRITICAL, COLOR_HIGH, COLOR_MEDIUM, COLOR_LOW, COLOR_HEADER, FONT_WHITE, COL_RANK, OUTPUT_COLS_PRIORITY (12), OUTPUT_COLS_CAMPUS (15)"
    - path: "src/output_generator.py"
      provides: "_write_whatsapp_csv and _write_run_log private helpers"
    - path: "tests/test_config.py"
      provides: "assertions that OUTPUT_COLS_PRIORITY is len 12, OUTPUT_COLS_CAMPUS is len 15"
    - path: "tests/test_output_generator.py"
      provides: "unit tests for _write_whatsapp_csv and _write_run_log"
  key_links:
    - from: "src/output_generator.py"
      to: "src/config.py"
      via: "cfg.COLOR_CRITICAL, cfg.OUTPUT_COLS_PRIORITY, cfg.COL_WHATSAPP_MESSAGE, etc."
      pattern: "cfg\\.COL_|cfg\\.COLOR_|cfg\\.OUTPUT_COLS_"
    - from: "tests/test_output_generator.py"
      to: "src/output_generator.py"
      via: "_write_whatsapp_csv, _write_run_log direct calls"
      pattern: "_write_whatsapp_csv|_write_run_log"
---

<objective>
Add Phase 4 formatting constants to src/config.py and implement the two lightweight
private helpers (_write_whatsapp_csv and _write_run_log) in output_generator.py.

Purpose: Establish the constants foundation that Plans 04-02 and 04-03 depend on,
and deliver the two non-openpyxl output writers (CSV and JSON) with full unit test coverage.

Output: Updated config.py with 9 new constants, two new private helpers in
output_generator.py, and unit tests for both helpers.
</objective>

<execution_context>
@C:/Users/abdul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-excel-csv-output-generation/04-CONTEXT.md
</context>

<interfaces>
<!-- Key constants already in src/config.py that D-10 builds on. -->
<!-- Executor: read src/config.py in full before editing. -->

From src/config.py (existing — Phase 4 appends to these):
  COL_STUDENT_ID: str = "student_id"
  COL_STUDENT_NAME: str = "student_name"
  COL_CAMPUS_ID: str = "campus_id"
  COL_PARENT_PHONE: str = "parent_phone"
  COL_FACILITATOR_EMAIL: str = "facilitator_email"
  COL_ATTENDANCE_RATE: str = "attendance_rate"
  COL_AVG_PRACTICE: str = "avg_practice_questions"
  COL_TREND_DIR: str = "trend_direction"
  COL_DAYS_SINCE_NOTE: str = "days_since_last_note"
  COL_RISK_SCORE: str = "risk_score"
  COL_RISK_LEVEL: str = "risk_level"
  COL_RECOMMENDED_ACTION: str = "recommended_action"
  COL_FACILITATOR_SUMMARY: str = "facilitator_summary"
  COL_WHATSAPP_MESSAGE: str = "whatsapp_message"
  COL_GENERATED_BY: str = "generated_by"
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 4 constants to src/config.py and extend test_config.py</name>
  <files>src/config.py, tests/test_config.py</files>

  <read_first>
    - src/config.py — read in full; append new constants after line 99 (after COL_LLM_ERROR_REASON)
    - tests/test_config.py — read in full; add assertions after existing tests
    - .planning/phases/04-excel-csv-output-generation/04-CONTEXT.md — D-10 exact values
  </read_first>

  <behavior>
    - test_color_constants_are_8char_hex: assert each COLOR_* value has len == 8 and starts with "FF"
    - test_output_cols_priority_length: assert len(cfg.OUTPUT_COLS_PRIORITY) == 12
    - test_output_cols_campus_length: assert len(cfg.OUTPUT_COLS_CAMPUS) == 15
    - test_output_cols_campus_is_superset: assert all c in cfg.OUTPUT_COLS_CAMPUS for c in cfg.OUTPUT_COLS_PRIORITY
    - test_col_rank_constant_value: assert cfg.COL_RANK == "rank"
    - test_font_white_value: assert cfg.FONT_WHITE == "FFFFFFFF"
    - test_color_header_value: assert cfg.COLOR_HEADER == "FF1F4E79"
  </behavior>

  <action>
  Append the following block to src/config.py immediately after the COL_LLM_ERROR_REASON line.
  Do NOT modify any existing constant. Use exactly these names and values (per D-10):

    # ---------------------------------------------------------------------------
    # Phase 4 output formatting constants (D-10)
    # ---------------------------------------------------------------------------
    COLOR_CRITICAL: str = "FFFFCCCC"   # light red  (8-char openpyxl ARGB, fill_type="solid")
    COLOR_HIGH: str = "FFFFE5CC"       # light orange
    COLOR_MEDIUM: str = "FFFFFFCC"     # light yellow
    COLOR_LOW: str = "FFCCFFCC"        # light green
    COLOR_HEADER: str = "FF1F4E79"     # dark navy (header row background)
    FONT_WHITE: str = "FFFFFFFF"       # white (header row text color)

    # Rank column (derived in _write_priority_list, not from risk_engine)
    COL_RANK: str = "rank"

    # OUT-01 column order — 12 columns, exact sequence for intervention_priority_list.xlsx
    OUTPUT_COLS_PRIORITY: list[str] = [
        COL_RANK, COL_STUDENT_ID, COL_STUDENT_NAME, COL_CAMPUS_ID,
        COL_FACILITATOR_EMAIL, COL_RISK_SCORE, COL_RISK_LEVEL,
        COL_ATTENDANCE_RATE, COL_AVG_PRACTICE, COL_TREND_DIR,
        COL_DAYS_SINCE_NOTE, COL_RECOMMENDED_ACTION,
    ]

    # OUT-02 campus dashboard columns — standard 12 + 3 LLM columns = 15 total (D-05)
    OUTPUT_COLS_CAMPUS: list[str] = OUTPUT_COLS_PRIORITY + [
        COL_FACILITATOR_SUMMARY, COL_WHATSAPP_MESSAGE, COL_GENERATED_BY,
    ]

  In tests/test_config.py, add a new test class or extend the existing module with
  the 7 assertions described in the behavior block above. Import cfg at top if not
  already imported. Each assertion is a standalone test function prefixed test_.
  </action>

  <verify>
    <automated>py -3.12 -m pytest tests/test_config.py -v -k "color or cols or rank or font" 2>&amp;&amp; py -3.12 -c "from src import config as cfg; assert len(cfg.OUTPUT_COLS_PRIORITY)==12; assert len(cfg.OUTPUT_COLS_CAMPUS)==15; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - src/config.py contains COLOR_CRITICAL = "FFFFCCCC" (exact value, 8-char)
    - src/config.py contains COL_RANK = "rank"
    - len(cfg.OUTPUT_COLS_PRIORITY) == 12 (importable, not just text)
    - len(cfg.OUTPUT_COLS_CAMPUS) == 15 (importable, not just text)
    - cfg.OUTPUT_COLS_CAMPUS[12] == cfg.COL_FACILITATOR_SUMMARY
    - All 7 new test functions pass under py -3.12 -m pytest tests/test_config.py -v
  </acceptance_criteria>

  <done>src/config.py exports all 9 new Phase 4 constants; 7 assertions pass in test_config.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _write_whatsapp_csv and _write_run_log in output_generator.py</name>
  <files>src/output_generator.py, tests/test_output_generator.py</files>

  <read_first>
    - src/output_generator.py — read in full; understand the existing 2-arg stub
    - src/config.py — read in full (after Task 1 runs) for all COL_* and COLOR_* constants
    - .planning/phases/04-excel-csv-output-generation/04-CONTEXT.md — D-04, D-06 exact column names, whatsapp CSV spec
    - .planning/REQUIREMENTS.md — OUT-03 column list, OUT-06 JSON key list
  </read_first>

  <behavior>
    _write_whatsapp_csv:
      - test_whatsapp_csv_only_critical_high: given df with CRITICAL/HIGH/MEDIUM/LOW rows,
        CSV output contains only CRITICAL and HIGH rows (MEDIUM and LOW filtered out)
      - test_whatsapp_csv_columns: CSV has exactly these columns in this order:
        student_id, student_name, parent_phone, facilitator_email, campus_id,
        risk_level, whatsapp_message, generated_by
        (note: OUT-03 spec says "message_text" as conceptual name but the actual
        DataFrame column and CSV column is cfg.COL_WHATSAPP_MESSAGE = "whatsapp_message")
      - test_whatsapp_csv_encoding_bom: file bytes start with UTF-8 BOM (b"\xef\xbb\xbf")
      - test_whatsapp_csv_returns_path: return value is a Path object pointing to the file

    _write_run_log:
      - test_run_log_keys: JSON output has all 7 keys: run_timestamp, students_processed,
        api_calls_made, tokens_used, errors_encountered, fallbacks_triggered, data_quality_warnings
      - test_run_log_returns_path: return value is a Path pointing to the written file
      - test_run_log_default_str_handles_non_serializable: run_log containing a datetime
        object serializes without raising TypeError (default=str covers it)
  </behavior>

  <action>
  Replace the current output_generator.py content with an updated module. Keep the
  module docstring. Add these imports at the top (after existing imports):

    import json
    import pandas as pd
    from pathlib import Path
    from src import config as cfg

  Keep: `logger = logging.getLogger(__name__)` — no print statements anywhere.

  Implement `_write_whatsapp_csv(df: pd.DataFrame, output_dir: Path) -> Path`:
    - Docstring: "Write whatsapp_messages.csv for all CRITICAL and HIGH risk students."
    - df_copy = df.copy()
    - Filter: mask = df_copy[cfg.COL_RISK_LEVEL].isin(["CRITICAL", "HIGH"])
    - Select columns in this exact order:
        cols = [cfg.COL_STUDENT_ID, cfg.COL_STUDENT_NAME, cfg.COL_PARENT_PHONE,
                cfg.COL_FACILITATOR_EMAIL, cfg.COL_CAMPUS_ID, cfg.COL_RISK_LEVEL,
                cfg.COL_WHATSAPP_MESSAGE, cfg.COL_GENERATED_BY]
    - path = output_dir / "whatsapp_messages.csv"
    - df_copy[mask][cols].to_csv(path, index=False, encoding="utf-8-sig")
    - logger.info("Wrote whatsapp CSV: %s (%d rows)", path, mask.sum())
    - return path

  Implement `_write_run_log(run_log: dict, output_dir: Path) -> Path`:
    - Docstring: "Write run_log.json from the in-memory run_log dict (D-06)."
    - path = output_dir / "run_log.json"
    - path.write_text(json.dumps(run_log, indent=2, default=str), encoding="utf-8")
    - logger.info("Wrote run log: %s", path)
    - return path

  Keep the existing `write_outputs` stub unchanged (still raises NotImplementedError("Phase 4")).
  Plan 04-03 replaces the stub — do not touch it in this plan.

  In tests/test_output_generator.py, create the file (it may not exist yet) with:
    - A module docstring
    - imports: pytest, json, pandas as pd, pathlib.Path, from src import config as cfg,
      from src.output_generator import _write_whatsapp_csv, _write_run_log
    - A `@pytest.fixture` named `sample_df` that returns a minimal DataFrame with
      these columns: student_id, student_name, parent_phone, facilitator_email,
      campus_id, risk_level, whatsapp_message, generated_by — with 4 rows:
      one CRITICAL, one HIGH, one MEDIUM, one LOW.
    - A `@pytest.fixture` named `sample_run_log` matching main.py schema:
        {"run_timestamp": "2026-01-01T00:00:00+00:00", "students_processed": 10,
         "api_calls_made": 2, "tokens_used": {"input": 100, "output": 50},
         "errors_encountered": [], "fallbacks_triggered": 0, "data_quality_warnings": []}
    - Implement all 7 test functions from the behavior block using tmp_path fixture.
  </action>

  <verify>
    <automated>py -3.12 -m pytest tests/test_output_generator.py -v -k "whatsapp or run_log" 2>&amp;&amp; py -3.12 -c "from src.output_generator import _write_whatsapp_csv, _write_run_log; print('imports OK')"</automated>
  </verify>

  <acceptance_criteria>
    - _write_whatsapp_csv filters to CRITICAL+HIGH rows only (MEDIUM/LOW excluded)
    - CSV column order matches: student_id, student_name, parent_phone, facilitator_email, campus_id, risk_level, whatsapp_message, generated_by
    - File bytes start with b"\xef\xbb\xbf" (UTF-8 BOM, encoding="utf-8-sig")
    - _write_run_log writes valid JSON with all 7 required keys
    - json.loads(path.read_text()) works without error on _write_run_log output
    - default=str handles datetime objects without TypeError
    - All 7 test functions pass under py -3.12 -m pytest tests/test_output_generator.py -v
    - Zero print statements in output_generator.py (only logging.getLogger calls)
    - All column references in output_generator.py use cfg.COL_* — no bare string literals
  </acceptance_criteria>

  <done>_write_whatsapp_csv and _write_run_log are implemented, tested (7 assertions), and import cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataFrame → CSV | Internal pipeline data written to disk; no user input in this path |
| run_log dict → JSON | Internal metrics only; must not contain ANTHROPIC_API_KEY |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | whatsapp_messages.csv | accept | File contains parent_phone by design (facilitator use); paths only in logger.info, not values |
| T-04-02 | Information Disclosure | run_log.json | mitigate | Verify run_log dict in main.py does not include ANTHROPIC_API_KEY before write; keys are fixed schema |
| T-04-SC | Tampering | pip installs | accept | No new packages installed in this plan; openpyxl already in requirements.txt |
</threat_model>

<verification>
After both tasks complete:
  py -3.12 -m pytest tests/test_config.py tests/test_output_generator.py -v
  py -3.12 -c "from src import config as cfg; assert len(cfg.OUTPUT_COLS_PRIORITY)==12; assert len(cfg.OUTPUT_COLS_CAMPUS)==15; print('constants OK')"
  py -3.12 -c "from src.output_generator import _write_whatsapp_csv, _write_run_log; print('helpers importable')"

All 65 existing tests must still pass:
  py -3.12 -m pytest tests/ -v --tb=short
</verification>

<success_criteria>
- src/config.py has 9 new Phase 4 constants (6 COLOR_*, COL_RANK, OUTPUT_COLS_PRIORITY, OUTPUT_COLS_CAMPUS)
- OUTPUT_COLS_PRIORITY has 12 elements; OUTPUT_COLS_CAMPUS has 15 elements
- _write_whatsapp_csv writes UTF-8 BOM CSV with 8 correct columns, CRITICAL/HIGH rows only
- _write_run_log writes valid JSON with all 7 run_log keys, default=str for non-serializable values
- 7 new test_config assertions pass; 7 new test_output_generator assertions pass
- All 65 pre-existing tests still GREEN
</success_criteria>

<output>
Create `.planning/phases/04-excel-csv-output-generation/04-01-SUMMARY.md` when done.
</output>
