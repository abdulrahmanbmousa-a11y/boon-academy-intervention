---
phase: 04-excel-csv-output-generation
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/output_generator.py
  - main.py
  - .planning/STATE.md
  - tests/test_output_generator.py
autonomous: true
requirements:
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-06
must_haves:
  truths:
    - "write_outputs(df, output_dir, run_log) orchestrates all 4 helpers and returns a unified dict"
    - "Return dict contains keys: priority_list, campus_{id} per campus, whatsapp, run_log"
    - "output_dir.mkdir(parents=True, exist_ok=True) called at entry — idempotent"
    - "main.py line ~87 is uncommented and calls write_outputs with 3 positional args"
    - "python main.py runs end-to-end without error (pipeline complete)"
    - "STATE.md module contract updated from 2-arg to 3-arg signature"
    - "Integration test: all 4 path keys exist in return dict after write_outputs call"
  artifacts:
    - path: "src/output_generator.py"
      provides: "write_outputs orchestrator — 3-arg signature, calls all 4 private helpers"
      exports: ["write_outputs"]
    - path: "main.py"
      provides: "Phase 4 wiring — uncommented write_outputs call with run_log positional arg"
    - path: ".planning/STATE.md"
      provides: "Updated module contract: write_outputs(df, output_dir, run_log) -> dict[str, Path]"
    - path: "tests/test_output_generator.py"
      provides: "Integration test: full write_outputs() call, all 4 path keys asserted"
  key_links:
    - from: "main.py"
      to: "src/output_generator.write_outputs"
      via: "output_generator.write_outputs(df, cfg.OUTPUT_DIR, run_log)"
      pattern: "write_outputs\\(df, cfg\\.OUTPUT_DIR, run_log\\)"
    - from: "src/output_generator.write_outputs"
      to: "_write_priority_list, _write_campus_dashboards, _write_whatsapp_csv, _write_run_log"
      via: "calls all 4 private helpers, merges results"
      pattern: "_write_priority_list|_write_campus_dashboards|_write_whatsapp_csv|_write_run_log"
---

<objective>
Replace the write_outputs stub with the 3-arg orchestrator that calls all 4 private helpers
and returns the unified path dict. Wire it into main.py. Update STATE.md contract. Add an
integration test that exercises the full write_outputs call.

Purpose: Complete Phase 4 by closing the pipeline loop — main.py calls write_outputs,
all 4 output files are generated, and the return dict is logged.

Output: Working write_outputs orchestrator, uncommented main.py wiring, updated STATE.md
module contract, and one integration test proving all 4 path keys exist.
</objective>

<execution_context>
@C:/Users/abdul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-excel-csv-output-generation/04-CONTEXT.md
@.planning/phases/04-excel-csv-output-generation/04-01-SUMMARY.md
@.planning/phases/04-excel-csv-output-generation/04-02-SUMMARY.md
</context>

<interfaces>
<!-- Private helpers now available in output_generator.py after Plans 04-01 and 04-02. -->

From src/output_generator.py (after Plans 04-01 + 04-02):
  _write_priority_list(df: pd.DataFrame, output_dir: Path) -> Path
  _write_campus_dashboards(df: pd.DataFrame, output_dir: Path) -> dict[str, Path]
  _write_whatsapp_csv(df: pd.DataFrame, output_dir: Path) -> Path
  _write_run_log(run_log: dict, output_dir: Path) -> Path

Target signature (D-01):
  write_outputs(df: pd.DataFrame, output_dir: Path, run_log: dict) -> dict[str, Path]

main.py wiring target (line ~87, D-01):
  paths = output_generator.write_outputs(df, cfg.OUTPUT_DIR, run_log)
  logger.info("Outputs written: %s", list(paths.keys()))

STATE.md module contract to update (line ~138):
  OLD: output_generator.write_outputs(df, output_dir) -> dict[str, Path] — idempotent
  NEW: output_generator.write_outputs(df, output_dir, run_log) -> dict[str, Path] — idempotent
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement write_outputs orchestrator and update STATE.md</name>
  <files>src/output_generator.py, .planning/STATE.md</files>

  <read_first>
    - src/output_generator.py — read in full (has all 4 private helpers; stub still raises NotImplementedError)
    - .planning/STATE.md — read in full; find "output_generator.write_outputs" in Module contracts section
    - .planning/phases/04-excel-csv-output-generation/04-CONTEXT.md — D-01 (signature), D-02 (return dict), D-03 (mkdir)
  </read_first>

  <behavior>
    Integration test (write_outputs full call):
      - test_write_outputs_returns_all_keys: calling write_outputs(sample_df_full, tmp_path, sample_run_log)
        returns a dict containing the key "priority_list", at least one key starting with "campus_",
        the key "whatsapp", and the key "run_log"
      - test_write_outputs_all_paths_exist: every Path value in the returned dict points to a file
        that exists on disk (path.exists() is True for each value)
      - test_write_outputs_creates_output_dir: calling write_outputs with a non-existent subdirectory
        does not raise — output_dir.mkdir(parents=True, exist_ok=True) handles it
  </behavior>

  <action>
  In src/output_generator.py, replace the `write_outputs` stub (the function that currently
  raises NotImplementedError("Phase 4")) with the following implementation. Do NOT remove or
  alter any of the 4 private helpers above it.

  New write_outputs implementation:

    def write_outputs(
        df: pd.DataFrame,
        output_dir: Path,
        run_log: dict,
    ) -> dict[str, Path]:
        """Write all Phase 4 output files for the intervention pipeline.

        Orchestrates four private helpers — one per output type — and returns a unified
        dict mapping semantic keys to resolved Path objects (D-01, D-02).

        D-03: Creates output_dir (including parents) if it does not exist — idempotent.
        D-04: Delegates to four independently-testable private helpers.
        D-09: Phase 5 will add _write_report() and _write_html_dashboard() here.

        Args:
            df: Fully enriched one-row-per-student DataFrame from enrich_with_llm().
            output_dir: Directory to write all output files into (cfg.OUTPUT_DIR).
            run_log: In-memory run metadata dict built throughout the pipeline run (D-05/D-06).
                     Keys: run_timestamp, students_processed, api_calls_made, tokens_used,
                     errors_encountered, fallbacks_triggered, data_quality_warnings.

        Returns:
            Dict mapping output file keys to their resolved Path objects.
            Keys: "priority_list", "campus_{campus_id}" per campus, "whatsapp", "run_log".
        """
        output_dir.mkdir(parents=True, exist_ok=True)   # D-03

        paths: dict[str, Path] = {}

        priority_path = _write_priority_list(df, output_dir)
        paths["priority_list"] = priority_path

        campus_paths = _write_campus_dashboards(df, output_dir)
        paths.update(campus_paths)

        whatsapp_path = _write_whatsapp_csv(df, output_dir)
        paths["whatsapp"] = whatsapp_path

        run_log_path = _write_run_log(run_log, output_dir)
        paths["run_log"] = run_log_path

        logger.info(
            "All outputs written to %s — keys: %s",
            output_dir,
            list(paths.keys()),
        )
        return paths

  In .planning/STATE.md, find the line containing:
    "output_generator.write_outputs(df, output_dir) -> dict[str, Path]"
  Replace it with:
    "output_generator.write_outputs(df, output_dir, run_log) -> dict[str, Path] — idempotent; run_log is required positional arg (D-01)"
  </action>

  <verify>
    <automated>py -3.12 -c "from src import output_generator; import inspect; sig = inspect.signature(output_generator.write_outputs); params = list(sig.parameters); assert params == ['df', 'output_dir', 'run_log'], params; print('signature OK:', params)"</automated>
  </verify>

  <acceptance_criteria>
    - write_outputs signature is exactly (df: pd.DataFrame, output_dir: Path, run_log: dict) -> dict[str, Path]
    - output_dir.mkdir(parents=True, exist_ok=True) is the first statement in the body
    - Function docstring references D-01, D-02, D-03, D-04, D-09
    - All 4 private helpers are called in order: _write_priority_list, _write_campus_dashboards, _write_whatsapp_csv, _write_run_log
    - Return dict is the union of all helper return values plus "priority_list", "whatsapp", "run_log" keys
    - STATE.md module contracts section shows 3-arg signature for write_outputs
    - py -3.12 -c "from src.output_generator import write_outputs; print('OK')" exits 0
  </acceptance_criteria>

  <done>write_outputs orchestrator replaces the stub; STATE.md contract updated to 3-arg signature.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire main.py, add integration test, verify full pipeline</name>
  <files>main.py, tests/test_output_generator.py</files>

  <read_first>
    - main.py — read in full; find line ~87 with commented write_outputs call
    - tests/test_output_generator.py — read in full (has all existing tests from 04-01 + 04-02)
    - .planning/phases/04-excel-csv-output-generation/04-CONTEXT.md — D-01 main.py wiring exact call
  </read_first>

  <behavior>
    Integration test assertions (verify write_outputs end-to-end):
      - test_write_outputs_returns_all_keys: return dict has keys "priority_list", at least
        one "campus_*" key, "whatsapp", "run_log"
      - test_write_outputs_all_paths_exist: all Path values in the dict point to existing files
      - test_write_outputs_creates_output_dir: non-existent subdirectory is created without error

    main.py smoke (no automated test — verified by pipeline run):
      - write_outputs call is uncommented, uses 3 positional args, return value captured in `paths`
      - logger.info logs list(paths.keys()) after the call
  </behavior>

  <action>
  In main.py, find the comment block around line 87:
    # Phase 4: Output generation — D-06: single write-at-end point
    # write_outputs(df, cfg.OUTPUT_DIR, run_log=run_log)

  Replace those two lines with (add import at top if missing):
    # Phase 4: Output generation — D-06: single write-at-end point
    from src import output_generator
    paths = output_generator.write_outputs(df, cfg.OUTPUT_DIR, run_log)
    logger.info("Outputs written: %s", list(paths.keys()))

  NOTE: Check whether `from src import output_generator` is already imported at the top of
  main.py. If it is, do NOT add a duplicate import — just uncomment the call.
  The call must use POSITIONAL args: write_outputs(df, cfg.OUTPUT_DIR, run_log) — NOT
  the keyword form `run_log=run_log` that is in the comment (D-01 specifies positional).

  In tests/test_output_generator.py, add a new integration test class or section:
    - Add a `@pytest.fixture` named `full_sample_df` that returns a DataFrame with all
      columns present (OUTPUT_COLS_CAMPUS + any additional columns in the enriched df).
      Include 2 campuses (ALPHA and BETA), at least 1 CRITICAL and 1 HIGH student.
      Use the same column dtypes as the real pipeline: student_id as str, parent_phone as str.
    - Implement the 3 integration test functions from the behavior block using tmp_path
      and full_sample_df fixtures.
    - All imports needed: from src.output_generator import write_outputs

  After implementing, run the full pipeline in dry-run to confirm no import errors:
    py -3.12 -c "import main; print('main imports OK')"
  </action>

  <verify>
    <automated>py -3.12 -m pytest tests/test_output_generator.py -v -k "write_outputs" 2>&amp;&amp; py -3.12 -m pytest tests/ -v --tb=short</automated>
  </verify>

  <acceptance_criteria>
    - main.py line ~87 uncommented: `paths = output_generator.write_outputs(df, cfg.OUTPUT_DIR, run_log)`
    - Call uses 3 POSITIONAL args (not keyword arg for run_log)
    - logger.info("Outputs written: %s", list(paths.keys())) present immediately after
    - 3 integration test functions pass: test_write_outputs_returns_all_keys, test_write_outputs_all_paths_exist, test_write_outputs_creates_output_dir
    - py -3.12 -m pytest tests/ -v --tb=short shows ALL tests passing (65 pre-existing + new Phase 4 tests)
    - py -3.12 -c "import main" exits 0 with no ImportError or AttributeError
    - Zero print statements anywhere in output_generator.py
    - Total test count increased from 65 by at least 17 new Phase 4 tests (7+8+9+3 minimum)
  </acceptance_criteria>

  <done>main.py wired, integration test passes, full test suite GREEN. Phase 4 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main.py → write_outputs | All args are internal pipeline values; output_dir from cfg (env var only) |
| run_log dict → JSON | Fixed schema built in main.py; ANTHROPIC_API_KEY must not appear |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | run_log.json | mitigate | run_log schema in main.py has no secrets field; executor must confirm ANTHROPIC_API_KEY is not in the dict before this plan ships |
| T-04-06 | Tampering | output_dir path | accept | output_dir = cfg.OUTPUT_DIR from env var; not user-controlled; mkdir(parents=True, exist_ok=True) is safe |
| T-04-SC | Tampering | pip installs | accept | No new packages; all dependencies already in requirements.txt |
</threat_model>

<verification>
Phase 4 complete when all of these pass:

  1. Full test suite green:
       py -3.12 -m pytest tests/ -v --tb=short

  2. Signature verification:
       py -3.12 -c "
       import inspect
       from src import output_generator as og
       sig = inspect.signature(og.write_outputs)
       params = list(sig.parameters)
       assert params == ['df', 'output_dir', 'run_log'], params
       print('signature OK')
       "

  3. Config constants:
       py -3.12 -c "
       from src import config as cfg
       assert len(cfg.OUTPUT_COLS_PRIORITY) == 12
       assert len(cfg.OUTPUT_COLS_CAMPUS) == 15
       assert cfg.COLOR_CRITICAL == 'FFFFCCCC'
       assert cfg.COLOR_HEADER == 'FF1F4E79'
       print('config OK')
       "

  4. main.py imports clean:
       py -3.12 -c "import main; print('main OK')"

  5. No bare column string literals in output_generator.py:
       py -3.12 -c "
       import ast, pathlib
       src = pathlib.Path('src/output_generator.py').read_text()
       tree = ast.parse(src)
       # Spot-check: 'student_id' must not appear as a bare string constant
       strings = [n.value for n in ast.walk(tree) if isinstance(n, ast.Constant) and isinstance(n.value, str)]
       forbidden = ['student_id', 'risk_level', 'risk_score', 'campus_id', 'parent_phone']
       for f in forbidden:
           assert f not in strings, f'Bare string literal found: {f!r}'
       print('no bare column strings OK')
       "

ROADMAP.md Phase 4 success criteria checklist:
  1. intervention_priority_list.xlsx: all students, ranked desc, color-coded, bold headers, frozen A2
  2. facilitator_dashboard_{campus}.xlsx: campus-only students, summary row, risk_score sorted
  3. whatsapp_messages.csv: 8 columns, CRITICAL/HIGH only, UTF-8 BOM
  4. run_log.json: 7 required keys present
</verification>

<success_criteria>
- write_outputs(df, output_dir, run_log) -> dict[str, Path] fully implemented with D-01..D-04
- main.py wired: paths = output_generator.write_outputs(df, cfg.OUTPUT_DIR, run_log)
- STATE.md module contract updated to 3-arg signature
- All 4 output files generated correctly in integration test
- Full test suite passes (65 pre-existing + minimum 17 new Phase 4 tests)
- Zero print statements, zero bare column string literals in output_generator.py
- Phase 4 complete — all 4 requirements (OUT-01, OUT-02, OUT-03, OUT-06) delivered
</success_criteria>

<output>
Create `.planning/phases/04-excel-csv-output-generation/04-03-SUMMARY.md` when done.
Then update ROADMAP.md Phase 4 plans section:
  **Plans:** 3 plans
  - [x] 04-01-PLAN.md — Config constants + _write_whatsapp_csv + _write_run_log + unit tests
  - [x] 04-02-PLAN.md — _write_priority_list + _write_campus_dashboards + Excel format tests
  - [x] 04-03-PLAN.md — write_outputs orchestrator + main.py wiring + integration test
</output>
