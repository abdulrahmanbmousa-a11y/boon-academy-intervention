---
phase: 8
plan: 08-03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - .planning/phases/08-end-to-end-integration-polish/VERIFICATION.md
autonomous: false
requirements_addressed: []

must_haves:
  truths:
    - "make test (py -3.12 -m pytest tests/ -v) passes with 0 failures and 0 errors"
    - "grep -rn \"print(\" src/ main.py returns zero matches"
    - "grep \"sk-ant-api03\" .env.example returns zero matches"
    - "HTML dashboard opens in Chrome and all interactive features work at 1280px and 1920px"
    - "VERIFICATION.md exists recording pass/fail for all 5 success criteria"
  artifacts:
    - path: ".planning/phases/08-end-to-end-integration-polish/VERIFICATION.md"
      provides: "Signed-off verification record for all 5 Phase 8 success criteria"
  key_links:
    - from: "VERIFICATION.md"
      to: "ROADMAP.md §Phase 8 success criteria"
      via: "Each SC-N maps to a numbered criterion in ROADMAP.md"
      pattern: "SC-1|SC-2|SC-3|SC-4|SC-5"
---

<objective>
Run all automated quality gates, perform the manual HTML dashboard UI check, and produce a VERIFICATION.md that records pass/fail against all 5 Phase 8 success criteria.

Purpose: Closes the phase with a documented, auditable verification record. This is the submission-readiness gate.
Output: VERIFICATION.md committed to the planning directory; Phase 8 marked complete.
</objective>

<execution_context>
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\08-end-to-end-integration-polish\08-CONTEXT.md
</execution_context>

<context>
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\CLAUDE.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\ROADMAP.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\08-end-to-end-integration-polish\08-01-SUMMARY.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\08-end-to-end-integration-polish\08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated quality gates (tests + code review scan)</name>

  <read_first>
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\Makefile (confirm test target: pytest tests/ -v)
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\ROADMAP.md (lines 148-158 — Phase 8 success criteria SC-3 and SC-4 exact wording)
  </read_first>

  <files>.planning/phases/08-end-to-end-integration-polish/VERIFICATION.md</files>

  <action>
    Run the following commands in sequence and record exact output counts:

    Gate 1 — Test suite (SC-3):
      py -3.12 -m pytest tests/ -v
    Record: total tests passed, failed, errors. Expected: 114 passed, 0 failed, 0 errors.
    If any test fails, diagnose and fix before continuing. The test suite must be green
    before writing the verification record.

    Gate 2 — Code review scan (SC-4, part 1 — no print statements):
      grep -rn "print(" src/ main.py
    Record: number of matches. Expected: 0 matches.

    Gate 3 — Code review scan (SC-4, part 2 — no hardcoded API keys):
      grep -v "^#" .env.example | grep -c "sk-ant-api03"
    Record: count. Expected: 0.
    Also run:
      grep -rn "sk-ant\|ANTHROPIC_API_KEY\s*=" src/ main.py
    Record: any matches that show a hardcoded key value (env var references like
    os.environ["ANTHROPIC_API_KEY"] are fine — only reject literal key string values).

    Gate 4 — Type hints and docstrings (SC-4, spot check):
    Run these checks to confirm coverage:
      grep -rn "def " src/ main.py | grep -v "-> " | grep -v "test_" | grep -v "__"
    Review the list. Every public function must have a return type annotation.
    If any are found missing, fix them now.

    Gate 5 — Output file count (SC-1 confirmation):
      py -3.12 -c "import os; files=os.listdir('outputs'); print(sorted(files))"
      py -3.12 -c "import os; files=os.listdir('docs'); print(sorted(files))"
    Confirm outputs/ has: intervention_priority_list.xlsx, whatsapp_messages.csv,
    run_log.json, facilitator_dashboard.html, intervention_report.docx + campus files.
    Confirm docs/ has all 8 .docx files.

    After all gates pass, create VERIFICATION.md at:
    .planning/phases/08-end-to-end-integration-polish/VERIFICATION.md

    Use this exact structure:
    ```
    # Phase 8 Verification Record

    **Date:** 2026-05-25
    **Verified by:** Claude (automated gates) + Human (UI check)

    ## SC-1: All 14 files produced from a fresh run
    **Status:** PASS
    **Evidence:** run_log.json students_processed={N}; outputs/ contains {X} files; docs/ contains 8 .docx files

    ## SC-2: All output files open without errors
    **Status:** PENDING — requires human verification (SC-2 and SC-5 verified together in Task 2)

    ## SC-3: make test passes with 0 failures
    **Status:** PASS
    **Evidence:** pytest tests/ — {N} passed, 0 failed, 0 errors

    ## SC-4: Code review — type hints, docstrings, no print, no hardcoded paths/keys
    **Status:** PASS
    **Evidence:**
    - grep "print(" src/ main.py: 0 matches
    - grep "sk-ant-api03" .env.example: 0 matches
    - All public functions have type annotations (spot check passed)

    ## SC-5: HTML dashboard UI check
    **Status:** PENDING — requires human verification in Task 2
    ```
    Fill in the actual numbers from gate outputs. Save the file.
  </action>

  <verify>
    <automated>py -3.12 -m pytest tests/ -v --tb=short 2>&1 | tail -5</automated>
    <automated>grep -rn "print(" src/ main.py</automated>
  </verify>

  <acceptance_criteria>
    - py -3.12 -m pytest tests/ -v exits with code 0 (all tests pass)
    - pytest output line contains "passed" and "0 failed" (or "no tests failed")
    - grep -rn "print(" src/ main.py returns zero matches
    - grep -v "^#" .env.example | grep "sk-ant-api03" returns zero matches
    - VERIFICATION.md file exists at .planning/phases/08-end-to-end-integration-polish/VERIFICATION.md
    - VERIFICATION.md contains "SC-3" with PASS status and actual test count
    - VERIFICATION.md contains "SC-4" with PASS status
  </acceptance_criteria>

  <done>All automated gates pass; VERIFICATION.md created with SC-1, SC-3, SC-4 marked PASS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual HTML dashboard UI check (SC-2, SC-5)</name>

  <read_first>
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\ROADMAP.md (lines 155-157 — SC-2 and SC-5 exact wording)
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\08-end-to-end-integration-polish\VERIFICATION.md (to update SC-2 and SC-5 after check)
  </read_first>

  <what-built>
    Task 1 ran all automated gates (114 tests, code scan) and created VERIFICATION.md
    with SC-1, SC-3, SC-4 marked PASS. SC-2 and SC-5 require a human to open the HTML
    dashboard and confirm interactive features work.
  </what-built>

  <how-to-verify>
    1. Open outputs/facilitator_dashboard.html in Chrome by double-clicking the file
       (file:// URL — no server needed).

    2. At 1280px viewport width, verify:
       a. The risk table is visible with student rows populated
       b. Campus filter dropdown shows campus options — select one and confirm table filters
       c. Risk level filter buttons (CRITICAL / HIGH / MEDIUM / LOW / ALL) work — click each
          and confirm rows filter accordingly
       d. Type a partial name in the search box — confirm matching rows remain visible
       e. Click a student row to expand it — confirm the detail panel shows:
            - Risk breakdown section
            - Facilitator summary text
            - WhatsApp message text
            - Copy button
       f. Click the Copy button — confirm the WhatsApp message text is copied to clipboard
          (paste into a text editor to verify)

    3. Resize browser to 1920px viewport width (or use DevTools device emulation):
       a. Confirm layout is not broken (no horizontal scroll, no overlapping elements)
       b. Confirm the table still fills the page properly

    4. Check that no network requests are made:
       Open DevTools → Network tab → reload the page — confirm zero external requests

    5. Open outputs/intervention_report.docx in Google Docs — confirm it opens without
       "unsupported format" error and shows a title/cover page.

    6. Open docs/analysis.docx in Google Docs — confirm it opens without errors and
       shows the 5-section structure.

  </how-to-verify>

  <resume-signal>
    After completing the UI check, update VERIFICATION.md:
    - Set SC-2 status to PASS or FAIL with evidence notes
    - Set SC-5 status to PASS or FAIL with specific feature results
    Then run:
      git add .planning/phases/08-end-to-end-integration-polish/VERIFICATION.md
      git commit -m "docs(08): add Phase 8 VERIFICATION.md — all 5 SC results recorded"
    Type "verification complete" to confirm the commit was made, or describe any failures found.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VERIFICATION.md → git | Verification record is a planning artifact; contains no secrets or PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | Test suite results | accept | Tests are run locally; results recorded in VERIFICATION.md are self-reported; no external attestation required for this project scope |
| T-08-05 | Information Disclosure | HTML dashboard student data | accept | Synthetic data only; self-contained file:// dashboard; no server, no exfiltration vector |
</threat_model>

<verification>
Phase 8 complete when:
- py -3.12 -m pytest tests/ -v exits with 0 failures
- grep -rn "print(" src/ main.py returns zero matches
- grep -v "^#" .env.example | grep "sk-ant-api03" returns zero matches
- VERIFICATION.md exists with all 5 SC entries and no PENDING status
- git log --oneline shows both the demo commit (08-02) and the verification commit (08-03)
</verification>

<success_criteria>
- 114 tests pass (0 failures, 0 errors)
- Zero print() calls in src/ and main.py
- No real API key in .env.example or any committed file
- HTML dashboard: campus filter, risk filter, copy button, search all functional at 1280px and 1920px
- VERIFICATION.md committed with PASS recorded for all 5 Phase 8 success criteria
</success_criteria>

<output>
Create `.planning/phases/08-end-to-end-integration-polish/08-03-SUMMARY.md` when done.
Update `.planning/STATE.md` — Phase 8 status to Complete; all 8 phases complete.
Update `.planning/ROADMAP.md` — Phase 8 entry checked: [x] Phase 8: End-to-End Integration + Polish
</output>
