---
phase: 8
plan: 08-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/generate_data.py
  - .env.example
  - requirements-dev.txt
  - README.md
autonomous: true
requirements_addressed: []

must_haves:
  truths:
    - "grep -rn \"print(\" src/ main.py returns zero matches"
    - ".env.example contains no real API key — only the placeholder sk-ant-REPLACE-WITH-YOUR-KEY"
    - "requirements-dev.txt exists and lists pytest, respx, freezegun with pinned versions"
    - "README.md Quick Start steps match the actual project (make demo / make install)"
  artifacts:
    - path: "src/generate_data.py"
      provides: "print() removed, logger.info() used instead"
    - path: ".env.example"
      provides: "placeholder API key, no real credentials"
    - path: "requirements-dev.txt"
      provides: "pinned test dependencies"
    - path: "README.md"
      provides: "accurate Quick Start instructions"
  key_links:
    - from: "src/generate_data.py"
      to: "module-level logger"
      via: "logging.getLogger(__name__)"
      pattern: "logger = logging.getLogger"
---

<objective>
Fix four code polish issues identified in Phase 8 context: remove the print() at generate_data.py:322, scrub the real API key from .env.example, create the missing requirements-dev.txt, and verify README accuracy.

Purpose: These fixes ensure the code review gate (SC-4) passes and the repository is safe to share. make install currently fails on a fresh clone because requirements-dev.txt is absent.
Output: Corrected generate_data.py, sanitised .env.example, new requirements-dev.txt, verified README.md.
</objective>

<execution_context>
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\08-end-to-end-integration-polish\08-CONTEXT.md
</execution_context>

<context>
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\CLAUDE.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\ROADMAP.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix generate_data.py print() and scrub .env.example API key</name>

  <read_first>
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\src\generate_data.py (lines 1-20 for logger setup, lines 315-330 for print location — per D-05)
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.env.example (full file — per D-06)
  </read_first>

  <files>src/generate_data.py, .env.example</files>

  <action>
    Fix 1 — src/generate_data.py line 322:
    Replace the print() call with logger.info(). The module already has a module-level
    `logger = logging.getLogger(__name__)`. The exact replacement:
    - Remove the multi-line print( ... ) block at lines 321-325
    - Replace with a single: logger.info("Generated synthetic data: %s students, %s metric rows, %s notes in %s", len(metadata), len(metrics), len(notes), cfg.DATA_DIR)
    - Also remove the comment on line 321 ("# Single summary print — permitted for utility scripts (01-RESEARCH.md L611)") — that comment is now stale and misleading.

    Fix 2 — .env.example line 2:
    Replace the real API key value on line 2. The current value starts with sk-ant-api03-.
    Replace the entire line with: ANTHROPIC_API_KEY=sk-ant-REPLACE-WITH-YOUR-KEY
    Keep all other lines unchanged.
  </action>

  <verify>
    <automated>grep -rn "print(" src/generate_data.py</automated>
    <automated>grep "sk-ant-api03" .env.example</automated>
  </verify>

  <acceptance_criteria>
    - grep -rn "print(" src/generate_data.py returns zero matches (no remaining print() calls)
    - grep "sk-ant-api03" .env.example returns zero matches (real key removed)
    - grep "sk-ant-REPLACE-WITH-YOUR-KEY" .env.example returns exactly 1 match
    - grep "logger.info" src/generate_data.py returns at least 1 match confirming the replacement was made
    - src/generate_data.py line where print was must now contain logger.info( not print(
  </acceptance_criteria>

  <done>generate_data.py has zero print() calls; .env.example has only the placeholder key value.</done>
</task>

<task type="auto">
  <name>Task 2: Create requirements-dev.txt and verify README Quick Start</name>

  <read_first>
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\requirements.txt (for format/style to mirror — per D-03)
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\README.md (full file — per D-07)
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\Makefile (verify make targets referenced in README exist)
  </read_first>

  <files>requirements-dev.txt, README.md</files>

  <action>
    Create requirements-dev.txt at project root. Mirror requirements.txt style (one package per
    line, pinned versions, brief comment header). Installed versions confirmed:
      pytest==8.3.5
      respx==0.23.1
      freezegun==1.5.5
    The test suite has zero async tests (grep confirms no pytest.mark.anyio or asyncio imports),
    so pytest-anyio is NOT needed. Do not add it — per D-03, only what the current test suite uses.
    File content:
    ```
    # Test dependencies — install alongside requirements.txt
    pytest==8.3.5
    respx==0.23.1
    freezegun==1.5.5
    ```

    README.md review — read the current content and fix any inaccuracies:
    1. The Quick Start shows `make demo` for Unix/macOS and `./make.ps1 demo` for Windows — verify
       Makefile has a `demo` target (it does: python -m src.generate_data && python main.py). Keep.
    2. The Quick Start shows `pip install -r requirements.txt -r requirements-dev.txt` — this is
       correct once requirements-dev.txt is created. Keep.
    3. The "What it produces" section lists 5 output types but omits `run_log.json` and `docs/`.
       Add two lines:
       - `outputs/run_log.json` — pipeline run metadata (student counts, token usage)
       - `docs/` — 8 documentation .docx files (architecture, security, scalability, and more)
    4. The Requirements section says "Python 3.11+ recommended (3.12, 3.13 also tested)". Update
       to: "Python 3.12 required (pandas==2.2.3 has no wheel for Python 3.14)".
    If any other stale content is found, fix it. Do not introduce new sections — only correct
    existing inaccuracies.
  </action>

  <verify>
    <automated>py -3.12 -m pytest --collect-only -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - requirements-dev.txt exists at project root
    - grep "pytest==8.3.5" requirements-dev.txt returns 1 match
    - grep "respx==0.23.1" requirements-dev.txt returns 1 match
    - grep "freezegun==1.5.5" requirements-dev.txt returns 1 match
    - requirements-dev.txt does NOT contain pytest-anyio (grep "pytest-anyio" returns zero matches)
    - py -3.12 -m pytest --collect-only -q completes without import errors (confirms test deps are coherent)
    - README.md contains "run_log.json" (new line added to What it produces)
    - README.md contains "Python 3.12 required" (updated from 3.11+)
  </acceptance_criteria>

  <done>requirements-dev.txt created with 3 pinned packages; README.md Quick Start and output list are accurate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.example → git history | Placeholder must replace real key; git history may still have it if committed previously — note to user in VERIFICATION.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | .env.example | mitigate | Replace real ANTHROPIC_API_KEY with sk-ant-REPLACE-WITH-YOUR-KEY placeholder (D-06) |
| T-08-SC | Tampering | requirements-dev.txt installs | accept | All three packages (pytest, respx, freezegun) are established, widely-used test libraries with no legitimacy concerns |
</threat_model>

<verification>
After both tasks complete:
- grep -rn "print(" src/ main.py — must return zero matches
- grep "sk-ant-api03" .env.example — must return zero matches
- cat requirements-dev.txt — must show pytest==8.3.5, respx==0.23.1, freezegun==1.5.5
- py -3.12 -m pytest --collect-only -q — must complete without errors
</verification>

<success_criteria>
- Zero print() calls in src/ and main.py
- .env.example has placeholder API key only
- requirements-dev.txt exists with 3 pinned test packages
- README.md Quick Start matches actual project structure
</success_criteria>

<output>
Create `.planning/phases/08-end-to-end-integration-polish/08-01-SUMMARY.md` when done.
</output>
