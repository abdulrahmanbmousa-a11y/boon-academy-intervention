---
phase: 8
plan: 08-02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - outputs/intervention_priority_list.xlsx
  - outputs/whatsapp_messages.csv
  - outputs/run_log.json
  - outputs/facilitator_dashboard.html
  - outputs/intervention_report.docx
  - docs/analysis.docx
  - docs/architecture.docx
  - docs/security.docx
  - docs/engineering_decisions.docx
  - docs/data_handling.docx
  - docs/scalability.docx
  - docs/system_design.docx
  - docs/alternatives.docx
  - analysis.md
  - .gitignore
autonomous: false
requirements_addressed: []

must_haves:
  truths:
    - "python main.py completes without raising an exception"
    - "outputs/ contains intervention_priority_list.xlsx, whatsapp_messages.csv, run_log.json, facilitator_dashboard.html, intervention_report.docx, and one facilitator_dashboard_{campus_id}.xlsx per campus"
    - "docs/ contains all 8 .docx files: analysis.docx, architecture.docx, security.docx, engineering_decisions.docx, data_handling.docx, scalability.docx, system_design.docx, alternatives.docx"
    - "analysis.md at project root contains real student counts and risk distribution numbers from the live run"
    - "All demo output files are committed to git"
  artifacts:
    - path: "outputs/run_log.json"
      provides: "Live run metadata with real student counts, token usage, API call counts"
    - path: "analysis.md"
      provides: "5-section memo with real pipeline numbers"
    - path: ".gitignore"
      provides: "outputs/ exclusion removed so demo files can be committed"
  key_links:
    - from: "run_log.json"
      to: "analysis.md"
      via: "doc_generator._write_analysis_md() embeds run_log numbers"
      pattern: "students_processed"
---

<objective>
Run the full live pipeline with a real Anthropic API key, verify all 14 output files are produced, confirm analysis.md has real numbers, update .gitignore to allow demo outputs, and commit everything to the repo.

Purpose: Satisfies success criterion SC-1 (all 14 files produced from a fresh run) and SC-2 (files open without errors). The demo outputs must be in the repo so reviewers can inspect them without running the pipeline themselves (D-02).
Output: All 14 output/doc files on disk and committed; analysis.md with live run numbers committed.
</objective>

<execution_context>
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\08-end-to-end-integration-polish\08-CONTEXT.md
</execution_context>

<context>
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\CLAUDE.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\ROADMAP.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\STATE.md
@C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.planning\phases\08-end-to-end-integration-polish\08-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run live pipeline — make demo</name>

  <read_first>
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\Makefile (confirm demo target: python -m src.generate_data && python main.py)
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.env (confirm ANTHROPIC_API_KEY is set to a real key — NOT the placeholder)
  </read_first>

  <what-built>
    Plan 08-01 completed all code fixes: print() removed from generate_data.py,
    .env.example sanitised, requirements-dev.txt created. The pipeline code is now
    in its final polished state. This task runs it live.
  </what-built>

  <how-to-verify>
    1. Confirm .env contains a real ANTHROPIC_API_KEY (not the placeholder sk-ant-REPLACE-WITH-YOUR-KEY).
       If it still has the placeholder, update .env with your real key before continuing.
    2. From the project root, run:
           py -3.12 -m src.generate_data
           py -3.12 main.py
       (Or: make demo — but note make uses the system python; py -3.12 is safer on this machine.)
    3. Watch for any errors or tracebacks. The run should complete in ~30-120 seconds depending
       on API response time.
    4. After it completes, confirm these files exist:
       outputs/ directory:
         - intervention_priority_list.xlsx
         - whatsapp_messages.csv
         - run_log.json
         - facilitator_dashboard.html
         - intervention_report.docx
         - facilitator_dashboard_{campus_id}.xlsx (one per campus — expect ~20 files)
       docs/ directory:
         - analysis.docx
         - architecture.docx
         - security.docx
         - engineering_decisions.docx
         - data_handling.docx
         - scalability.docx
         - system_design.docx
         - alternatives.docx
       Project root:
         - analysis.md
    5. Run: py -3.12 -c "import json; d=json.load(open('outputs/run_log.json')); print(d)"
       Confirm students_processed is a positive integer and api_calls_made >= 1.
  </how-to-verify>

  <resume-signal>
    Type "run complete" and paste the students_processed count from run_log.json,
    OR describe any error that occurred.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify analysis.md has real numbers; update .gitignore; commit demo outputs</name>

  <read_first>
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\analysis.md (check for placeholder values vs real numbers — per D-08)
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\outputs\run_log.json (source of truth for real numbers — per D-08)
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\.gitignore (current exclusion rules — per D-02)
    - C:\Users\abdul\Desktop\NOON ACADEMY ASSIGNEMTN\docs\ (list all .docx files present — per D-09)
  </read_first>

  <files>analysis.md, .gitignore</files>

  <action>
    Step 1 — Verify analysis.md (D-08):
    Read analysis.md and run_log.json. Check whether analysis.md contains real numbers
    (e.g., actual student count like "300 students" not "N students" or "0 students").
    If analysis.md contains placeholder values, update it with the real numbers from run_log.json:
      - students_processed → update the student count sentence in Section 1 (Diagnosis)
      - CRITICAL/HIGH/MEDIUM/LOW counts → update the risk distribution sentence
      - api_calls_made, tokens_used → update Section 3 (What you built) API stats sentence
    If analysis.md already has real numbers (generated by the live run just completed), no changes needed.

    Step 2 — Verify docs/ completeness (D-09):
    List all files in docs/ and confirm all 8 are present:
    analysis.docx, architecture.docx, security.docx, engineering_decisions.docx,
    data_handling.docx, scalability.docx, system_design.docx, alternatives.docx.
    If any are missing, note them in the SUMMARY. Do not attempt to regenerate them here
    (that would require a separate pipeline re-run) — just document the finding.

    Step 3 — Update .gitignore to allow demo outputs (D-02):
    The current .gitignore excludes outputs/ entirely. Remove or narrow that exclusion so
    the specific demo output files can be committed. Replace the line:
      outputs/
    with a negation pattern that allows the non-campus-dashboard files:
      outputs/
      !outputs/intervention_priority_list.xlsx
      !outputs/whatsapp_messages.csv
      !outputs/run_log.json
      !outputs/facilitator_dashboard.html
      !outputs/intervention_report.docx
    This keeps the per-campus xlsx files gitignored (they are numerous — ~20 files) while
    committing the 5 shared outputs.
    Also add docs/ to .gitignore with negation exceptions for all 8 .docx files so they
    are committed:
      docs/
      !docs/analysis.docx
      !docs/architecture.docx
      !docs/security.docx
      !docs/engineering_decisions.docx
      !docs/data_handling.docx
      !docs/scalability.docx
      !docs/system_design.docx
      !docs/alternatives.docx
    NOTE: If docs/ is not currently in .gitignore (it was not seen in the current .gitignore),
    simply do not add it — all docs/ files will be tracked automatically.

    Step 4 — Stage and commit demo outputs:
    Run the following git commands:
      git add outputs/intervention_priority_list.xlsx
      git add outputs/whatsapp_messages.csv
      git add outputs/run_log.json
      git add outputs/facilitator_dashboard.html
      git add outputs/intervention_report.docx
      git add docs/
      git add analysis.md
      git add .gitignore
      git commit -m "demo(08): add live pipeline outputs — 300 students, all 14 files verified"
    Adjust the commit message student count to match the actual students_processed from run_log.json.
  </action>

  <verify>
    <automated>git log --oneline -1</automated>
    <automated>py -3.12 -c "import json; d=json.load(open('outputs/run_log.json')); print('students:', d.get('students_processed'), 'api_calls:', d.get('api_calls_made'))"</automated>
  </verify>

  <acceptance_criteria>
    - analysis.md does NOT contain literal placeholder text "N students" or "0 students" or "TBD"
    - analysis.md contains a number matching run_log.json students_processed value
    - .gitignore updated: the line "outputs/" is followed by !outputs/ negation exceptions for the 5 shared files
    - git log --oneline -1 shows the demo commit with "demo(08)" prefix
    - git show --stat HEAD lists intervention_priority_list.xlsx, whatsapp_messages.csv, run_log.json, facilitator_dashboard.html, intervention_report.docx among committed files
    - All 8 docs/*.docx files are present on disk (ls docs/*.docx shows 8 files)
  </acceptance_criteria>

  <done>analysis.md has real run numbers; .gitignore allows demo outputs; all 14 output files committed to repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → pipeline | Real API key used only in .env (not .env.example); key must NOT appear in run_log.json or committed files |
| run_log.json → git | run_log.json is committed; confirm it contains no API key (T-08-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Information Disclosure | run_log.json committed to git | mitigate | Verify run_log.json contains only the 7 safe keys (run_timestamp, students_processed, api_calls_made, tokens_used, errors_encountered, fallbacks_triggered, data_quality_warnings) — no API key |
| T-08-03 | Information Disclosure | outputs/*.xlsx student data in git | accept | Synthetic data only — no real student PII; acceptable for demo/submission purposes |
</threat_model>

<verification>
After both tasks complete:
- py -3.12 -c "import json; d=json.load(open('outputs/run_log.json')); assert d['students_processed'] > 0" — must pass
- ls outputs/ — must include intervention_priority_list.xlsx, whatsapp_messages.csv, run_log.json, facilitator_dashboard.html, intervention_report.docx
- ls docs/ — must include all 8 .docx files
- git show --stat HEAD — must include analysis.md and at least 5 outputs/ files
- grep "sk-ant" outputs/run_log.json — must return zero matches
</verification>

<success_criteria>
- python main.py completes without exception on fresh synthetic data
- All 14 file types produced and verified on disk
- analysis.md contains real pipeline run numbers
- All demo outputs committed to git
</success_criteria>

<output>
Create `.planning/phases/08-end-to-end-integration-polish/08-02-SUMMARY.md` when done.
</output>
