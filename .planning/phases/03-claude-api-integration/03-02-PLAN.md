---
phase: 03-claude-api-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/llm_engine.py
autonomous: true
requirements:
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-04
  - LLM-05
  - LLM-06
  - LLM-08

must_haves:
  truths:
    - "enrich_with_llm(df, api_key) never raises — all exceptions are caught and handled at the chunk level"
    - "CRITICAL and HIGH students receive either LLM-generated or template-generated facilitator_summary and whatsapp_message"
    - "MEDIUM and LOW students have None in all four LLM output columns"
    - "One API call is made per campus chunk (at most MAX_STUDENTS_PER_LLM_CALL students), not one per student"
    - "Within a campus, CRITICAL students sorted by risk_score descending appear before HIGH students"
    - "Token counts accumulate correctly in the returned counts dict after each successful API call"
    - "student_name and parent_phone do not appear in any log output"
    - "LLM_ENABLED=false bypasses all API calls and applies templates with llm_error_reason='llm_disabled'"
  artifacts:
    - path: "src/llm_engine.py"
      provides: "Full campus-batched LLM enrichment with three-layer fallback"
      exports:
        - "enrich_with_llm"
      min_lines: 120
    - path: "src/llm_engine.py"
      provides: "INTERVENTION_TOOL dict for tool-use structured output"
      contains: "generate_interventions"
    - path: "src/llm_engine.py"
      provides: "YAML template loaded at module import"
      contains: "_TEMPLATES"
  key_links:
    - from: "src/llm_engine.py"
      to: "src/llm_templates.yaml"
      via: "Path(__file__).parent / 'llm_templates.yaml' at module level"
      pattern: "yaml\\.safe_load"
    - from: "src/llm_engine.py"
      to: "anthropic.Anthropic()"
      via: "instantiated inside enrich_with_llm() with api_key parameter, NOT at module level"
      pattern: "anthropic\\.Anthropic\\("
    - from: "enrich_with_llm return"
      to: "main.py run_log"
      via: "tuple (df, counts_dict) — counts_dict keys: api_calls_made, tokens_used, fallbacks_triggered"
      pattern: "api_calls_made.*tokens_used.*fallbacks_triggered"
---

<objective>
Implement src/llm_engine.py: replace the NotImplementedError stub with the full campus-batched enrichment function including tool-use structured output, three-layer fallback (SDK retry → re-prompt → YAML template), PII-safe logging, LLM_ENABLED bypass, and tuple return.

Purpose: This is the core Phase 3 deliverable. Every CRITICAL and HIGH student must receive AI-generated or template-generated intervention content by the time this plan completes.

Output: src/llm_engine.py — fully implemented, importable, never raises. Returns (df, counts_dict).
</objective>

<execution_context>
@C:\Users\abdul\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\abdul\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-claude-api-integration/03-CONTEXT.md
@.planning/phases/03-claude-api-integration/03-RESEARCH.md
@.planning/phases/03-claude-api-integration/03-PATTERNS.md
@.planning/phases/03-claude-api-integration/03-01-SUMMARY.md

<interfaces>
From src/config.py (added in 03-01):
  ANTHROPIC_MODEL: str = "claude-sonnet-4-5"
  LLM_ENABLED: bool  (True by default; False when env LLM_ENABLED=false)
  MAX_TOKENS: int = 1024
  TEMPERATURE: float = 0.3
  TIMEOUT_SECONDS: int = 30
  MAX_STUDENTS_PER_LLM_CALL: int = 10  (existing, line 36)
  COL_FACILITATOR_SUMMARY: str = "facilitator_summary"
  COL_WHATSAPP_MESSAGE: str = "whatsapp_message"
  COL_GENERATED_BY: str = "generated_by"
  COL_LLM_ERROR_REASON: str = "llm_error_reason"
  COL_RISK_LEVEL: str = "risk_level"
  COL_RISK_SCORE: str = "risk_score"
  COL_CAMPUS_ID: str = "campus_id"
  COL_STUDENT_ID: str = "student_id"
  COL_ATTENDANCE_RATE: str = "attendance_rate"
  COL_AVG_PRACTICE: str = "avg_practice_questions"
  COL_TREND_DIR: str = "trend_direction"
  COL_DAYS_SINCE_NOTE: str = "days_since_last_note"
  COL_RECOMMENDED_ACTION: str = "recommended_action"
  COL_STUDENT_NAME: str = "student_name"       (in df but NEVER sent to API or logged)
  COL_PARENT_PHONE: str = "parent_phone"       (in df but NEVER sent to API or logged)
  COL_ATTENDANCE_COMPONENT: str = "attendance_component"
  COL_PRACTICE_COMPONENT: str = "practice_component"
  COL_TREND_COMPONENT: str = "trend_component"
  COL_NOTES_COMPONENT: str = "notes_component"

From src/llm_templates.yaml (created in 03-01):
  _TEMPLATES["CRITICAL"]["facilitator_summary"]  -- format string with {student_id}, {risk_score}, etc.
  _TEMPLATES["CRITICAL"]["whatsapp_message"]
  _TEMPLATES["HIGH"]["facilitator_summary"]
  _TEMPLATES["HIGH"]["whatsapp_message"]
  Interpolated via row.to_dict() passed to str.format_map()

Locked function signature (STATE.md, updated in 03-01):
  def enrich_with_llm(
      df: pd.DataFrame,
      api_key: str,
      http_client: httpx.Client | None = None,
  ) -> tuple[pd.DataFrame, dict]:

  counts_dict shape: {"api_calls_made": int, "tokens_used": {"input": int, "output": int}, "fallbacks_triggered": int}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement src/llm_engine.py — full campus-batched enrichment</name>
  <files>src/llm_engine.py</files>

  <read_first>
    - src/llm_engine.py — current stub (32 lines); full replacement
    - src/config.py — all COL_* constants and tunables now available (added in 03-01)
    - src/llm_templates.yaml — CRITICAL/HIGH template structure (created in 03-01)
    - src/risk_engine.py — df.copy() purity pattern; logger = logging.getLogger(__name__); cfg.COL_* column access pattern
    - 03-RESEARCH.md §Pattern 1 — client instantiation inside function (not module level, Pitfall 3)
    - 03-RESEARCH.md §Pattern 2 — INTERVENTION_TOOL dict; tool_choice={"type":"tool","name":"generate_interventions"}; ToolUseBlock parsing
    - 03-RESEARCH.md §Pattern 3 — three-layer exception hierarchy; _classify_error()
    - 03-RESEARCH.md §Pattern 5 — yaml.safe_load() at module level; Path(__file__).parent
    - 03-RESEARCH.md §Pattern 6 — tuple return; counts_dict shape
    - 03-RESEARCH.md §Pattern 7 — chunk loop; CRITICAL-first sort using key=lambda col: col.map({"CRITICAL":0,"HIGH":1})
    - 03-RESEARCH.md §Pitfall 1 — guard tool_block.input["students"] with KeyError → Layer 3
    - 03-RESEARCH.md §Pitfall 2 — alphabetical sort puts HIGH before CRITICAL; use map key
    - 03-RESEARCH.md §Pitfall 3 — never instantiate Anthropic() at module level
    - 03-PATTERNS.md §src/llm_engine.py — imports block, module-level constants, column assignment pattern, PII logging pattern
    - 03-CONTEXT.md §D-04 — chunk overflow: 15 students → 2 calls (10+5); no truncation
    - 03-CONTEXT.md §D-05 — CRITICAL first descending, then HIGH descending
    - 03-CONTEXT.md §D-06 — 4 new columns; exact values
    - 03-CONTEXT.md §D-07 — MEDIUM/LOW get None, not empty string, not "skipped"
    - 03-CONTEXT.md §D-08 — generated_by: "llm" or "template" only (never None for CRITICAL/HIGH)
  </read_first>

  <behavior>
    - enrich_with_llm(df_with_only_medium_low, api_key) -> (df, {"api_calls_made": 0, "tokens_used": {"input": 0, "output": 0}, "fallbacks_triggered": 0}); all 4 LLM columns are None
    - enrich_with_llm(df_with_one_critical, api_key) on successful API call -> generated_by == "llm", facilitator_summary non-empty, whatsapp_message non-empty
    - enrich_with_llm(df_with_one_critical, api_key) when API raises after all retries -> generated_by == "template", llm_error_reason in {"timeout","rate_limit","max_retries_exceeded"}, function returns without raising
    - enrich_with_llm(df_with_one_critical, api_key) when API returns malformed tool response (KeyError on "students") -> generated_by == "template", llm_error_reason == "malformed_response", fallbacks_triggered == 1
    - enrich_with_llm(df_15_critical_same_campus, api_key) -> exactly 2 API calls made (chunks of 10 + 5); all 15 students have generated_by set
    - LLM_ENABLED=false -> 0 API calls, all CRITICAL/HIGH get generated_by="template", llm_error_reason="llm_disabled"
    - Token counts: after 2 successful API calls returning usage.input_tokens=150, usage.output_tokens=80 each -> tokens_used == {"input": 300, "output": 160}
  </behavior>

  <action>
    Replace src/llm_engine.py entirely. Structure (no fenced code — directives only):

    MODULE-LEVEL SECTION:
    - Docstring referencing RESEARCH.md patterns 1-7, D-01..D-08, Pitfalls 1-6
    - Imports: logging, pathlib.Path, typing.Optional, anthropic, httpx, pandas as pd, yaml, from src import config as cfg
    - logger = logging.getLogger(__name__)
    - _TEMPLATES_PATH = Path(__file__).parent / "llm_templates.yaml"
    - Load _TEMPLATES using yaml.safe_load() inside a with block — module level, not inside function
    - INTERVENTION_TOOL dict constant: name="generate_interventions"; input_schema has top-level "students" array; each item has student_id (string), facilitator_summary (string, "Exactly 2 sentences. Action-oriented summary for the facilitator."), whatsapp_message (string, "Under 100 words. Warm, professional tone. Does not mention risk scores."); all three required

    PRIVATE HELPER: _classify_error(exc: anthropic.APIError) -> str
    - Returns "timeout" for APITimeoutError, "rate_limit" for RateLimitError, "max_retries_exceeded" otherwise

    PRIVATE HELPER: _apply_templates(chunk: pd.DataFrame, error_reason: str) -> list[dict]
    - For each row in chunk.iterrows(), call _TEMPLATES[row[cfg.COL_RISK_LEVEL]]["facilitator_summary"].format_map(row.to_dict()) and same for whatsapp_message
    - Return list of dicts with keys: student_id, facilitator_summary, whatsapp_message, generated_by="template", llm_error_reason=error_reason

    PUBLIC FUNCTION: enrich_with_llm(df, api_key, http_client=None) -> tuple[pd.DataFrame, dict]
    - Docstring: describes campus-batched enrichment, three-layer fallback, never raises
    - df = df.copy() — first statement (purity guarantee, mirrors score_risk())
    - Initialize four columns to None for ALL rows: cfg.COL_FACILITATOR_SUMMARY, cfg.COL_WHATSAPP_MESSAGE, cfg.COL_GENERATED_BY, cfg.COL_LLM_ERROR_REASON
    - Initialize accumulators: api_calls=0, tokens={"input":0,"output":0}, fallbacks=0

    LLM_ENABLED=false EARLY EXIT PATH:
    - If not cfg.LLM_ENABLED: filter CRITICAL/HIGH rows, call _apply_templates on all of them with error_reason="llm_disabled", write results back via df.loc[idx, ...] for each column, log aggregate count (no PII), return (df, counts_dict) immediately

    CLIENT INSTANTIATION (production path):
    - If http_client is None: create Anthropic client with api_key=api_key, max_retries=3, timeout=cfg.TIMEOUT_SECONDS
    - If http_client is not None (test injection): create Anthropic client with api_key=api_key, http_client=http_client, max_retries=0

    CAMPUS LOOP:
    - Filter df to CRITICAL/HIGH only: at_risk_df = df[df[cfg.COL_RISK_LEVEL].isin(["CRITICAL","HIGH"])]
    - If at_risk_df is empty, skip loop
    - Group by cfg.COL_CAMPUS_ID
    - For each campus_id, campus_df group:
      - Sort: use key=lambda col: col.map({"CRITICAL":0,"HIGH":1}) if col.name == cfg.COL_RISK_LEVEL else col, ascending=[True, False] on [cfg.COL_RISK_LEVEL, cfg.COL_RISK_SCORE]
      - Chunk loop: for i in range(0, len(campus_students), cfg.MAX_STUDENTS_PER_LLM_CALL): chunk = campus_students.iloc[i:i+cfg.MAX_STUDENTS_PER_LLM_CALL]
      - Build student_data list for prompt: include only student_id, risk_level, risk_score, attendance_rate, avg_practice_questions, trend_direction, days_since_last_note, recommended_action — NEVER include student_name or parent_phone
      - Build prompt string from student_data (plain text, no PII)
      - LAYER 1 TRY: client.messages.create() with model=cfg.ANTHROPIC_MODEL, max_tokens=cfg.MAX_TOKENS, temperature=cfg.TEMPERATURE, tools=[INTERVENTION_TOOL], tool_choice={"type":"tool","name":"generate_interventions"}, messages=[{"role":"user","content":prompt}]
        - Parse: tool_block = next(b for b in response.content if isinstance(b, anthropic.types.ToolUseBlock)); results = tool_block.input["students"]  (KeyError → Layer 3)
        - Accumulate tokens: tokens["input"] += response.usage.input_tokens; tokens["output"] += response.usage.output_tokens; api_calls += 1
        - Write results back via df.loc for each student
      - EXCEPT (anthropic.APIConnectionError, anthropic.APITimeoutError, anthropic.RateLimitError, anthropic.APIStatusError) as exc:
        - error_reason = _classify_error(exc)
        - Log warning with campus_id and error_reason — NO student names
        - LAYER 2 TRY: simplified re-prompt (same structure, same tool), new client.messages.create() call
          - On success: parse + accumulate + write; api_calls += 1
        - EXCEPT Exception:
          - LAYER 3: results = _apply_templates(chunk, error_reason); write back; fallbacks += len(chunk)
      - EXCEPT (KeyError, ValueError, StopIteration) as exc:
        - Log warning with "malformed tool response" — NO student names
        - LAYER 3: results = _apply_templates(chunk, "malformed_response"); write back; fallbacks += len(chunk)
    - After campus loop: log aggregate summary (api_calls, tokens, fallbacks) — NO PII
    - Return (df, {"api_calls_made": api_calls, "tokens_used": tokens, "fallbacks_triggered": fallbacks})

    COLUMN WRITE-BACK PATTERN for both LLM success and template fallback:
    - For each result dict in results: find the df.index where cfg.COL_STUDENT_ID == result["student_id"]
    - Use df.loc[idx, cfg.COL_FACILITATOR_SUMMARY] = result["facilitator_summary"] etc.
    - For LLM success: generated_by="llm", llm_error_reason=None (or leave as initialized None)
    - For template: generated_by="template", llm_error_reason=error_reason

    NEVER use bare string literals for column names anywhere in llm_engine.py.
    NEVER log api_key, student_name, or parent_phone.
  </action>

  <verify>
    <automated>py -3.12 -c "from src.llm_engine import enrich_with_llm; import inspect; sig = inspect.signature(enrich_with_llm); params = list(sig.parameters); assert params == ['df','api_key','http_client'], f'Wrong params: {params}'; print('signature OK')"</automated>
  </verify>

  <done>
    src/llm_engine.py imports cleanly under py -3.12 with no errors.
    enrich_with_llm signature matches tuple[pd.DataFrame, dict] with optional http_client parameter.
    The function body handles all seven behavior cases listed in the behavior block above.
    No bare column string literals in the file — all access via cfg.COL_*.
    No print statements — only logger.* calls.
    No PII (student_name, parent_phone, api_key) appears in any log statement.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student data → Anthropic API prompt | Only non-PII fields sent: student_id (opaque), risk_level, scores, recommended_action |
| Anthropic API response → DataFrame | Tool-use schema validates structure; KeyError guard routes to fallback |
| llm_templates.yaml → format_map() | Known keys only from row.to_dict(); no eval(); yaml.safe_load() prevents YAML code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | logger.* calls in llm_engine.py | mitigate | Mask: never log COL_STUDENT_NAME or COL_PARENT_PHONE; aggregate-only logs; api_key never logged (LLM-07, LLM-08) |
| T-03-04 | Information Disclosure | Anthropic API prompt construction | mitigate | student_data list excludes student_name and parent_phone fields explicitly (LLM-08); only student_id used as identifier |
| T-03-05 | Tampering | tool_block.input["students"] parsing | mitigate | KeyError/StopIteration caught and routed to Layer 3 template; no unguarded dict access |
| T-03-06 | Tampering | _apply_templates format_map() | mitigate | Known keys from row.to_dict() only; no user-controlled free-text enters format string |
| T-03-07 | Denial of Service | Anthropic API unavailability | mitigate | Three-layer fallback ensures pipeline completes; function never raises; template fallback always succeeds |
| T-03-SC | Tampering | Anthropic SDK HTTP calls | accept | SDK uses HTTPS with certificate validation; no custom cert override |
</threat_model>

<verification>
After task completion:

1. py -3.12 -c "from src import llm_engine; print('import OK')" — exits 0 (no import error)
2. py -3.12 -c "import inspect; from src.llm_engine import enrich_with_llm; print(inspect.signature(enrich_with_llm))" — prints (df: pd.DataFrame, api_key: str, http_client: ... = None) -> ...
3. py -3.12 -c "from src.llm_engine import _TEMPLATES; assert 'CRITICAL' in _TEMPLATES; assert 'HIGH' in _TEMPLATES; print('templates loaded OK')"
4. py -3.12 -m pytest tests/ -q — existing 53 tests still GREEN (no regression)
5. Grep for bare column strings: py -3.12 -c "import re; src=open('src/llm_engine.py').read(); src=re.sub(r'\"\"\".*?\"\"\"','',src,flags=re.DOTALL); src=re.sub(r'#.*','',src); hits=[m for m in re.findall(r'\"([a-z][a-z0-9_]{3,})\"',src) if m not in {'llm','template','llm_disabled','timeout','rate_limit','malformed_response','max_retries_exceeded','students','tool','name','utf'}]; assert not hits, f'Bare column strings: {hits}'; print('no bare strings OK')"
</verification>

<success_criteria>
- src/llm_engine.py replaces the stub entirely — no NotImplementedError remains
- Function signature: enrich_with_llm(df, api_key, http_client=None) -> tuple[pd.DataFrame, dict]
- INTERVENTION_TOOL defined at module level with "generate_interventions" name and correct input_schema
- _TEMPLATES loaded at module level via yaml.safe_load (not inside the function)
- Anthropic() client instantiated inside enrich_with_llm() — never at module level
- Three-layer fallback: Layer 1 (SDK max_retries=3) → Layer 2 (re-prompt) → Layer 3 (template)
- LLM_ENABLED=false path: 0 API calls, all CRITICAL/HIGH templated with llm_error_reason="llm_disabled"
- MEDIUM/LOW rows: all four LLM columns remain None
- No bare column string literals in production code (cfg.COL_* used throughout)
- No PII in any log statement
- py -3.12 -m pytest tests/ -q: 53 tests GREEN
</success_criteria>

<output>
Create .planning/phases/03-claude-api-integration/03-02-SUMMARY.md when done.
</output>
