---
phase: 03-claude-api-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - main.py
  - tests/test_llm_engine.py
autonomous: true
requirements:
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-05
  - LLM-06
  - LLM-08
  - LLM-09

must_haves:
  truths:
    - "main.py calls enrich_with_llm and unpacks the tuple return into df and llm_counts"
    - "run_log api_calls_made, tokens_used, and fallbacks_triggered are updated from llm_counts after the call"
    - "All nine LLM-* requirements have at least one test in test_llm_engine.py"
    - "Fallback test: mocked API failure produces generated_by='template' for the affected student"
    - "Token logging test: mocked successful call produces counts['tokens_used']['input'] > 0"
    - "Batching test: two-campus cohort produces exactly two API calls (one per campus)"
    - "LLM_ENABLED=false test: zero API calls, all CRITICAL/HIGH students get generated_by='template'"
    - "PII test: student_name and parent_phone values do not appear in any caplog record"
    - "Source-scan test: no bare column-name strings in llm_engine.py"
  artifacts:
    - path: "main.py"
      provides: "LLM enrichment wired at lines 74-78; from src import llm_engine added to imports"
      contains: "llm_engine.enrich_with_llm"
    - path: "tests/test_llm_engine.py"
      provides: "pytest suite covering all 9 LLM requirements"
      min_lines: 200
  key_links:
    - from: "main.py"
      to: "llm_engine.enrich_with_llm"
      via: "df, llm_counts = llm_engine.enrich_with_llm(df, cfg.ANTHROPIC_API_KEY)"
      pattern: "llm_counts\\[.api_calls_made.\\]"
    - from: "tests/test_llm_engine.py"
      to: "enrich_with_llm"
      via: "httpx.Client(transport=respx_mock) injected via http_client= parameter"
      pattern: "httpx\\.Client\\(transport=respx_mock\\)"
---

<objective>
Wire enrich_with_llm into main.py and write the complete test suite for src/llm_engine.py using respx-mocked HTTP transport.

Purpose: Integration wiring closes the pipeline loop so main.py runs end-to-end. The test suite proves all nine LLM requirements are met and catches regressions before downstream phases build on the enriched DataFrame.

Output: main.py with LLM wiring at lines 74-78 + import added; tests/test_llm_engine.py with tests for LLM-01 through LLM-09.
</objective>

<execution_context>
@C:\Users\abdul\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\abdul\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-claude-api-integration/03-CONTEXT.md
@.planning/phases/03-claude-api-integration/03-RESEARCH.md
@.planning/phases/03-claude-api-integration/03-PATTERNS.md
@.planning/phases/03-claude-api-integration/03-01-SUMMARY.md
@.planning/phases/03-claude-api-integration/03-02-SUMMARY.md

<interfaces>
From src/llm_engine.py (implemented in 03-02):
  def enrich_with_llm(
      df: pd.DataFrame,
      api_key: str,
      http_client: httpx.Client | None = None,
  ) -> tuple[pd.DataFrame, dict]:
  # dict keys: api_calls_made (int), tokens_used (dict[str,int]), fallbacks_triggered (int)
  # Never raises.

From main.py lines 74-78 (current stub to replace):
  # Phase 3: LLM enrichment (wired in plan 03-01)
  # df = llm_engine.enrich_with_llm(df, cfg.ANTHROPIC_API_KEY)
  # run_log["api_calls_made"] = ...
  # run_log["tokens_used"] = ...
  # run_log["fallbacks_triggered"] = ...

main.py run_log dict shape (lines 45-53):
  api_calls_made: 0        (int)
  tokens_used: {"input": 0, "output": 0}  (dict[str, int])
  fallbacks_triggered: 0   (int)

From tests/conftest.py (existing):
  os.environ.setdefault("ANTHROPIC_API_KEY", "dummy-key-for-tests")
  -- runs before any src.config import; no per-test API key setup needed

respx injection pattern (RESEARCH.md Pattern 4):
  http_client = httpx.Client(transport=respx_mock)
  enrich_with_llm(df, "test-key", http_client=http_client)
  -- respx_mock is a pytest fixture provided by respx library; no setup required

Anthropic API mock response shape (_make_tool_response helper):
  {"id":"msg_test","type":"message","role":"assistant","model":cfg.ANTHROPIC_MODEL,
   "stop_reason":"tool_use","stop_sequence":None,
   "usage":{"input_tokens":150,"output_tokens":80},
   "content":[{"type":"tool_use","id":"toolu_test","name":"generate_interventions",
               "input":{"students":[{...}]}}]}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire enrich_with_llm into main.py</name>
  <files>main.py</files>

  <read_first>
    - main.py — full file; import block at lines 11-13; Phase 3 stub at lines 74-78; run_log dict at lines 45-53
    - 03-PATTERNS.md §main.py — exact target state for lines 74-78; import placement note (top-level imports, not inline)
    - .planning/STATE.md §Module contracts — updated tuple return contract
  </read_first>

  <action>
    Two changes to main.py:

    CHANGE 1 — Add import at top-level imports block (lines 11-13):
    Add "from src import llm_engine" after the existing "from src.risk_engine import score_risk" line.
    Keep the three existing imports; add one new line only.

    CHANGE 2 — Replace Phase 3 comment block (lines 74-78):
    Remove the five commented-out lines and replace with:

      # Phase 3: LLM enrichment
      df, llm_counts = llm_engine.enrich_with_llm(df, cfg.ANTHROPIC_API_KEY)
      run_log["api_calls_made"] = llm_counts["api_calls_made"]
      run_log["tokens_used"] = llm_counts["tokens_used"]
      run_log["fallbacks_triggered"] = llm_counts["fallbacks_triggered"]
      logger.info(
          f"LLM enrichment complete — "
          f"api_calls={llm_counts['api_calls_made']}, "
          f"fallbacks={llm_counts['fallbacks_triggered']}"
      )

    No other changes to main.py.
  </action>

  <verify>
    <automated>py -3.12 -c "import ast, pathlib; ast.parse(pathlib.Path('main.py').read_text()); print('main.py parses OK')"</automated>
  </verify>

  <done>
    main.py parses without syntax errors.
    "from src import llm_engine" appears in the top-level imports block.
    "llm_engine.enrich_with_llm" appears in the Phase 3 section.
    "llm_counts['api_calls_made']" wired into run_log.
    No commented-out Phase 3 stubs remain.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/test_llm_engine.py — full LLM-01 through LLM-09 suite</name>
  <files>tests/test_llm_engine.py</files>

  <read_first>
    - tests/test_risk_engine.py — builder helper pattern (_build_student_row as plain function), caplog pattern, parametrize pattern, source-scan pattern (lines 24-59, 241-268, 445-463, 470-506)
    - tests/conftest.py — dummy API key setdefault; existing fixtures
    - src/config.py — all cfg.COL_* constants to reference in builder and assertions
    - 03-RESEARCH.md §Pattern 4 — respx mock transport injection; _make_tool_response helper; mocking timeout (side_effect=httpx.TimeoutException); mocking 429
    - 03-PATTERNS.md §tests/test_llm_engine.py — imports block, builder helper with all columns, respx fixture usage, source-scan test structure
    - 03-RESEARCH.md §Validation Architecture — exact test function names expected; one test per requirement
    - 03-RESEARCH.md §Pitfall 6 — respx injection must use httpx.Client(transport=respx_mock), not @respx.mock decorator
  </read_first>

  <behavior>
    - test_medium_low_students_skipped: df with MEDIUM + LOW students only → api_calls_made==0, all 4 LLM columns NaN, len(respx_mock.calls)==0 [LLM-01]
    - test_campus_batching: df with students from two campuses (campus A: 1 CRITICAL, campus B: 1 HIGH) → exactly 2 respx_mock.calls (one per campus) [LLM-02]
    - test_tool_use_structured_output: mocked success → generated_by=="llm", facilitator_summary non-empty, whatsapp_message non-empty for CRITICAL student [LLM-03]
    - test_max_retries_config: verify Anthropic client is created with max_retries=3 in production path (monkeypatch Anthropic constructor, capture kwargs) [LLM-04]
    - test_fallback_to_template: respx mocks all calls with timeout side_effect → generated_by=="template", llm_error_reason not None/empty, function does not raise [LLM-05]
    - test_token_logging: mocked success returning input_tokens=150, output_tokens=80 → counts["tokens_used"]["input"]==150, counts["tokens_used"]["output"]==80 [LLM-06]
    - test_api_key_not_in_logs (caplog): call enrich_with_llm with api_key="SUPERSECRETKEY123" → no caplog record contains "SUPERSECRETKEY123" [LLM-07]
    - test_pii_not_in_logs (caplog): student_name="Sensitive Student", parent_phone="0509991111" in df → no caplog record contains either value at DEBUG or above [LLM-08]
    - test_chunk_size_limit: df with 15 CRITICAL students on same campus → exactly 2 API calls (chunks of 10 + 5) [LLM-09]
    - test_llm_disabled_uses_templates (monkeypatch LLM_ENABLED=False via monkeypatch.setattr(cfg,"LLM_ENABLED",False)): CRITICAL + HIGH students → 0 API calls, all generated_by=="template", all llm_error_reason=="llm_disabled" [D-09/LLM-05]
    - test_malformed_tool_response: mock returns valid HTTP 200 but tool input lacks "students" key → generated_by=="template", llm_error_reason=="malformed_response", fallbacks_triggered==1 [LLM-05]
    - test_no_bare_column_strings_in_llm_engine (source scan): read src/llm_engine.py, strip docstrings and comments, regex for bare lowercase column-like strings → none found outside allowed set [CLAUDE.md/LLM-08]
  </behavior>

  <action>
    Create tests/test_llm_engine.py as a new file. Structure (directives, no fenced code):

    FILE-LEVEL DOCSTRING: "Tests for src/llm_engine.py — covers LLM-01 through LLM-09. Each test maps to requirement ID in its docstring. All tests use respx-mocked Anthropic client injected via http_client= parameter. No real API calls are made."

    IMPORTS: logging, pathlib.Path, re, httpx, respx, pytest, anthropic, pandas as pd, from src import config as cfg, from src.llm_engine import enrich_with_llm

    CONSTANT: ANTHROPIC_API_URL = "https://api.anthropic.com/v1/messages"

    HELPER: _build_student_row(...) — plain function (not fixture), keyword args with defaults:
    - student_id="S0001", campus_id="C01", risk_level="CRITICAL", risk_score=85.0,
      attendance_rate=0.3, avg_practice_questions=2.0, trend_direction="declining",
      days_since_last_note=20.0, recommended_action="Contact parent immediately"
    - Returns dict with ALL columns output by score_risk(): student_id, student_name, campus_id,
      parent_phone, facilitator_email, risk_level, risk_score, attendance_rate,
      avg_practice_questions, trend_direction, days_since_last_note, recommended_action,
      attendance_component, practice_component, trend_component, notes_component
    - Use cfg.COL_* for all dict keys
    - student_name="Test Student", parent_phone="0501234567" (present in df; must not reach API or logs)

    HELPER: _make_tool_response(students: list[dict]) -> dict
    - Returns the full Anthropic API JSON response shape with stop_reason="tool_use",
      usage={"input_tokens":150,"output_tokens":80}, content with one tool_use block
    - name="generate_interventions", input={"students": students}

    TEST FUNCTION PATTERN: docstring mentions req ID(s); uses respx_mock fixture;
    calls enrich_with_llm with http_client=httpx.Client(transport=respx_mock);
    single focused assertion per requirement; assertion failure messages are descriptive.

    Write all 12 test functions from the behavior block above.

    For test_api_key_not_in_logs and test_pii_not_in_logs: use caplog.at_level(logging.DEBUG, logger="src.llm_engine"); for PII test use LLM_ENABLED=false path via monkeypatch.setattr(cfg,"LLM_ENABLED",False) to avoid respx setup complexity.

    For test_max_retries_config: use monkeypatch to patch anthropic.Anthropic; capture the kwargs passed to the constructor; assert max_retries==3 when http_client=None.

    For test_malformed_tool_response: mock returns httpx.Response(200, json={"id":"msg_test","type":"message","role":"assistant","model":cfg.ANTHROPIC_MODEL,"stop_reason":"tool_use","stop_sequence":None,"usage":{"input_tokens":10,"output_tokens":5},"content":[{"type":"tool_use","id":"toolu_bad","name":"generate_interventions","input":{"wrong_key":[]}}]}); assert fallbacks_triggered==1.

    For test_no_bare_column_strings_in_llm_engine: read src/llm_engine.py, strip triple-quoted docstrings via re.sub(r'""".*?"""',"",flags=re.DOTALL) then strip comments via re.sub(r"#.*",""), extract quoted lowercase strings >=4 chars with re.findall(r'"([a-z][a-z0-9_]{3,})"',cleaned), compare against allowed set {"llm","template","llm_disabled","timeout","rate_limit","malformed_response","max_retries_exceeded","students","tool","name","utf"}.
  </action>

  <verify>
    <automated>py -3.12 -m pytest tests/test_llm_engine.py -x -q</automated>
  </verify>

  <done>
    tests/test_llm_engine.py exists with all 12 test functions.
    py -3.12 -m pytest tests/test_llm_engine.py -x -q passes with 0 failures.
    py -3.12 -m pytest tests/ -q passes with 0 failures (full suite: 53 existing + all new LLM tests).
    Each of LLM-01 through LLM-09 has at least one passing test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main.py → llm_engine | Passes cfg.ANTHROPIC_API_KEY — key must not be logged downstream |
| test suite → mock transport | respx intercepts at transport layer; real API key never used in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | main.py logger.info LLM completion message | mitigate | Logs only api_calls and fallbacks counts — no key, no student identifiers |
| T-03-09 | Spoofing | tests/conftest.py dummy API key | accept | conftest.py uses setdefault (does not override real key if set); tests use respx mock so key is never transmitted |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all packages already installed in Wave 1 |
</threat_model>

<verification>
After all tasks:

1. py -3.12 -m pytest tests/test_llm_engine.py -v — all 12 new tests GREEN with test names visible
2. py -3.12 -m pytest tests/ -q — full suite (53 original + all new tests) GREEN, 0 failures
3. py -3.12 -c "import ast, pathlib; ast.parse(pathlib.Path('main.py').read_text()); print('syntax OK')"
4. grep -c "llm_engine.enrich_with_llm" main.py — prints 1
5. grep -c "llm_counts\[" main.py — prints 3 (one for each run_log key updated)
</verification>

<success_criteria>
- main.py: "from src import llm_engine" in top-level imports; "df, llm_counts = llm_engine.enrich_with_llm(df, cfg.ANTHROPIC_API_KEY)" replaces Phase 3 comment stub; run_log["api_calls_made"], run_log["tokens_used"], run_log["fallbacks_triggered"] all assigned from llm_counts
- tests/test_llm_engine.py: 12 test functions; each has a docstring referencing its LLM-* requirement; all pass with 0 failures
- Full test suite: py -3.12 -m pytest tests/ -q exits 0 with 0 failures and 0 errors
- Coverage: LLM-01 (medium/low skipped), LLM-02 (campus batching), LLM-03 (structured output), LLM-04 (max_retries), LLM-05 (template fallback + malformed), LLM-06 (token logging), LLM-07 (API key not logged), LLM-08 (PII masking + source scan), LLM-09 (chunk size limit) — all nine IDs provably tested
</success_criteria>

<output>
Create .planning/phases/03-claude-api-integration/03-03-SUMMARY.md when done.
</output>
