---
phase: 03-claude-api-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/config.py
  - src/llm_templates.yaml
  - .planning/STATE.md
autonomous: true
requirements:
  - LLM-04
  - LLM-07
  - LLM-09

must_haves:
  truths:
    - "PyYAML==6.0.3 and respx==0.23.1 are in requirements.txt and installed under py -3.12"
    - "src/config.py exports ANTHROPIC_MODEL, LLM_ENABLED, MAX_TOKENS, TEMPERATURE, TIMEOUT_SECONDS, COL_FACILITATOR_SUMMARY, COL_WHATSAPP_MESSAGE, COL_GENERATED_BY, COL_LLM_ERROR_REASON"
    - "src/llm_templates.yaml loads without error and contains CRITICAL and HIGH top-level keys, each with facilitator_summary and whatsapp_message sub-keys"
    - "Template strings contain Python format placeholders (e.g., {student_id}, {risk_score}) that survive yaml.safe_load() intact"
    - "STATE.md module contract reflects tuple return type for enrich_with_llm"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned dependencies including PyYAML==6.0.3 and respx==0.23.1"
      contains: "PyYAML==6.0.3"
    - path: "src/config.py"
      provides: "All 9 D-09 constants"
      exports:
        - "ANTHROPIC_MODEL"
        - "LLM_ENABLED"
        - "MAX_TOKENS"
        - "TEMPERATURE"
        - "TIMEOUT_SECONDS"
        - "COL_FACILITATOR_SUMMARY"
        - "COL_WHATSAPP_MESSAGE"
        - "COL_GENERATED_BY"
        - "COL_LLM_ERROR_REASON"
    - path: "src/llm_templates.yaml"
      provides: "CRITICAL and HIGH fallback template strings"
      contains: "CRITICAL"
  key_links:
    - from: "src/llm_engine.py"
      to: "src/llm_templates.yaml"
      via: "Path(__file__).parent / 'llm_templates.yaml' loaded at module import"
      pattern: "llm_templates\\.yaml"
    - from: "src/llm_engine.py"
      to: "src/config.py"
      via: "from src import config as cfg"
      pattern: "cfg\\.ANTHROPIC_MODEL|cfg\\.LLM_ENABLED|cfg\\.MAX_TOKENS"
---

<objective>
Install missing dependencies and lay down all static prerequisites Wave 1 depends on: two new pinned packages, nine D-09 config constants, the YAML fallback template file, and the updated STATE.md module contract.

Purpose: Wave 1 (llm_engine.py implementation) cannot proceed without the config constants it imports or the YAML file it loads at module import time. Establishing these first eliminates import errors and keeps implementation plans focused on logic.

Output: requirements.txt with PyYAML==6.0.3 + respx==0.23.1, nine new constants in src/config.py, src/llm_templates.yaml with verified format strings, STATE.md with updated contract.
</objective>

<execution_context>
@C:\Users\abdul\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\abdul\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-claude-api-integration/03-CONTEXT.md
@.planning/phases/03-claude-api-integration/03-RESEARCH.md
@.planning/phases/03-claude-api-integration/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add PyYAML and respx to requirements.txt and install</name>
  <files>requirements.txt</files>

  <read_first>
    - requirements.txt — current pinned deps (7 lines); append two lines only, preserve existing content
    - 03-RESEARCH.md §Environment Availability — confirms PyYAML==6.0.3 not installed under py3.12, respx==0.23.1 confirmed present on PyPI
    - 03-PATTERNS.md §requirements.txt — exact append format: name==version, no extras
  </read_first>

  <action>
    Append two lines to requirements.txt after the existing 7 entries, using the same pinned name==version format:

      PyYAML==6.0.3
      respx==0.23.1

    Then install both into the Python 3.12 environment:

      py -3.12 -m pip install PyYAML==6.0.3 respx==0.23.1

    No other changes to requirements.txt. Do not reorder existing entries.
  </action>

  <verify>
    <automated>py -3.12 -m pip show PyYAML | findstr "Version" && py -3.12 -m pip show respx | findstr "Version"</automated>
  </verify>

  <done>requirements.txt contains PyYAML==6.0.3 and respx==0.23.1 as pinned entries; py -3.12 -m pip show confirms both installed with correct versions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 9 D-09 constants to src/config.py and create src/llm_templates.yaml</name>
  <files>src/config.py, src/llm_templates.yaml</files>

  <read_first>
    - src/config.py — full file; Tunables block ends at line 36, column constants block ends at line 86; insertion points are after line 36 (LLM tunables) and after line 86 (LLM output columns)
    - 03-CONTEXT.md §D-09 — exact constant names, types, env-var keys, and default values
    - 03-PATTERNS.md §src/config.py — os.getenv pattern for optional config; LLM_ENABLED bool-from-string idiom
    - 03-RESEARCH.md §Pattern 5 — YAML block scalar structure and pitfall about curly braces
    - 03-RESEARCH.md §Pitfall 5 — use >- block scalars so {format_placeholders} are not misread by YAML parser
  </read_first>

  <action>
    In src/config.py, add two new blocks:

    BLOCK A — after the existing Tunables block (after MAX_STUDENTS_PER_LLM_CALL on line 36), insert:

      # LLM tunables — all env-overridable, safe defaults (D-09)
      ANTHROPIC_MODEL: str = os.getenv("ANTHROPIC_MODEL", "claude-sonnet-4-5")
      LLM_ENABLED: bool = os.getenv("LLM_ENABLED", "true").lower() == "true"
      MAX_TOKENS: int = int(os.getenv("MAX_TOKENS", "1024"))
      TEMPERATURE: float = float(os.getenv("TEMPERATURE", "0.3"))
      TIMEOUT_SECONDS: int = int(os.getenv("TIMEOUT_SECONDS", "30"))

    Use os.getenv (not os.environ) — these are optional tunables, not required secrets (per D-08).
    LLM_ENABLED uses .lower() == "true" — NOT bool(os.getenv(...)) which is always True for any non-empty string.

    BLOCK B — after the D-09 component score columns block (after COL_NOTES_COMPONENT on line 86), insert:

      # LLM output columns (Phase 3 — D-06)
      COL_FACILITATOR_SUMMARY: str = "facilitator_summary"
      COL_WHATSAPP_MESSAGE: str = "whatsapp_message"
      COL_GENERATED_BY: str = "generated_by"
      COL_LLM_ERROR_REASON: str = "llm_error_reason"

    Then create src/llm_templates.yaml (new file, co-located with llm_engine.py per D-01).
    Use YAML block scalars (>- notation) for ALL template strings — this prevents the YAML parser from
    misinterpreting curly braces as flow mappings (Pitfall 5).

    The YAML must have exactly two top-level keys: CRITICAL and HIGH.
    Each key must have exactly two sub-keys: facilitator_summary and whatsapp_message.

    facilitator_summary format fields to include: student_id, risk_score, attendance_rate (:.0%),
    avg_practice_questions (:.1f), trend_direction, recommended_action, days_since_last_note (:.0f).
    whatsapp_message: warm, professional tone, under 100 words, no risk scores mentioned.

    After creating the file, verify format placeholders survived yaml.safe_load():
      py -3.12 -c "import yaml; from pathlib import Path; t = yaml.safe_load(Path('src/llm_templates.yaml').read_text()); assert '{attendance_rate:.0%}' in t['CRITICAL']['facilitator_summary'], 'YAML format string broken'; print('YAML OK')"
  </action>

  <verify>
    <automated>py -3.12 -c "from src import config as cfg; assert cfg.ANTHROPIC_MODEL == 'claude-sonnet-4-5'; assert cfg.LLM_ENABLED == True; assert cfg.MAX_TOKENS == 1024; assert cfg.TEMPERATURE == 0.3; assert cfg.TIMEOUT_SECONDS == 30; assert cfg.COL_FACILITATOR_SUMMARY == 'facilitator_summary'; assert cfg.COL_WHATSAPP_MESSAGE == 'whatsapp_message'; assert cfg.COL_GENERATED_BY == 'generated_by'; assert cfg.COL_LLM_ERROR_REASON == 'llm_error_reason'; print('config OK')"</automated>
  </verify>

  <done>
    src/config.py exports all 9 D-09 constants with correct types and defaults; import assertion passes.
    src/llm_templates.yaml loads via yaml.safe_load() with CRITICAL and HIGH keys intact and format placeholders preserved (verified via py -3.12 -c assertion).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md module contract to tuple return type</name>
  <files>.planning/STATE.md</files>

  <read_first>
    - .planning/STATE.md §Session Continuity — current module contract line reads: "llm_engine.enrich_with_llm(df, api_key) -> DataFrame — never raises"
    - 03-RESEARCH.md §Pattern 6 — tuple return rationale; df.attrs fragility (Pitfall 4)
    - 03-PATTERNS.md §Critical Implementation Notes — A6 resolution: update STATE.md contract, update main.py lines 74-78 in plan 03-03
  </read_first>

  <action>
    In .planning/STATE.md, locate the Module contracts section under Session Continuity.
    Find the line:
      "llm_engine.enrich_with_llm(df, api_key) -> DataFrame — never raises"

    Replace it with:
      "llm_engine.enrich_with_llm(df, api_key, http_client=None) -> tuple[DataFrame, dict] — never raises; dict keys: api_calls_made (int), tokens_used (dict[str,int]), fallbacks_triggered (int)"

    Also update the "Last updated" line at the bottom of STATE.md to:
      "*Last updated: 2026-05-23 after Phase 3 planning (03-01 prerequisites — tuple return contract locked, D-09 constants added)*"

    No other changes to STATE.md.
  </action>

  <verify>
    <automated>py -3.12 -c "import pathlib; s = pathlib.Path('.planning/STATE.md').read_text(); assert 'tuple[DataFrame, dict]' in s, 'Contract not updated'; assert 'http_client=None' in s, 'http_client param missing'; print('STATE.md OK')"</automated>
  </verify>

  <done>STATE.md module contract line for enrich_with_llm reflects tuple[DataFrame, dict] return type and optional http_client parameter; py -3.12 -c assertion passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → config.py | Optional tunable values read from process environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | src/config.py ANTHROPIC_API_KEY | mitigate | Already uses os.environ["ANTHROPIC_API_KEY"] (fail-loud); never logged — existing control preserved |
| T-03-02 | Tampering | src/llm_templates.yaml format strings | mitigate | yaml.safe_load() (not yaml.load()) prevents code execution; .format_map() with known keys only — no eval() |
| T-03-SC | Tampering | pip install PyYAML==6.0.3 respx==0.23.1 | accept | Both packages verified on PyPI registry — PyYAML 15+ years, respx 5+ years; tagged [ASSUMED] per protocol but risk is negligible |
</threat_model>

<verification>
After all three tasks:

1. py -3.12 -m pytest tests/ -q — all 53 existing tests still GREEN (no regression from config additions)
2. py -3.12 -c "from src import config as cfg; print(cfg.ANTHROPIC_MODEL, cfg.LLM_ENABLED, cfg.MAX_TOKENS, cfg.COL_GENERATED_BY)" — prints expected defaults
3. py -3.12 -c "import yaml; t = yaml.safe_load(open('src/llm_templates.yaml')); assert set(t.keys()) == {'CRITICAL','HIGH'}; assert 'facilitator_summary' in t['CRITICAL']; print('templates OK')"
4. grep "tuple\[DataFrame, dict\]" .planning/STATE.md — exits 0
</verification>

<success_criteria>
- requirements.txt: 9 lines total — original 7 + PyYAML==6.0.3 + respx==0.23.1
- src/config.py: 5 new LLM tunables (ANTHROPIC_MODEL, LLM_ENABLED, MAX_TOKENS, TEMPERATURE, TIMEOUT_SECONDS) + 4 new column constants (COL_FACILITATOR_SUMMARY, COL_WHATSAPP_MESSAGE, COL_GENERATED_BY, COL_LLM_ERROR_REASON)
- src/llm_templates.yaml: CRITICAL and HIGH keys, each with facilitator_summary (uses >- block scalar with format placeholders) and whatsapp_message (under 100 words, no risk scores)
- .planning/STATE.md: enrich_with_llm contract shows tuple return and http_client parameter
- py -3.12 -m pytest tests/ -q: 53 tests, 0 failures
</success_criteria>

<output>
Create .planning/phases/03-claude-api-integration/03-01-SUMMARY.md when done.
</output>
