---
phase: "06-documentation-suite"
plan: "04"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/doc_generator.py
autonomous: true
requirements:
  - DOCS-03
  - DOCS-06
  - DOCS-07

must_haves:
  truths:
    - "docs/architecture.docx exists and contains sections: Pipeline Overview, Component Descriptions, Technology Choices, Why Not n8n/Zapier or Airtable"
    - "docs/data_handling.docx exists and contains sections covering input schema, cleaning, missing data, merge logic"
    - "docs/scalability.docx exists and contains a programmatic cost projection table (add_table style='Table Grid')"
    - "docs/scalability.docx contains text about $200/month budget and CSV → SQLite → PostgreSQL migration path"
    - "All three .docx files open without error in python-docx Document()"
    - "Hybrid structure used: each section has at least one paragraph, bullet items rendered as list items"
    - "doc.save(str(path)) used in all three helpers"
  artifacts:
    - path: "docs/architecture.docx"
      provides: "Pipeline architecture documentation with ASCII diagram"
      contains: "Pipeline Overview"
    - path: "docs/data_handling.docx"
      provides: "Input schema, cleaning steps, edge case handling"
      contains: "Input Schema"
    - path: "docs/scalability.docx"
      provides: "Scale comparison, migration path, cost projection table"
      contains: "Cost Projection"
  key_links:
    - from: "_write_architecture / _write_data_handling / _write_scalability"
      to: "docs_content.yaml sections"
      via: "content['sections'] loop with type:paragraph → add_paragraph, type:bullet → add_paragraph with '• ' prefix"
      pattern: "content\\[.sections"
---

<objective>
Implement the three hybrid-style static doc helpers: _write_architecture, _write_data_handling,
and _write_scalability. These share a common rendering pattern (YAML content → docx) but
scalability adds a programmatic cost projection table not in the YAML.

Purpose: Hybrid docs are reference material — paragraph opener per section, then bullets.
All three share the same YAML renderer loop, so implementing them together avoids duplicating
that logic across plans.
Output: Three working helpers replacing Wave 1 stubs, producing architecture.docx,
data_handling.docx, and scalability.docx.
</objective>

<execution_context>
@C:/Users/abdul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/PROJECT.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/ROADMAP.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/STATE.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/phases/06-documentation-suite/06-CONTEXT.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/src/doc_generator.py
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/src/output_generator.py
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/src/templates/docs_content.yaml

<interfaces>
<!-- Helper signatures (D-09): -->
<!--
def _write_architecture(docs_dir: Path, content: dict) -> Path:
def _write_data_handling(docs_dir: Path, content: dict) -> Path:
def _write_scalability(docs_dir: Path, content: dict) -> Path:
    # content = _DOCS_CONTENT["architecture"] / ["data_handling"] / ["scalability"]
    # content["title"] -> str (document title)
    # content["sections"] -> list of {heading: str, body: list[{type: str, text: str}]}
-->

<!-- Shared YAML → docx rendering pattern: -->
<!--
doc = Document()
doc.add_heading(content["title"], level=0)         # Document title
for section in content.get("sections", []):
    doc.add_heading(section["heading"], level=1)   # Section heading
    for item in section.get("body", []):
        if item["type"] == "paragraph":
            doc.add_paragraph(item["text"])
        elif item["type"] == "bullet":
            doc.add_paragraph(f"• {item['text']}")  # unicode bullet prefix
doc.save(str(path))
-->

<!-- scalability cost projection table (DOCS-07, CONTEXT.md specifics): -->
<!--
Scale comparison table — add_table(rows=3, cols=3, style="Table Grid"):
  Headers: "Scale", "At-Risk Students/Run", "Est. Tokens/Run"
  Row 1:   "20 campuses (current)", "~120", "~12,720"   # 300 students × 40% = 120; 120×106
  Row 2:   "100 campuses",          "~600", "~63,600"   # 100×15×40% = 600; 600×106

Cost table — add_table(rows=3, cols=3, style="Table Grid"):
  Headers: "Runs/Month", "Tokens/Run", "Est. Monthly Cost"
  Row 1 (daily):   "30",  "~12,720",  "<$1/month"       # current scale
  Row 2 (daily):   "30",  "~63,600",  "~$5-10/month"    # 100 campuses
  # Note: claude-sonnet-4-5 pricing; exact cost depends on current API pricing
  # State that both scenarios are well within the $200/month budget
-->

<!-- python-docx table pattern (from output_generator.py): -->
<!--
table = doc.add_table(rows=N, cols=M, style="Table Grid")
table.cell(0, 0).text = "Header"
table.cell(1, 0).text = "Value"
# No OxmlElement, no custom styles — only "Table Grid"
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement _write_architecture and _write_data_handling (YAML renderer loop)</name>
  <files>src/doc_generator.py</files>
  <read_first>
    - src/doc_generator.py — read the full current file; locate _write_architecture and _write_data_handling stubs
    - src/templates/docs_content.yaml — read the architecture and data_handling sections to understand the exact structure being rendered
    - src/output_generator.py lines 195-230 — _write_report() docx doc.save(str(path)) pattern and table style
    - .planning/phases/06-documentation-suite/06-CONTEXT.md §D-10, D-14 — python-docx constraints, hybrid content depth
  </read_first>
  <action>
    Replace the _write_architecture and _write_data_handling stubs with working implementations.

    Both helpers follow the same pattern: receive content dict from _DOCS_CONTENT, render it to a docx.

    _write_architecture(docs_dir: Path, content: dict) -> Path:
    - path = docs_dir / "architecture.docx"
    - doc = Document()
    - doc.add_heading(content["title"], level=0) for the document title
    - Loop over content.get("sections", []): for each section, doc.add_heading(section["heading"], level=1), then loop body items: type:paragraph → doc.add_paragraph(item["text"]); type:bullet → doc.add_paragraph(f"• {item['text']}")
    - doc.save(str(path))
    - logger.info("Wrote architecture.docx: %s", path)
    - Return path

    _write_data_handling(docs_dir: Path, content: dict) -> Path:
    - Identical rendering pattern to _write_architecture.
    - path = docs_dir / "data_handling.docx"
    - Same loop: title at level=0, section headings at level=1, paragraph/bullet body items.
    - doc.save(str(path)); logger.info; return path.

    Add docstrings to both: describe the hybrid structure (paragraph opener + bullets), reference D-14.
    Note in each docstring: "No OxmlElement. No custom styles. Table Grid only. per D-10."
  </action>
  <verify>
    <automated>python -c "
import tempfile
from pathlib import Path
from docx import Document
import sys
sys.path.insert(0, '.')
import src.doc_generator as dg

with tempfile.TemporaryDirectory() as tmp:
    docs_dir = Path(tmp) / 'docs'
    docs_dir.mkdir()
    content = {
        'title': 'Test Arch',
        'sections': [
            {'heading': 'Overview', 'body': [
                {'type': 'paragraph', 'text': 'Intro paragraph.'},
                {'type': 'bullet', 'text': 'Bullet point one'},
            ]},
        ]
    }
    p = dg._write_architecture(docs_dir, content)
    assert p.exists()
    doc = Document(str(p))
    texts = [para.text for para in doc.paragraphs]
    assert any('Intro paragraph' in t for t in texts), f'Missing paragraph: {texts}'
    assert any('Bullet point one' in t for t in texts), f'Missing bullet: {texts}'
    p2 = dg._write_data_handling(docs_dir, content)
    assert p2.exists()
    print('architecture.docx and data_handling.docx OK')
"</automated>
  </verify>
  <done>
    _write_architecture and _write_data_handling replace stubs; both render content["title"] as level=0 heading, section headings as level=1, paragraphs as add_paragraph text, bullets as add_paragraph with "• " prefix; doc.save(str(path)) used; logger.info called; file exists on disk after call.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement _write_scalability with programmatic cost projection table</name>
  <files>src/doc_generator.py</files>
  <read_first>
    - src/doc_generator.py — read after Task 1 completes; locate _write_scalability stub
    - src/templates/docs_content.yaml — read the scalability section to see what YAML content exists and where the table should be inserted
    - src/output_generator.py lines 230-250 — add_table(rows, cols, style="Table Grid") and cell text assignment pattern
    - .planning/phases/06-documentation-suite/06-CONTEXT.md §D-10, DOCS-07 specifics — $200/month anchor, migration path, table requirement
  </read_first>
  <action>
    Replace the _write_scalability stub with a working implementation.

    _write_scalability(docs_dir: Path, content: dict) -> Path:
    - path = docs_dir / "scalability.docx"
    - doc = Document()
    - doc.add_heading(content["title"], level=0)
    - Render all sections from content["sections"] using the same loop as Task 1 (paragraph → add_paragraph, bullet → add_paragraph with "• ").
    - After rendering the YAML content sections, add a heading "Cost Projection" at level=1, then add a paragraph explaining the calculation basis (106 tokens/student derived from 31,771 tokens ÷ 300 students in demo run).
    - Add a scale comparison table: doc.add_table(rows=3, cols=3, style="Table Grid"). Row 0 headers: "Scale", "At-Risk Students/Run", "Est. Tokens/Run". Row 1: "20 campuses (current)", "~120", "~12,720". Row 2: "100 campuses", "~600", "~63,600".
    - Add a paragraph after the table stating both scenarios are well within the $200/month budget.
    - doc.save(str(path))
    - logger.info("Wrote scalability.docx: %s", path)
    - Return path

    Docstring: describe the hybrid + table structure, note the cost projection numbers are hardcoded from the demo run (D-13), reference DOCS-07.
  </action>
  <verify>
    <automated>python -c "
import tempfile
from pathlib import Path
from docx import Document
import sys
sys.path.insert(0, '.')
import src.doc_generator as dg

with tempfile.TemporaryDirectory() as tmp:
    docs_dir = Path(tmp) / 'docs'
    docs_dir.mkdir()
    content = {
        'title': 'Scalability',
        'sections': [
            {'heading': 'Current Scale', 'body': [
                {'type': 'paragraph', 'text': 'Handles 20 campuses.'},
                {'type': 'bullet', 'text': '300 students total'},
            ]},
        ]
    }
    p = dg._write_scalability(docs_dir, content)
    assert p.exists(), f'File not found: {p}'
    doc = Document(str(p))
    texts = ' '.join(para.text for para in doc.paragraphs)
    assert 'Cost Projection' in texts, 'Missing Cost Projection heading'
    assert '200' in texts, 'Missing \$200/month reference'
    # Verify table exists
    assert len(doc.tables) >= 1, f'Expected at least 1 table, got {len(doc.tables)}'
    table = doc.tables[0]
    assert table.cell(0,0).text == 'Scale', f'Bad table header: {table.cell(0,0).text}'
    print(f'scalability.docx OK — {len(doc.tables)} table(s)')
"</automated>
  </verify>
  <done>
    _write_scalability replaces stub; renders YAML sections with hybrid pattern; adds "Cost Projection" heading and scale comparison table with style="Table Grid"; table has 3 rows × 3 cols with correct headers; $200/month appears in paragraph text; doc.save(str(path)) used; file exists on disk.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content dict → docx | Sourced from _DOCS_CONTENT (static YAML loaded at import) — no user input |
| docs_dir (Path) | Internal path from cfg.DOCS_DIR — no traversal beyond docs_dir / filename |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | scalability.docx cost table | accept | Numbers are hardcoded estimates from demo run, not real billing data; no API key or student data |
| T-06-07 | Tampering | content dict body items | accept | Sourced from static YAML file, not user-supplied; item["type"] check prevents unexpected rendering |
</threat_model>

<verification>
After plan executes, run from project root:
  python -c "
import tempfile
from pathlib import Path
from docx import Document
import src.doc_generator as dg

with tempfile.TemporaryDirectory() as tmp:
    docs_dir = Path(tmp) / 'docs'
    docs_dir.mkdir()
    import src.doc_generator as dg
    content_arch = dg._DOCS_CONTENT['architecture']
    content_dh = dg._DOCS_CONTENT['data_handling']
    content_sc = dg._DOCS_CONTENT['scalability']
    p1 = dg._write_architecture(docs_dir, content_arch)
    p2 = dg._write_data_handling(docs_dir, content_dh)
    p3 = dg._write_scalability(docs_dir, content_sc)
    for p in [p1, p2, p3]:
        doc = Document(str(p))
        print(p.name, '— paragraphs:', len(doc.paragraphs))
  "
Expected: three files printed with paragraph counts > 0, no exception.
</verification>

<success_criteria>
- _write_architecture produces docs/architecture.docx
- _write_data_handling produces docs/data_handling.docx
- _write_scalability produces docs/scalability.docx with at least 1 table (style="Table Grid")
- All three files open without error via Document(str(path))
- Hybrid rendering: type:paragraph → doc.add_paragraph(text); type:bullet → doc.add_paragraph("• " + text)
- scalability.docx cost table headers: "Scale", "At-Risk Students/Run", "Est. Tokens/Run"
- "$200" or "200/month" appears in scalability.docx paragraph text
- "CSV" and "PostgreSQL" appear in scalability.docx (migration path present)
- doc.save(str(path)) used in all three — not doc.save(path)
- No OxmlElement usage anywhere
</success_criteria>

<output>
Create .planning/phases/06-documentation-suite/06-04-SUMMARY.md when done
</output>
