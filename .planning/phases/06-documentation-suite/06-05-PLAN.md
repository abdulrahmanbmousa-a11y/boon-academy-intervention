---
phase: "06-documentation-suite"
plan: "05"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/doc_generator.py
autonomous: true
requirements:
  - DOCS-04
  - DOCS-05
  - DOCS-08
  - DOCS-09

must_haves:
  truths:
    - "docs/security.docx exists and explicitly covers env-var-only API key handling"
    - "docs/security.docx explicitly covers PII masking of student names and parent phones in logs"
    - "docs/security.docx includes a data retention recommendation"
    - "docs/engineering_decisions.docx exists and covers: risk scoring formula rationale, LLM batching rationale, fallback logic, output format choices, intentional simplicity"
    - "docs/system_design.docx exists and covers AI role boundaries, human review loop, failure modes"
    - "docs/alternatives.docx exists and covers risk scoring alternatives, delivery alternatives, infrastructure alternatives, and 'What Is Worth Building Next'"
    - "All four .docx files open without error in python-docx Document()"
    - "Narrative structure used: 2-4 type:paragraph entries per section, minimal bullets"
  artifacts:
    - path: "docs/security.docx"
      provides: "API key management, PII handling, data retention, access control documentation"
      contains: "API Key"
    - path: "docs/engineering_decisions.docx"
      provides: "Technical decision rationale for all major choices"
      contains: "Risk Scoring"
    - path: "docs/system_design.docx"
      provides: "AI role, tradeoffs, human review loop, failure modes"
      contains: "Human Review"
    - path: "docs/alternatives.docx"
      provides: "What was not built and why, alternative approaches evaluated"
      contains: "Alternatives"
  key_links:
    - from: "four narrative helpers"
      to: "docs_content.yaml sections"
      via: "content['sections'] loop identical to hybrid helpers in plan 06-04"
      pattern: "content\\[.sections"
---

<objective>
Implement the four narrative-style static doc helpers: _write_security, _write_engineering_decisions,
_write_system_design, and _write_alternatives.

Purpose: These four docs share the same YAML-to-docx rendering loop as the hybrid docs in plan 06-04,
but their content is entirely type:paragraph (narrative prose). Implementing them together ensures a
consistent rendering approach and avoids plan-boundary file conflicts.
Output: Four working helpers replacing Wave 1 stubs.
</objective>

<execution_context>
@C:/Users/abdul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/PROJECT.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/ROADMAP.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/STATE.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/phases/06-documentation-suite/06-CONTEXT.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/src/doc_generator.py
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/src/output_generator.py
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/src/templates/docs_content.yaml

<interfaces>
<!-- Helper signatures (D-09): -->
<!--
def _write_security(docs_dir: Path, content: dict) -> Path:
def _write_engineering_decisions(docs_dir: Path, content: dict) -> Path:
def _write_system_design(docs_dir: Path, content: dict) -> Path:
def _write_alternatives(docs_dir: Path, content: dict) -> Path:
    # content = _DOCS_CONTENT["security"] / ["engineering_decisions"] / ["system_design"] / ["alternatives"]
-->

<!-- Shared rendering pattern (identical to hybrid docs, plan 06-04 Task 1): -->
<!--
doc = Document()
doc.add_heading(content["title"], level=0)
for section in content.get("sections", []):
    doc.add_heading(section["heading"], level=1)
    for item in section.get("body", []):
        if item["type"] == "paragraph":
            doc.add_paragraph(item["text"])
        elif item["type"] == "bullet":
            doc.add_paragraph(f"• {item['text']}")
doc.save(str(path))
-->

<!-- Output filenames: -->
<!--
security           → docs_dir / "security.docx"
engineering_decisions → docs_dir / "engineering_decisions.docx"
system_design      → docs_dir / "system_design.docx"
alternatives       → docs_dir / "alternatives.docx"
-->

<!-- Key content requirements from REQUIREMENTS.md: -->
<!--
DOCS-04 security.docx must: describe env-var-only API key handling (os.environ["ANTHROPIC_API_KEY"]),
  PII masking (names and phones masked in all logs), data retention recommendation,
  access control recommendation, what must NOT appear in outputs.

DOCS-05 engineering_decisions.docx must: risk scoring formula rationale, LLM batching rationale,
  fallback logic rationale, output format choices, why Claude API, what was left intentionally simple.

DOCS-08 system_design.docx must: AI choices and why, what is NOT done by AI and why,
  LLM accuracy vs cost vs latency tradeoffs, human review loop recommendation, failure modes.

DOCS-09 alternatives.docx must: what was NOT built with rationale, alternative risk scoring approaches,
  alternative delivery methods, what's worth building next.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement _write_security and _write_engineering_decisions</name>
  <files>src/doc_generator.py</files>
  <read_first>
    - src/doc_generator.py — read the full current file; locate _write_security and _write_engineering_decisions stubs
    - src/templates/docs_content.yaml — read security and engineering_decisions sections to understand the exact content being rendered
    - .planning/REQUIREMENTS.md §DOCS-04, DOCS-05 — exact topics each doc must cover
    - .planning/phases/06-documentation-suite/06-CONTEXT.md §D-10, D-14, specifics — narrative structure, security doc specifics
  </read_first>
  <action>
    Replace the _write_security and _write_engineering_decisions stubs.

    Both use the identical YAML rendering loop (same pattern as _write_architecture in plan 06-04 Task 1):
    doc.add_heading(content["title"], level=0), loop sections, add_heading(level=1), loop body items
    (type:paragraph → add_paragraph, type:bullet → add_paragraph with "• " prefix), doc.save(str(path)).

    _write_security(docs_dir: Path, content: dict) -> Path:
    - path = docs_dir / "security.docx"
    - Docstring: "Writes security.docx from docs_content.yaml. Covers env-var API key management,
      PII masking, data retention, access control. Narrative prose (D-14). No OxmlElement (D-10)."
    - Apply the YAML renderer loop.
    - doc.save(str(path)); logger.info("Wrote security.docx: %s", path); return path.

    _write_engineering_decisions(docs_dir: Path, content: dict) -> Path:
    - path = docs_dir / "engineering_decisions.docx"
    - Docstring: "Writes engineering_decisions.docx from docs_content.yaml. Covers risk scoring
      formula rationale, LLM batching, fallback logic, output format choices, intentional simplicity.
      Narrative prose (D-14). No OxmlElement (D-10)."
    - Apply the YAML renderer loop.
    - doc.save(str(path)); logger.info("Wrote engineering_decisions.docx: %s", path); return path.
  </action>
  <verify>
    <automated>python -c "
import tempfile
from pathlib import Path
from docx import Document
import sys
sys.path.insert(0, '.')
import src.doc_generator as dg

with tempfile.TemporaryDirectory() as tmp:
    docs_dir = Path(tmp) / 'docs'
    docs_dir.mkdir()
    content = {
        'title': 'Security',
        'sections': [
            {'heading': 'API Key Management', 'body': [
                {'type': 'paragraph', 'text': 'Keys are loaded via os.environ.'},
                {'type': 'paragraph', 'text': 'Never logged or hardcoded.'},
            ]},
            {'heading': 'PII Handling', 'body': [
                {'type': 'paragraph', 'text': 'Student names masked in logs.'},
            ]},
        ]
    }
    p_sec = dg._write_security(docs_dir, content)
    assert p_sec.exists()
    doc = Document(str(p_sec))
    texts = ' '.join(p.text for p in doc.paragraphs)
    assert 'API Key Management' in texts
    assert 'os.environ' in texts or 'Keys are loaded' in texts
    assert 'PII Handling' in texts

    content2 = {'title': 'Engineering Decisions', 'sections': [
        {'heading': 'Risk Scoring Formula', 'body': [{'type': 'paragraph', 'text': 'Deterministic weighted formula.'}]}
    ]}
    p_ed = dg._write_engineering_decisions(docs_dir, content2)
    assert p_ed.exists()
    print('security.docx and engineering_decisions.docx OK')
"</automated>
  </verify>
  <done>
    _write_security produces docs/security.docx; _write_engineering_decisions produces docs/engineering_decisions.docx; both open without error in Document(); section headings rendered as level=1; paragraphs rendered via add_paragraph(); doc.save(str(path)) used; logger.info called.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement _write_system_design and _write_alternatives</name>
  <files>src/doc_generator.py</files>
  <read_first>
    - src/doc_generator.py — read after Task 1 completes; locate _write_system_design and _write_alternatives stubs
    - src/templates/docs_content.yaml — read system_design and alternatives sections
    - .planning/REQUIREMENTS.md §DOCS-08, DOCS-09 — exact topics each doc must cover
  </read_first>
  <action>
    Replace the _write_system_design and _write_alternatives stubs.

    Both use the same YAML renderer loop as Tasks above.

    _write_system_design(docs_dir: Path, content: dict) -> Path:
    - path = docs_dir / "system_design.docx"
    - Docstring: "Writes system_design.docx from docs_content.yaml. Covers AI role boundaries,
      what AI does not do, accuracy/cost/latency tradeoffs, human review loop recommendation,
      failure modes. Narrative prose (D-14). No OxmlElement (D-10)."
    - Apply YAML renderer loop.
    - doc.save(str(path)); logger.info("Wrote system_design.docx: %s", path); return path.

    _write_alternatives(docs_dir: Path, content: dict) -> Path:
    - path = docs_dir / "alternatives.docx"
    - Docstring: "Writes alternatives.docx from docs_content.yaml. Covers risk scoring alternatives,
      delivery method alternatives, infrastructure alternatives, and what is worth building next.
      Narrative prose (D-14). No OxmlElement (D-10)."
    - Apply YAML renderer loop.
    - doc.save(str(path)); logger.info("Wrote alternatives.docx: %s", path); return path.
  </action>
  <verify>
    <automated>python -c "
import tempfile
from pathlib import Path
from docx import Document
import sys
sys.path.insert(0, '.')
import src.doc_generator as dg

with tempfile.TemporaryDirectory() as tmp:
    docs_dir = Path(tmp) / 'docs'
    docs_dir.mkdir()
    content_sd = {
        'title': 'System Design',
        'sections': [
            {'heading': 'AI Role and Boundaries', 'body': [
                {'type': 'paragraph', 'text': 'AI generates summaries for CRITICAL and HIGH students.'},
            ]},
            {'heading': 'Human Review Loop', 'body': [
                {'type': 'paragraph', 'text': 'Facilitator reviews before acting.'},
            ]},
        ]
    }
    p_sd = dg._write_system_design(docs_dir, content_sd)
    assert p_sd.exists()
    doc_sd = Document(str(p_sd))
    texts_sd = ' '.join(p.text for p in doc_sd.paragraphs)
    assert 'Human Review Loop' in texts_sd

    content_alt = {
        'title': 'Alternatives',
        'sections': [
            {'heading': 'Risk Scoring Alternatives', 'body': [
                {'type': 'paragraph', 'text': 'ML model viable after 6 months of labeled data.'},
            ]},
        ]
    }
    p_alt = dg._write_alternatives(docs_dir, content_alt)
    assert p_alt.exists()
    doc_alt = Document(str(p_alt))
    texts_alt = ' '.join(p.text for p in doc_alt.paragraphs)
    assert 'Risk Scoring Alternatives' in texts_alt
    print('system_design.docx and alternatives.docx OK')
"</automated>
  </verify>
  <done>
    _write_system_design produces docs/system_design.docx; _write_alternatives produces docs/alternatives.docx; both open without error in Document(); section headings rendered; paragraph body rendered; doc.save(str(path)) used; logger.info called.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content dict → docx paragraph text | Sourced from static YAML — no user input, no secrets |
| docs_dir / filename | Internal path from cfg.DOCS_DIR — filenames are hardcoded, not derived from user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Information Disclosure | security.docx content | mitigate | YAML content must NOT include actual ANTHROPIC_API_KEY value or any real student data — verified by content authoring in plan 06-01 |
| T-06-09 | Tampering | content["sections"] loop | accept | content sourced from module-level _DOCS_CONTENT (static, trusted); no runtime user data flows into the loop |
</threat_model>

<verification>
After plan executes:
  python -c "
import tempfile
from pathlib import Path
from docx import Document
import src.doc_generator as dg

with tempfile.TemporaryDirectory() as tmp:
    docs_dir = Path(tmp) / 'docs'
    docs_dir.mkdir()
    for key, fn in [('security','security.docx'),('engineering_decisions','engineering_decisions.docx'),
                    ('system_design','system_design.docx'),('alternatives','alternatives.docx')]:
        content = dg._DOCS_CONTENT[key]
        writer = getattr(dg, f'_write_{key}')
        p = writer(docs_dir, content)
        doc = Document(str(p))
        print(fn, '— paragraphs:', len(doc.paragraphs), '— tables:', len(doc.tables))
  "
Expected: all 4 files print with paragraph counts > 0, no exception.
</verification>

<success_criteria>
- _write_security produces docs/security.docx — content includes "API key", "PII", and "retention" topics
- _write_engineering_decisions produces docs/engineering_decisions.docx — content includes risk scoring formula and LLM batching rationale topics
- _write_system_design produces docs/system_design.docx — content includes human review loop and failure modes topics
- _write_alternatives produces docs/alternatives.docx — content includes alternative risk scoring and delivery alternatives topics
- All four files open without error via Document(str(path))
- doc.save(str(path)) used in all four (not doc.save(path))
- No OxmlElement in any helper
- No print() statements; all logging via logger.info()
- Type hints and docstrings on all four functions
</success_criteria>

<output>
Create .planning/phases/06-documentation-suite/06-05-SUMMARY.md when done
</output>
