---
phase: "06-documentation-suite"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/docs_content.yaml
autonomous: true
requirements:
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06
  - DOCS-07
  - DOCS-08
  - DOCS-09

must_haves:
  truths:
    - "docs_content.yaml exists in src/templates/ and is valid YAML loadable with yaml.safe_load()"
    - "Every static doc key is present at top level: architecture, security, engineering_decisions, data_handling, scalability, system_design, alternatives"
    - "Each key has a title (str) and sections (list), each section has heading (str) and body (list of type+text dicts)"
    - "body items use only type: paragraph or type: bullet — no other values"
    - "architecture, data_handling, scalability sections each lead with a paragraph then use bullets (hybrid pattern D-14)"
    - "security, engineering_decisions, system_design, alternatives sections use 2-4 paragraphs per section (narrative pattern D-14)"
    - "scalability doc includes cost projection anchored to $200/month and migration path CSV → SQLite → PostgreSQL"
    - "security doc explicitly covers env-var-only API key handling, PII masking in logs, and data retention recommendation"
  artifacts:
    - path: "src/templates/docs_content.yaml"
      provides: "All 7 static documentation doc content"
      contains: "architecture:"
  key_links:
    - from: "src/doc_generator.py (_write_architecture, etc.)"
      to: "src/templates/docs_content.yaml"
      via: "yaml.safe_load() at module import"
      pattern: "yaml\\.safe_load"
---

<objective>
Author the complete content for all 7 static documentation files in a single YAML file that
doc_generator.py will load at import time.

Purpose: Content authoring is separated from code implementation so both can be written and
reviewed independently (Wave 1 parallel). All static doc text is maintained in one place.
Output: src/templates/docs_content.yaml with full, accurate content for all 7 .docx files.
</objective>

<execution_context>
@C:/Users/abdul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/PROJECT.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/ROADMAP.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/STATE.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/phases/06-documentation-suite/06-CONTEXT.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/src/config.py
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/src/llm_engine.py
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/src/output_generator.py

<interfaces>
<!-- YAML schema contract (D-06): -->
<!--
each_doc_key:
  title: "Document Title"
  sections:
    - heading: "Section Heading"
      body:
        - type: paragraph
          text: "Narrative text..."
        - type: bullet
          text: "Bullet point text..."
-->
<!-- D-14 content depth rules:
  - narrative docs (security, engineering_decisions, system_design, alternatives):
      each section body = 2-4 type:paragraph entries
  - hybrid docs (architecture, data_handling, scalability):
      each section body = 1 type:paragraph opener, then type:bullet entries
-->
<!-- D-13 real pipeline numbers for reference (embed in relevant sections):
  300 students, 20 campuses (C01-C20, 15 students each)
  14 API calls, 31,771 tokens (19,693 input + 12,078 output)
  0 fallbacks, 0 errors, 9 duplicate IDs removed
-->
<!-- scalability cost formula (from CONTEXT.md specifics):
  31,771 tokens / 300 students ≈ 106 tokens/student
  100 campuses × 15 students × 40% CRITICAL/HIGH = 600 at-risk students
  600 × 106 tokens = 63,600 tokens per run → project monthly cost vs $200/month budget
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs_content.yaml — architecture, data_handling, scalability (hybrid docs)</name>
  <files>src/templates/docs_content.yaml</files>
  <read_first>
    - src/config.py — all COL_* constants, weight constants, RISK_THRESHOLD_*, module structure
    - src/output_generator.py — what each module does, pipeline flow, output files list
    - .planning/REQUIREMENTS.md §DOCS-03, DOCS-06, DOCS-07 — exact topics each doc must cover
    - .planning/phases/06-documentation-suite/06-CONTEXT.md §D-06, D-14 — YAML schema, content depth rules
  </read_first>
  <action>
    Create src/templates/docs_content.yaml. Write the three hybrid-style docs: architecture, data_handling, scalability.

    Use YAML >- block scalars for all text values to avoid misinterpretation of any special characters.

    architecture (DOCS-03) — sections: "Pipeline Overview", "Component Descriptions", "Technology Choices", "Why Not n8n/Zapier or Airtable". Each section: one type:paragraph opener, then type:bullet entries. Include ASCII diagram of the 5-stage pipeline (ingestion → risk_engine → llm_engine → output_generator → doc_generator) as a bullet under Pipeline Overview. Include per-module bullet descriptions (when it runs, why it exists). Include the LLM call location (llm_engine.py, campus-batched) and why. Technology choice bullets: pandas 2.2.3, openpyxl 3.1.5, python-docx 1.1.2, PyYAML 6.0.3, anthropic 0.103.1, respx for testing.

    data_handling (DOCS-06) — sections: "Input Schema", "Data Cleaning Pipeline", "Missing Data Strategy", "Edge Cases and Quality Issues", "Merge Logic". Each section: one paragraph opener, then bullets listing concrete column names, steps, or rules drawn from src/config.py COL_* constants and ingestion.py behavior described in STATE.md.

    scalability (DOCS-07) — sections: "Current Scale", "Bottlenecks at 100 Campuses", "Migration Path", "Cost Projection". "Current Scale" and "Bottlenecks" use hybrid (paragraph + bullets). "Migration Path" uses paragraph + bullets for CSV → SQLite → PostgreSQL steps. "Cost Projection" uses a paragraph with the calculation: 31,771 tokens ÷ 300 students ≈ 106 tokens/student; at 100 campuses × 15 students × 40% at-risk rate = 600 at-risk students; 600 × 106 = 63,600 tokens/run. At daily runs, anchor to $200/month budget and state the current scale (300 students) is well within it. No programmatic table needed — the table is generated in _write_scalability(); put the numbers in the paragraph text here.
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('src/templates/docs_content.yaml',encoding='utf-8')); assert all(k in d for k in ['architecture','data_handling','scalability']); [assert s.get('heading') and s.get('body') for doc in ['architecture','data_handling','scalability'] for s in d[doc]['sections']] ; print('hybrid docs OK')"</automated>
  </verify>
  <done>
    architecture, data_handling, and scalability keys exist in docs_content.yaml; each has title and sections; every section has heading and body; body items all have type (paragraph or bullet) and text; yaml.safe_load() loads without error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author docs_content.yaml — security, engineering_decisions, system_design, alternatives (narrative docs)</name>
  <files>src/templates/docs_content.yaml</files>
  <read_first>
    - src/templates/docs_content.yaml — read the existing content written in Task 1 before appending
    - .planning/REQUIREMENTS.md §DOCS-04, DOCS-05, DOCS-08, DOCS-09 — exact topics each doc must cover
    - .planning/phases/06-documentation-suite/06-CONTEXT.md §D-14, specifics — content depth and security doc specifics
    - src/llm_engine.py — PII masking implementation, three-layer fallback, batching rationale
  </read_first>
  <action>
    Append the four narrative-style docs to src/templates/docs_content.yaml. Use YAML >- block scalars. Each section body uses 2-4 type:paragraph entries (no bullets unless one bullet set per section is needed to enumerate a list that prose cannot cleanly express).

    security (DOCS-04) — sections: "API Key Management", "PII Handling", "Data Retention", "Access Control". "API Key Management": explain env-var-only loading via os.environ["ANTHROPIC_API_KEY"] (fail-loud at import time), that the key is never logged, never written to any output file, never embedded in prompts. "PII Handling": explain that student names and parent phones are masked at INFO/DEBUG level in llm_engine.py before any log statement; student data is never sent to external services except the Anthropic API in anonymised campus-cohort form. "Data Retention": recommend not persisting student data beyond the run day; outputs/ contains PII and should be treated as sensitive; recommend deletion after facilitator review. "Access Control": single-machine execution model; recommend restricting outputs/ directory read access to the facilitator role.

    engineering_decisions (DOCS-05) — sections: "Risk Scoring Formula", "LLM Batching Strategy", "Fallback Logic", "Output Format Choices", "Intentional Simplicity". Each section 2-3 paragraphs explaining the why behind each choice. Risk scoring: deterministic weighted formula, no ML, auditable and explainable, weights are unvalidated starting points needing academic director review. LLM batching: one call per campus (not per student) reduces API calls from 300 to 14 in the demo run; preserves cohort context for the model. Fallback logic: three layers (SDK retry, re-prompt, rule-based template); pipeline never halts; outputs are labeled generated_by:template so facilitators know which messages are AI vs rule-based. Output formats: Excel for facilitators (openpyxl, not xlsxwriter — read+write needed for tests), CSV for WhatsApp copy-paste, self-contained HTML for offline use, Word for reports. Intentional simplicity: no Docker, no auth server, no real-time dashboard — daily batch run is sufficient for the use case at current scale.

    system_design (DOCS-08) — sections: "AI Role and Boundaries", "What AI Does Not Do", "Accuracy vs Cost vs Latency Tradeoffs", "Human Review Loop", "Failure Modes". Each section 2-3 paragraphs. AI Role: generates 2-sentence facilitator summaries and WhatsApp messages for CRITICAL/HIGH students; does not score risk (that is deterministic). What AI Does Not Do: does not decide who to contact, does not send messages, does not access real student systems. Tradeoffs: claude-sonnet-4-5 chosen for quality/cost balance; campus batching reduces cost ~10x vs per-student calls; latency acceptable for daily batch. Human Review: facilitator reviews intervention_priority_list.xlsx before acting; WhatsApp messages are drafts, not sent automatically; facilitator judgment is the final gate. Failure Modes: API down → rule-based templates (labeled); bad CSV → logged warnings, pipeline continues; empty campus → skipped gracefully.

    alternatives (DOCS-09) — sections: "Risk Scoring Alternatives", "Delivery Method Alternatives", "Infrastructure Alternatives", "What Is Worth Building Next". Each section 2-3 paragraphs. Risk Scoring Alternatives: ML model (viable after 6 months of labeled outcome data, not now), rule-based thresholds only without LLM (faster but no personalized messaging), third-party engagement analytics platforms (vendor lock-in, cost). Delivery Alternatives: n8n/Zapier (hard to debug, can't version-control, breaks at scale), Airtable (paid, vendor lock-in, no offline use), direct WhatsApp Business API (carrier approval required, cost per message). Infrastructure Alternatives: Docker (over-engineered for 1 part-time engineer at 20 campuses), scheduled cloud job (viable at 50+ campuses), SQLite persistence (migration path, not needed yet). What Is Worth Building Next: real student data hookup, risk weight calibration with academic director, dialect selection per campus, scheduled daily trigger.
  </action>
  <verify>
    <automated>python -c "
import yaml
d = yaml.safe_load(open('src/templates/docs_content.yaml', encoding='utf-8'))
required = ['architecture','security','engineering_decisions','data_handling','scalability','system_design','alternatives']
missing = [k for k in required if k not in d]
assert not missing, f'Missing keys: {missing}'
for k in required:
    assert 'title' in d[k], f'{k} missing title'
    assert 'sections' in d[k], f'{k} missing sections'
    for s in d[k]['sections']:
        assert 'heading' in s, f'{k} section missing heading'
        assert 'body' in s, f'{k} section missing body'
        for item in s['body']:
            assert item.get('type') in ('paragraph','bullet'), f'{k} bad type: {item}'
            assert 'text' in item, f'{k} body item missing text'
print('ALL 7 docs validated OK')
"</automated>
  </verify>
  <done>
    All 7 top-level keys present in docs_content.yaml; yaml.safe_load() succeeds; every section has heading and body; every body item has type in (paragraph, bullet) and a non-empty text string; security doc mentions env-var API key handling and PII masking; scalability doc mentions $200/month and CSV → SQLite → PostgreSQL migration path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML file → yaml.safe_load() | Static file authored by Claude, loaded at Python import time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | docs_content.yaml | accept | Static content file, no user input, no secrets embedded; any edit is visible in git diff |
| T-06-SC | Tampering | yaml.safe_load() | mitigate | yaml.safe_load() (not yaml.load()) prevents code execution from YAML — enforced in Task 2 verify step |
</threat_model>

<verification>
Run from project root:
  python -c "import yaml; d=yaml.safe_load(open('src/templates/docs_content.yaml',encoding='utf-8')); print(list(d.keys()))"
Expected output: ['architecture', 'security', 'engineering_decisions', 'data_handling', 'scalability', 'system_design', 'alternatives'] (order may vary)

All 7 keys present, each with title and sections, no yaml.load() usage anywhere in the file.
</verification>

<success_criteria>
- src/templates/docs_content.yaml exists and loads with yaml.safe_load() without error
- All 7 top-level doc keys are present
- Every section has heading (str) and body (list)
- Every body item has type in (paragraph, bullet) and text (non-empty str)
- security section body covers env-var-only API key, PII masking, data retention
- scalability section body covers $200/month budget anchor and CSV → SQLite → PostgreSQL path
- No yaml.load() (unsafe) usage anywhere in the file
- No secrets, API keys, or real student data embedded
</success_criteria>

<output>
Create .planning/phases/06-documentation-suite/06-01-SUMMARY.md when done
</output>
