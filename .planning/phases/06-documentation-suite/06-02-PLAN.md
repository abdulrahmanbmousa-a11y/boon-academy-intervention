---
phase: "06-documentation-suite"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/doc_generator.py
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06
  - DOCS-07
  - DOCS-08
  - DOCS-09

must_haves:
  truths:
    - "src/doc_generator.py exists with write_docs() public function"
    - "write_docs() signature is write_docs(df: pd.DataFrame, run_log: dict, docs_dir: Path) -> dict[str, Path]"
    - "Module loads docs_content.yaml at import time via Path(__file__).parent / 'templates' / 'docs_content.yaml'"
    - "All 9 private helpers exist with correct signatures and return type Path"
    - "All functions have type hints and docstrings on public functions"
    - "logger = logging.getLogger(__name__) present — zero print statements"
    - "docs_dir.mkdir(parents=True, exist_ok=True) called at top of write_docs()"
  artifacts:
    - path: "src/doc_generator.py"
      provides: "Module skeleton with write_docs() orchestrator and 9 private helper stubs"
      contains: "def write_docs"
  key_links:
    - from: "src/doc_generator.py (module import)"
      to: "src/templates/docs_content.yaml"
      via: "yaml.safe_load()"
      pattern: "yaml\\.safe_load"
    - from: "src/doc_generator.py (write_docs)"
      to: "src/config.py"
      via: "from src import config as cfg"
      pattern: "import config as cfg"
---

<objective>
Create the src/doc_generator.py module with the public write_docs() orchestrator and all 9 private
helper stubs. Helpers return placeholder Paths so the module is importable and testable before
implementations are filled in Wave 2.

Purpose: Separating skeleton creation from implementation (Wave 1 parallel with YAML content
authoring) means Wave 2 plans can implement helpers independently without conflicts.
Output: src/doc_generator.py with correct signatures, docstrings, YAML load, and stub returns.
</objective>

<execution_context>
@C:/Users/abdul/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/abdul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/PROJECT.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/ROADMAP.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/STATE.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/.planning/phases/06-documentation-suite/06-CONTEXT.md
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/src/output_generator.py
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/src/llm_engine.py
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/src/config.py
@C:/Users/abdul/Desktop/NOON ACADEMY ASSIGNEMTN/main.py

<interfaces>
<!-- Public function contract (D-01, D-03): -->
<!--
def write_docs(df: pd.DataFrame, run_log: dict, docs_dir: Path) -> dict[str, Path]:
    Return keys: analysis_md, analysis_docx, architecture, security,
                 engineering_decisions, data_handling, scalability, system_design, alternatives
-->

<!-- Private helper signatures (D-09): -->
<!--
def _write_analysis_md(df: pd.DataFrame, run_log: dict, docs_dir: Path) -> Path:
    # writes analysis.md at project root — use Path(docs_dir).parent / "analysis.md"
    # or cfg.PROJECT_ROOT / "analysis.md" if that constant exists; fallback: Path("analysis.md")

def _write_analysis_docx(run_log: dict, docs_dir: Path, content: dict) -> Path:
    # writes docs/analysis.docx using python-docx

def _write_architecture(docs_dir: Path, content: dict) -> Path:
def _write_security(docs_dir: Path, content: dict) -> Path:
def _write_engineering_decisions(docs_dir: Path, content: dict) -> Path:
def _write_data_handling(docs_dir: Path, content: dict) -> Path:
def _write_scalability(docs_dir: Path, content: dict) -> Path:
def _write_system_design(docs_dir: Path, content: dict) -> Path:
def _write_alternatives(docs_dir: Path, content: dict) -> Path:
-->

<!-- YAML load pattern (D-05) — from src/llm_engine.py: -->
<!--
_TEMPLATE_PATH = Path(__file__).parent / "templates" / "docs_content.yaml"
with _TEMPLATE_PATH.open(encoding="utf-8") as fh:
    _DOCS_CONTENT: dict = yaml.safe_load(fh)
-->

<!-- Module-level discipline (from src/output_generator.py): -->
<!--
import logging
logger = logging.getLogger(__name__)
# No print() statements anywhere
# All column names via cfg.COL_*
# doc.save(str(path))  # str() required for python-docx 1.1.2 on Windows
-->

<!-- From src/config.py: relevant constants for doc_generator -->
<!--
COL_RISK_LEVEL: str = "risk_level"
DOCS_DIR: Path = Path(os.getenv("DOCS_DIR", "docs"))  # line 30 — no change needed
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/doc_generator.py skeleton with YAML load and all stubs</name>
  <files>src/doc_generator.py</files>
  <read_first>
    - src/output_generator.py — module docstring style, logger pattern, helper stub discipline, doc.save(str(path)) pattern
    - src/llm_engine.py lines 1-50 — YAML load pattern at module import (_TEMPLATE_PATH, yaml.safe_load)
    - src/config.py — COL_RISK_LEVEL, DOCS_DIR (line 30), confirm no PROJECT_ROOT constant exists
    - .planning/phases/06-documentation-suite/06-CONTEXT.md §D-01, D-05, D-08, D-09, D-12 — exact signatures and discipline
  </read_first>
  <action>
    Create src/doc_generator.py. Structure:

    Module docstring: describe Phase 6, write_docs() entry point, 9 private helpers, YAML content file.
    Reference D-01 (signature), D-05 (YAML load pattern), D-08 (module discipline), D-09 (helper list).

    Imports: logging, pathlib.Path, typing (no Optional needed yet), pandas as pd, yaml, docx.Document, from src import config as cfg.

    Module-level YAML load (D-05): define _CONTENT_PATH = Path(__file__).parent / "templates" / "docs_content.yaml" and load into _DOCS_CONTENT: dict = yaml.safe_load(_CONTENT_PATH.open(encoding="utf-8")).

    logger = logging.getLogger(__name__)

    write_docs() public function:
    - Docstring: describe purpose, Args (df, run_log, docs_dir), Returns dict keys.
    - First line: docs_dir.mkdir(parents=True, exist_ok=True)  [D-12]
    - Call all 9 helpers and collect paths into a dict with the 9 keys from D-03.
    - logger.info("docs written: %s", list(paths.keys()))
    - Return paths dict.

    For _write_analysis_md: signature is _write_analysis_md(df: pd.DataFrame, run_log: dict, docs_dir: Path) -> Path.
    analysis.md goes at project root. Since there is no cfg.PROJECT_ROOT constant, derive the path as docs_dir.parent / "analysis.md". This works because docs_dir defaults to Path("docs") and the pipeline runs from the project root.

    For _write_analysis_docx: signature is _write_analysis_docx(run_log: dict, docs_dir: Path, content: dict) -> Path.

    For the 7 static helpers (_write_architecture through _write_alternatives): signature is (docs_dir: Path, content: dict) -> Path.

    All stubs: add a TODO docstring and return docs_dir / "{name}.docx" (or the analysis.md path). Stubs must NOT raise NotImplementedError — return the path so the orchestrator can run without crashing.

    Do NOT implement any of the helpers in this plan — that is Wave 2 work. Stubs only.
  </action>
  <verify>
    <automated>python -c "
from unittest.mock import MagicMock, patch
import pandas as pd
from pathlib import Path
import tempfile, os

# Patch yaml.safe_load at import to avoid requiring the YAML file
with patch('yaml.safe_load', return_value={
    'architecture': {'title':'T','sections':[]},
    'security': {'title':'T','sections':[]},
    'engineering_decisions': {'title':'T','sections':[]},
    'data_handling': {'title':'T','sections':[]},
    'scalability': {'title':'T','sections':[]},
    'system_design': {'title':'T','sections':[]},
    'alternatives': {'title':'T','sections':[]},
}), patch('builtins.open', MagicMock()):
    import importlib, sys
    if 'src.doc_generator' in sys.modules:
        del sys.modules['src.doc_generator']
    import src.doc_generator as dg

# Check public function exists with correct signature
import inspect
sig = inspect.signature(dg.write_docs)
params = list(sig.parameters.keys())
assert params == ['df', 'run_log', 'docs_dir'], f'Bad params: {params}'
assert sig.return_annotation != inspect.Parameter.empty, 'Missing return annotation'

# Check all 9 private helpers exist
helpers = ['_write_analysis_md','_write_analysis_docx','_write_architecture',
           '_write_security','_write_engineering_decisions','_write_data_handling',
           '_write_scalability','_write_system_design','_write_alternatives']
for h in helpers:
    assert hasattr(dg, h), f'Missing helper: {h}'
print('Skeleton OK — all signatures and helpers present')
"</automated>
  </verify>
  <done>
    src/doc_generator.py exists; python -c "import src.doc_generator" succeeds (assuming yaml file present or patched); write_docs has correct 3-parameter signature with return type dict[str, Path]; all 9 private helpers exist; logger = logging.getLogger(__name__) present; zero print() statements; YAML loaded at module import via yaml.safe_load().
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs_dir (Path) | Caller-supplied directory path — validated via mkdir, not traversed |
| run_log (dict) | In-memory dict built by main.py — trusted internal data, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | doc_generator.py | mitigate | No ANTHROPIC_API_KEY or student PII in any logger.* call — stubs log path only |
| T-06-03 | Tampering | _DOCS_CONTENT load | mitigate | yaml.safe_load() (not yaml.load()); _CONTENT_PATH is file-adjacent, not user-supplied |
| T-06-SC | Tampering | yaml import | mitigate | slopcheck: verify yaml.safe_load used (not yaml.load) in final file |
</threat_model>

<verification>
After both Wave 1 plans complete:
  python -c "import src.doc_generator; print('import OK')"

Expected: "import OK" with no exception. If docs_content.yaml is not yet written (plan 06-01 not executed), the import will fail at _CONTENT_PATH.open() — that is expected; both Wave 1 plans must complete before Wave 2 executes.
</verification>

<success_criteria>
- src/doc_generator.py exists
- Module imports without error when docs_content.yaml is present
- write_docs(df, run_log, docs_dir) -> dict[str, Path] signature is correct
- All 9 private helpers present with correct parameter signatures
- logger = logging.getLogger(__name__) at module level
- _DOCS_CONTENT loaded via yaml.safe_load() at import time
- docs_dir.mkdir(parents=True, exist_ok=True) is the first line of write_docs()
- No print() statements anywhere in the file
- Type hints on all functions
- Docstring on write_docs()
</success_criteria>

<output>
Create .planning/phases/06-documentation-suite/06-02-SUMMARY.md when done
</output>
