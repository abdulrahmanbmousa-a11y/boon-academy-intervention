---
phase: 07-test-suite
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - .planning/phases/07-test-suite/07-01-BASELINE.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - "A baseline pytest run (py -3.12 -m pytest tests/ -v) is captured and every failure is documented before any test code is written or fixed"
    - "tests/conftest.py imports src.config as cfg at module scope"
    - "tests/conftest.py exposes a function-scoped minimal_enriched_df fixture that returns a pd.DataFrame with 5 rows across 2 campuses using only cfg.COL_* constants as column names"
    - "minimal_enriched_df includes at least one CRITICAL row in each campus so downstream LLM/output tests can exercise per-campus batching and per-level color coding"
  artifacts:
    - path: ".planning/phases/07-test-suite/07-01-BASELINE.md"
      provides: "Captured stdout of the baseline pytest run plus a categorized failure list (TEST-01..04 gaps vs other failures, per D-02)"
      contains: "## Baseline Failures"
    - path: "tests/conftest.py"
      provides: "minimal_enriched_df fixture + cfg import; reused by 07-02 and 07-03"
      contains: "minimal_enriched_df"
  key_links:
    - from: "tests/conftest.py"
      to: "src/config.py"
      via: "from src import config as cfg"
      pattern: "from src import config as cfg"
    - from: ".planning/phases/07-test-suite/07-01-BASELINE.md"
      to: "07-02-PLAN.md and 07-03-PLAN.md"
      via: "Documented failure list categorized by test file — Wave 1 plans must fix only the listed real failures (D-01)"
      pattern: "TEST-0[1-4]"
---

<objective>
Establish the Phase 7 baseline by running the full pytest suite under py -3.12, capturing
every failure verbatim, and categorizing failures by test file and by requirement
(TEST-01..TEST-04 first per D-02). In the same plan, extend tests/conftest.py with the
shared minimal_enriched_df fixture (D-05) that Wave 1 plans 07-02 and 07-03 both depend on.

Purpose: Honour D-01 — Wave 1 plans MUST fix only real failures, not pre-emptively rewrite
passing tests. The baseline document is the source of truth for what is broken vs. missing.
Purpose: Honour D-05 — the shared fixture must exist before any TEST-03 or TEST-04 work
references it, so Wave 1 plans run in parallel without colliding on conftest.py.

Output:
- .planning/phases/07-test-suite/07-01-BASELINE.md (full failure log + categorization)
- tests/conftest.py with cfg import + minimal_enriched_df fixture
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-test-suite/07-CONTEXT.md
@.planning/phases/07-test-suite/07-PATTERNS.md
@CLAUDE.md
@tests/conftest.py
@src/config.py

<interfaces>
Existing conftest.py (tests/conftest.py lines 14-15) already sets the env var:
  os.environ.setdefault("ANTHROPIC_API_KEY", "dummy-key-for-tests")
This must be preserved. Do not move or duplicate this line.

Existing conftest.py imports (lines 1-17) cover: os, shutil, pathlib.Path, pytest.
Missing import to add: from src import config as cfg

cfg.COL_* constants referenced by the new fixture (all already defined in src/config.py):
  COL_STUDENT_ID, COL_STUDENT_NAME, COL_CAMPUS_ID, COL_PARENT_PHONE,
  COL_FACILITATOR_EMAIL, COL_RISK_SCORE, COL_RISK_LEVEL,
  COL_ATTENDANCE_RATE, COL_AVG_PRACTICE, COL_TREND_DIR, COL_DAYS_SINCE_NOTE,
  COL_RECOMMENDED_ACTION, COL_FACILITATOR_SUMMARY, COL_WHATSAPP_MESSAGE,
  COL_GENERATED_BY, COL_LLM_ERROR_REASON,
  COL_ATTENDANCE_COMPONENT, COL_PRACTICE_COMPONENT, COL_TREND_COMPONENT, COL_NOTES_COMPONENT

If any of these constants is absent from src/config.py at the time of execution, STOP and
report — do not invent a constant. The Pattern Map confirms all 20 are present in production.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture baseline pytest run and produce 07-01-BASELINE.md</name>
  <files>.planning/phases/07-test-suite/07-01-BASELINE.md</files>
  <read_first>
    - tests/conftest.py (current fixture set; informs what is already in place)
    - .planning/phases/07-test-suite/07-CONTEXT.md (decision D-01 and D-02 wording)
    - .planning/phases/07-test-suite/07-PATTERNS.md (existing test inventory per file)
  </read_first>
  <action>
    Run `py -3.12 -m pytest tests/ -v --tb=short` from the project root and capture the entire stdout+stderr output.

    Write `.planning/phases/07-test-suite/07-01-BASELINE.md` with this exact structure:

    Section 1: "## Baseline Command" — the literal command executed and the Python/pytest version banner from the run.

    Section 2: "## Summary Line" — the final pytest summary line (e.g. "X passed, Y failed, Z errors in N.NNs").

    Section 3: "## Failures By Test File" — one subsection per failing test file. Under each file, list every failing test name with a one-line failure synopsis (first line of the traceback or AssertionError text). Do NOT paste full tracebacks — one line per failure.

    Section 4: "## TEST-01..TEST-04 Gap Analysis" — for each requirement TEST-01, TEST-02, TEST-03, TEST-04, list:
      - Required test cases per .planning/REQUIREMENTS.md (e.g. for TEST-01: boundary at 75, boundary at 74, all-zeros, perfect student)
      - Whether each required case is present in the codebase (use test function name)
      - For present-but-failing cases: mark "FAIL"
      - For absent cases: mark "MISSING"
      - For present-and-passing cases: mark "OK"
      The two known MISSING cases per 07-PATTERNS.md observation 3 are `test_score_75_is_critical` and `test_score_74_is_high`.

    Section 5: "## Priority Order Per D-02" — bulleted list ordered exactly:
      1. All TEST-01..TEST-04 MISSING and FAIL items from Section 4
      2. All other failures from Section 3 (test_config, test_generate_data, test_package_structure, test_no_hardcoded_paths, etc.)
      Wave 1 plans 07-02 and 07-03 will consume this section.

    Do NOT modify any production source code in this task. Do NOT modify any test code in this task.
  </action>
  <verify>
    <automated>py -3.12 -m pytest tests/ -v --tb=short 2>&amp;1 | tee /tmp/pytest-baseline.txt &amp;&amp; test -s .planning/phases/07-test-suite/07-01-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-test-suite/07-01-BASELINE.md` exists and is non-empty
    - File contains the literal string "## Baseline Command"
    - File contains the literal string "## Failures By Test File"
    - File contains the literal string "## TEST-01..TEST-04 Gap Analysis"
    - File contains the literal string "## Priority Order Per D-02"
    - File contains an explicit entry for `test_score_75_is_critical` marked MISSING
    - File contains an explicit entry for `test_score_74_is_high` marked MISSING
    - The Summary Line section reproduces the actual final-line pytest output verbatim
    - No production source files (src/*.py) were modified during this task (`git diff --name-only src/` returns no output)
  </acceptance_criteria>
  <done>Baseline file written; Wave 1 has an authoritative failure inventory categorized by D-02 priority.</done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/conftest.py with cfg import + minimal_enriched_df fixture (D-05)</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (current state — DO NOT remove or reorder the existing os.environ.setdefault line)
    - src/config.py (verify all 20 cfg.COL_* constants listed in the interfaces block exist)
    - .planning/phases/07-test-suite/07-PATTERNS.md §"tests/conftest.py — add shared minimal DataFrame fixture" (the fixture column structure is specified there)
  </read_first>
  <action>
    Edit tests/conftest.py. Make exactly two changes:

    Change A — Add the import `from src import config as cfg` to the conftest imports block. Place it AFTER the existing `os.environ.setdefault("ANTHROPIC_API_KEY", "dummy-key-for-tests")` line (the env var must be set before src.config is imported, since src.config calls os.environ at module load per D-08 of the project).

    Change B — Append a new fixture named `minimal_enriched_df` to the end of conftest.py. Requirements:
      - Decorator: `@pytest.fixture()` (function scope by default — Pattern Map decision: function scope so output_generator tests can mutate the DataFrame freely)
      - Signature: `def minimal_enriched_df() -> pd.DataFrame:`
      - Docstring: brief description noting it is a 5-row, 2-campus inline fixture covering all 4 risk levels plus an extra CRITICAL row, used by test_output_generator.py and test_llm_engine.py
      - Body: returns `pd.DataFrame({...})` using ONLY cfg.COL_* constants as keys (no bare string column names anywhere)
      - Row count: exactly 5
      - Campus distribution: 3 rows with campus_id="C01", 2 rows with campus_id="C02"
      - Risk level distribution: at least one CRITICAL in each campus (so test_campus_batching gets 2 campus groups). Suggested levels: ["CRITICAL", "CRITICAL", "MEDIUM", "CRITICAL", "LOW"]
      - All 20 cfg.COL_* columns listed in the interfaces block must be present as keys
      - student_id values: "S001", "S002", "S003", "S004", "S005"
      - parent_phone values must be Saudi-format strings starting with "05" (e.g. "0501111111") — never int
      - Component columns (attendance_component, practice_component, trend_component, notes_component) must be floats that approximately sum-via-weights to the corresponding risk_score row (35% + 30% + 20% + 15%)
      - facilitator_summary and whatsapp_message: non-None strings for CRITICAL rows; None for MEDIUM and LOW rows
      - generated_by values: at least one "llm", at least one "template", None for MEDIUM/LOW rows

    Add `import pandas as pd` to the imports block if not already present.

    Do NOT remove or modify any existing fixture in conftest.py. Do NOT change the env-var setdefault line.
  </action>
  <verify>
    <automated>py -3.12 -c "import sys; sys.path.insert(0, '.'); from tests.conftest import minimal_enriched_df" &amp;&amp; py -3.12 -m pytest tests/conftest.py --collect-only -q 2>&amp;1 | grep -i "no tests" ; py -3.12 -m pytest tests/ --collect-only -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` contains the literal string `from src import config as cfg`
    - `tests/conftest.py` contains the literal string `os.environ.setdefault("ANTHROPIC_API_KEY"` (unchanged from before)
    - `tests/conftest.py` contains the literal string `def minimal_enriched_df`
    - `tests/conftest.py` contains the literal string `@pytest.fixture`
    - `tests/conftest.py` contains the literal string `cfg.COL_STUDENT_ID`
    - `tests/conftest.py` contains the literal string `cfg.COL_GENERATED_BY`
    - `tests/conftest.py` contains the literal string `cfg.COL_ATTENDANCE_COMPONENT`
    - `tests/conftest.py` does NOT contain any bare string `"student_id"` or `"risk_score"` as a dict key inside the fixture body (only cfg.COL_* references)
    - `py -3.12 -m pytest tests/ --collect-only -q` completes without ImportError or fixture-resolution error
    - Behavior: a one-off `py -3.12 -c "from tests.conftest import minimal_enriched_df"` import succeeds without raising
    - No production source files were modified (`git diff --name-only src/` returns no output)
  </acceptance_criteria>
  <done>conftest.py has the new fixture and import; Wave 1 plans can reference minimal_enriched_df immediately.</done>
</task>

</tasks>

<verification>
After both tasks complete, the project state must satisfy:
- Baseline failure inventory exists at .planning/phases/07-test-suite/07-01-BASELINE.md
- tests/conftest.py exposes minimal_enriched_df without breaking any currently-passing test collection
- A second invocation of `py -3.12 -m pytest tests/ --collect-only -q` returns the same collection count as the baseline (no new collection errors introduced)
</verification>

<success_criteria>
- 07-01-BASELINE.md present and has all 5 required sections
- conftest.py has cfg import + minimal_enriched_df fixture with 5 rows and 2 campuses
- pytest collection still succeeds under py -3.12
- Zero changes under src/
</success_criteria>

<output>
Create `.planning/phases/07-test-suite/07-01-SUMMARY.md` when done.
</output>
