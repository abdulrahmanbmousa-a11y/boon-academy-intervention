---
phase: 07-test-suite
plan: 03
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - tests/test_llm_engine.py
  - tests/test_output_generator.py
autonomous: true
requirements:
  - TEST-03
  - TEST-04

must_haves:
  truths:
    - "tests/test_llm_engine.py defines test_fallback_to_template which uses respx to simulate an HTTP error from the Anthropic endpoint and asserts every CRITICAL/HIGH row has cfg.COL_GENERATED_BY == 'template' and a non-empty cfg.COL_WHATSAPP_MESSAGE"
    - "tests/test_llm_engine.py defines test_campus_batching which asserts exactly len(respx_mock.calls) == 2 when CRITICAL/HIGH students span 2 distinct campuses AND pins cfg.MAX_STUDENTS_PER_LLM_CALL=10 via monkeypatch to guarantee each campus fits in a single batch"
    - "tests/test_llm_engine.py token-logging test asserts both that counts['tokens_used']['input'] is the expected mock value AND that the input/output token counts appear in caplog.text after a successful mocked API call"
    - "tests/test_output_generator.py defines test_all_6_output_files_exist which calls write_outputs() with tmp_path as output_dir and asserts every Path in the returned dict exists, including the 5 fixed keys priority_list/whatsapp/run_log/dashboard/report"
    - "tests/test_output_generator.py contains at least one PatternFill color assertion comparing cell.fill.fgColor.rgb to cfg.COLOR_CRITICAL where the production constant value is 'FFFFCCCC' (8-character hex with FF alpha prefix as defined in src/config.py line 109)"
    - "py -3.12 -m pytest tests/test_llm_engine.py tests/test_output_generator.py -v exits with code 0"
    - "Combined with 07-02, the full-suite gate `py -3.12 -m pytest tests/ -v` exits 0 — ROADMAP success criterion #1"
  artifacts:
    - path: "tests/test_llm_engine.py"
      provides: "Fallback, campus-batching (with MAX_STUDENTS_PER_LLM_CALL pin), and token-logging tests aligned with D-08 / D-09 / TEST-03"
      contains: "test_fallback_to_template"
    - path: "tests/test_output_generator.py"
      provides: "End-to-end test_all_6_output_files_exist using tmp_path (D-06) plus repaired baseline failures"
      contains: "test_all_6_output_files_exist"
  key_links:
    - from: "tests/test_llm_engine.py::test_fallback_to_template"
      to: "src/llm_engine.py::enrich_with_llm (three-layer fallback)"
      via: "respx HTTP 500 / TimeoutException injection -> generated_by == 'template'"
      pattern: "respx_mock.post.*\\.mock"
    - from: "tests/test_llm_engine.py::test_campus_batching"
      to: "src/llm_engine.py::enrich_with_llm (campus batching loop)"
      via: "respx_mock.calls length equals number of unique campus_ids with CRITICAL/HIGH students; MAX_STUDENTS_PER_LLM_CALL pinned to 10 via monkeypatch"
      pattern: "len\\(respx_mock\\.calls\\)"
    - from: "tests/test_output_generator.py::test_all_6_output_files_exist"
      to: "src/output_generator.py::write_outputs"
      via: "write_outputs(df, tmp_path, run_log) returns dict[str, Path]; assert each path.exists()"
      pattern: "write_outputs\\("
---

<objective>
Close all TEST-03 and TEST-04 gaps identified in 07-01-BASELINE.md.

For TEST-03 (Claude API integration tests):
- Ensure test_fallback_to_template exists and uses respx to simulate API failure, asserting generated_by == "template" and non-empty whatsapp_message for every CRITICAL/HIGH row (D-08)
- Ensure test_campus_batching exists and asserts exactly len(respx_mock.calls) == 2 for 2 distinct campuses (D-09), with cfg.MAX_STUDENTS_PER_LLM_CALL pinned via monkeypatch so per-campus students fit in a single batch (eliminating false failures if the env-driven default ever drops below the test's per-campus row count)
- Ensure token-logging test asserts both counts dict values AND caplog.text contains the token numbers (CONTEXT.md §TEST-03 requires the caplog assertion which is not in the existing test per 07-PATTERNS.md observation lines 144-162)
- Fix any other test_llm_engine.py failures flagged in 07-01-BASELINE.md

For TEST-04 (output generator tests):
- Add test_all_6_output_files_exist (D-06) — calls write_outputs() end-to-end with tmp_path and asserts all returned Paths exist
- Verify the PatternFill color assertion is present and compares against cfg.COLOR_CRITICAL whose production value is "FFFFCCCC" (8-character hex with FF alpha prefix, per src/config.py line 109)
- Fix any other test_output_generator.py failures flagged in 07-01-BASELINE.md

Purpose: Deliver requirements TEST-03 and TEST-04, completing ROADMAP success criteria #4 and #5.
Purpose: Combined with 07-02, deliver ROADMAP success criterion #1 (full pytest suite exits 0).

Output:
- tests/test_llm_engine.py with fallback/batching/token-logging tests aligned to D-08/D-09/TEST-03
- tests/test_output_generator.py with test_all_6_output_files_exist and verified PatternFill color check
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-test-suite/07-CONTEXT.md
@.planning/phases/07-test-suite/07-PATTERNS.md
@.planning/phases/07-test-suite/07-01-BASELINE.md
@CLAUDE.md
@src/llm_engine.py
@src/output_generator.py
@src/config.py
@tests/test_llm_engine.py
@tests/test_output_generator.py
@tests/conftest.py

<interfaces>
enrich_with_llm signature (src/llm_engine.py, ALREADY has http_client per 07-PATTERNS.md observation 1):
  def enrich_with_llm(
      df: pd.DataFrame,
      api_key: str,
      http_client: Optional[httpx.Client] = None,
  ) -> tuple[pd.DataFrame, dict]
  counts dict keys: api_calls_made (int), tokens_used (dict with keys "input" and "output"),
                    fallbacks_triggered (int)

respx injection pattern (07-PATTERNS.md "Shared Patterns" + tests/test_llm_engine.py lines 112-115):
  http_client = httpx.Client(transport=httpx.MockTransport(respx_mock.handler))
  result_df, counts = enrich_with_llm(df, "test-key", http_client=http_client)

ANTHROPIC_API_URL constant (defined at top of tests/test_llm_engine.py — verify before reuse)

write_outputs signature (src/output_generator.py):
  def write_outputs(df: pd.DataFrame, output_dir: Path, run_log: dict) -> dict[str, Path]
  Return dict has keys: priority_list, campus_<id_1>, campus_<id_2>, whatsapp,
                         run_log, dashboard, report

minimal_enriched_df fixture (added in 07-01-PLAN Task 2; available via conftest.py):
  5-row DataFrame across 2 campuses (C01 x3, C02 x2) with all 20 cfg.COL_* columns

cfg.COLOR_CRITICAL constant (src/config.py line 109):
  COLOR_CRITICAL: str = "FFFFCCCC"
The production value is "FFFFCCCC" (8-character ARGB hex with FF alpha prefix matching
openpyxl's PatternFill.fgColor.rgb return value). All other Phase 4 color constants
(COLOR_HIGH="FFFFE5CC", COLOR_MEDIUM="FFFFFFCC", COLOR_LOW="FFCCFFCC", COLOR_HEADER="FF1F4E79",
FONT_WHITE="FFFFFFFF") share the FF alpha prefix convention. Every Phase 7 assertion that
references the literal color value MUST use "FFFFCCCC", NOT "00FFCCCC". Tests should compare
against `cfg.COLOR_CRITICAL` (the constant) rather than hardcoding the literal string.

cfg.MAX_STUDENTS_PER_LLM_CALL constant (src/config.py line 36):
  MAX_STUDENTS_PER_LLM_CALL: int = int(os.getenv("MAX_STUDENTS_PER_LLM_CALL", "10"))
The default is 10 (env-overridable). Tests that depend on "one API call per campus" MUST
pin this constant via monkeypatch so they remain deterministic regardless of the env value
at test-time and regardless of any future default change.

Platform note: Windows 11 / PowerShell. All verify commands below are PowerShell-safe:
no /tmp, no `tee`, no `test -s`, no `grep -c`, no `2>&1` redirection of native exes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align tests/test_llm_engine.py with D-08, D-09 (with MAX_STUDENTS pin), and TEST-03 caplog requirement</name>
  <files>tests/test_llm_engine.py</files>
  <read_first>
    - tests/test_llm_engine.py (current file — locate existing test_campus_batching ~lines 134-178, test_fallback_to_template ~lines 257-285, test_token_logging ~lines 288-319, and the ANTHROPIC_API_URL constant near the top)
    - src/llm_engine.py (confirm enrich_with_llm signature at lines 221-225, confirm three-layer fallback labels generated_by as "template" on Layer 3 failure, confirm the logger name used for token-count log lines)
    - src/config.py lines 36 and 100-105 (confirm cfg.MAX_STUDENTS_PER_LLM_CALL default=10, cfg.COL_GENERATED_BY, cfg.COL_WHATSAPP_MESSAGE, cfg.COL_STUDENT_ID, cfg.COL_LLM_ERROR_REASON)
    - .planning/phases/07-test-suite/07-01-BASELINE.md (TEST-03 failures and any other test_llm_engine.py issues)
    - .planning/phases/07-test-suite/07-PATTERNS.md §"tests/test_llm_engine.py — add/verify respx-based tests" (the three required test patterns)
  </read_first>
  <behavior>
    - test_fallback_to_template: given a 1-row CRITICAL DataFrame and a respx-mocked endpoint that raises httpx.TimeoutException("timed out") (or returns HTTP 500), enrich_with_llm() must return result_df where the row's cfg.COL_GENERATED_BY == "template", cfg.COL_LLM_ERROR_REASON is set, cfg.COL_WHATSAPP_MESSAGE is a non-empty string with len > 0, and counts["fallbacks_triggered"] >= 1
    - test_campus_batching: given a DataFrame with CRITICAL/HIGH students across 2 distinct campus_ids AND cfg.MAX_STUDENTS_PER_LLM_CALL pinned to 10 via monkeypatch (or input constructed so each campus has ≤ 10 CRITICAL/HIGH students), after enrich_with_llm() runs against a successful respx mock, len(respx_mock.calls) == 2 exactly (not 1 per student, not 1 total, not >2 from split-batching)
    - test_token_logging: given a successful respx mock returning usage {"input_tokens": 150, "output_tokens": 80}, both counts["tokens_used"]["input"] == 150 AND the substring "150" (or substring "token") appears in caplog.text captured at INFO level for logger "src.llm_engine"
  </behavior>
  <action>
    Step A — test_fallback_to_template alignment with D-08:
      Locate the existing test_fallback_to_template (around lines 257-285). Verify it:
        1. Mocks the Anthropic endpoint to raise httpx.TimeoutException or to return HTTP 500. Either is acceptable per D-08.
        2. Asserts row[cfg.COL_GENERATED_BY] == "template"
        3. Asserts row[cfg.COL_WHATSAPP_MESSAGE] is non-None AND len(str(row[cfg.COL_WHATSAPP_MESSAGE])) > 0
        4. Asserts counts["fallbacks_triggered"] >= 1
      If any assertion is missing, add it using the pattern in 07-PATTERNS.md lines 173-191. Use cfg.COL_* constants exclusively — never bare strings like "generated_by".

    Step B — test_campus_batching alignment with D-09 (NOW INCLUDING MAX_STUDENTS_PER_LLM_CALL pin):
      Locate existing test_campus_batching (around lines 134-178).

      1. Add `monkeypatch` to the test signature alongside `respx_mock` if not already present:
           def test_campus_batching(respx_mock, monkeypatch) -> None
      2. At the TOP of the function body (before any DataFrame construction), pin the constant:
           monkeypatch.setattr(cfg, "MAX_STUDENTS_PER_LLM_CALL", 10)
         Rationale: D-09 requires "one API call per campus" semantics. If the env-driven default
         ever drops to a value smaller than the per-campus student count, enrich_with_llm() will
         split a single campus into multiple batches and respx_mock.calls would exceed 2, failing
         the assertion through no fault of the production behavior under test. Pinning to 10
         (matching the documented default in src/config.py line 36) guarantees the test asserts
         exactly what D-09 specifies. If your test input intentionally has more than 10
         CRITICAL/HIGH rows per campus, raise the pinned value accordingly — never let the test
         input exceed the pin.
      3. Verify the final assertion is exactly `assert len(respx_mock.calls) == 2`. If the existing
         assertion is weaker (e.g. `>= 2` or `== num_students`), tighten to `== 2` with a failure
         message naming LLM-02 and citing the pinned MAX_STUDENTS_PER_LLM_CALL value.
      4. Ensure the input DataFrame has CRITICAL/HIGH students in exactly 2 campuses with each
         campus having ≤ 10 CRITICAL/HIGH students (the pinned MAX value).

    Step C — test_token_logging extension with caplog (TEST-03 / CONTEXT.md §TEST-03):
      Locate existing test_token_logging (around lines 288-319). The existing test asserts counts["tokens_used"]["input"] == 150 but does NOT use caplog per 07-PATTERNS.md observation. Modify the test to:
        1. Add `caplog` as a parameter to the function signature alongside `respx_mock`
        2. Import `logging` if not already imported at the top of the file
        3. Wrap the enrich_with_llm call in `with caplog.at_level(logging.INFO, logger="src.llm_engine"):` — verify the logger name "src.llm_engine" matches the actual logger used in src/llm_engine.py; if production code uses a different logger name (e.g. __name__ resolved to something else), use that exact name
        4. Add after the existing counts assertion: `assert "150" in caplog.text or "token" in caplog.text.lower(), "TEST-03: token counts must be logged after successful API call"`

    Step D — Fix any other test_llm_engine.py failures listed in 07-01-BASELINE.md.

    Do NOT modify src/llm_engine.py (D-07 confirms http_client is already implemented). Do NOT modify src/config.py (the MAX_STUDENTS_PER_LLM_CALL default of 10 is correct; the test pins it for determinism, not because the source is wrong). If a test fails because the production behavior does not match the test's correct expectation, STOP and report — do not relax assertions.
  </action>
  <verify>
    <automated>py -3.12 -m pytest tests/test_llm_engine.py::test_fallback_to_template tests/test_llm_engine.py::test_campus_batching tests/test_llm_engine.py::test_token_logging -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_llm_engine.py` contains the literal string `def test_fallback_to_template(`
    - `tests/test_llm_engine.py` contains the literal string `def test_campus_batching(`
    - `tests/test_llm_engine.py` contains the literal string `def test_token_logging(`
    - `tests/test_llm_engine.py` contains the literal string `len(respx_mock.calls) == 2`
    - `tests/test_llm_engine.py` contains the literal string `caplog.at_level(`
    - `tests/test_llm_engine.py` contains the literal string `cfg.COL_GENERATED_BY` (no bare "generated_by" string in assertions)
    - `tests/test_llm_engine.py` test_fallback_to_template body contains a check that `cfg.COL_WHATSAPP_MESSAGE` is non-empty (assertion text like `len(str(row[cfg.COL_WHATSAPP_MESSAGE])) > 0` or `row[cfg.COL_WHATSAPP_MESSAGE] != ""`)
    - `tests/test_llm_engine.py` test_campus_batching body contains the literal string `monkeypatch.setattr(cfg, "MAX_STUDENTS_PER_LLM_CALL", 10)` (or an equivalent pinning to a value ≥ per-campus row count) — this is the MAX_STUDENTS pin acceptance criterion per WARNING-5
    - `py -3.12 -m pytest tests/test_llm_engine.py::test_fallback_to_template -v` exits 0
    - `py -3.12 -m pytest tests/test_llm_engine.py::test_campus_batching -v` exits 0
    - `py -3.12 -m pytest tests/test_llm_engine.py::test_token_logging -v` exits 0
    - `py -3.12 -m pytest tests/test_llm_engine.py -v` exits 0 (the whole file passes)
    - `git diff --name-only src/` returns no output (no production source touched)
  </acceptance_criteria>
  <done>All three required LLM tests aligned with D-08/D-09/TEST-03, test_campus_batching pins cfg.MAX_STUDENTS_PER_LLM_CALL via monkeypatch, and the full test_llm_engine.py file passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test_all_6_output_files_exist and verify PatternFill color check in tests/test_output_generator.py</name>
  <files>tests/test_output_generator.py</files>
  <read_first>
    - tests/test_output_generator.py (current file — locate existing test_write_outputs_all_paths_exist ~lines 599-608, test_write_outputs_returns_all_keys ~lines 571-596, and any existing color assertion ~lines 241-248)
    - src/output_generator.py (confirm write_outputs(df, output_dir, run_log) signature and that it returns dict[str, Path] with keys priority_list, whatsapp, run_log, dashboard, report, plus campus_<id> entries)
    - src/config.py line 109 (confirm cfg.COLOR_CRITICAL is defined and equals "FFFFCCCC" exactly — 8-char hex with FF alpha prefix; this is the production truth)
    - tests/conftest.py (confirm minimal_enriched_df fixture is now present from 07-01)
    - .planning/phases/07-test-suite/07-01-BASELINE.md (TEST-04 failures to fix)
    - .planning/phases/07-test-suite/07-PATTERNS.md §"tests/test_output_generator.py — add integration test" (the test_all_6_output_files_exist pattern)
  </read_first>
  <behavior>
    - test_all_6_output_files_exist: calling write_outputs(minimal_enriched_df, tmp_path, run_log_dict) returns a dict whose every value is a pathlib.Path that .exists() == True after the call; the dict additionally contains all 5 non-campus keys priority_list, whatsapp, run_log, dashboard, report
    - The existing CRITICAL row PatternFill check compares cell.fill.fgColor.rgb to cfg.COLOR_CRITICAL where cfg.COLOR_CRITICAL is the 8-character production hex string "FFFFCCCC" (NOT "00FFCCCC", NOT the 6-char "FFCCCC")
  </behavior>
  <action>
    Step A — Add test_all_6_output_files_exist:
      Append a new test function to tests/test_output_generator.py:
        - Name: test_all_6_output_files_exist (exact name per D-06)
        - Signature: def test_all_6_output_files_exist(minimal_enriched_df: pd.DataFrame, tmp_path: Path) -> None
        - Docstring: references TEST-04 and D-06
        - Build a minimal run_log dict inline with all 7 required keys per OUT-06 (run_timestamp, students_processed, api_calls_made, tokens_used, errors_encountered, fallbacks_triggered, data_quality_warnings)
        - Call result = write_outputs(minimal_enriched_df, tmp_path, run_log)
        - Iterate result.items() asserting each value is a Path instance and each path.exists() is True with a failure message naming the key
        - Then assert that all 5 fixed keys ("priority_list", "whatsapp", "run_log", "dashboard", "report") are present in result with a failure message naming the missing key
        - Reuse the existing import of write_outputs at the top of the test file. If write_outputs is not yet imported, add `from src.output_generator import write_outputs`

      Use the `minimal_enriched_df` fixture from conftest.py (added in 07-01) — do NOT define a new local fixture for this. tmp_path is a pytest built-in fixture.

    Step B — Verify the existing PatternFill color assertion (read source truth before changing anything):
      Locate the existing CRITICAL-row color assertion (per 07-PATTERNS.md around lines 241-248). Confirm:
        - It compares against `cfg.COLOR_CRITICAL` (the constant, NOT a bare hardcoded string)
        - `cfg.COLOR_CRITICAL` in src/config.py line 109 is defined as exactly `"FFFFCCCC"` (verify by reading the file; do NOT modify it)
      If the assertion uses a bare hardcoded string (whether "FFFFCCCC", "00FFCCCC", or "FFCCCC"), refactor it to compare against `cfg.COLOR_CRITICAL` so future constant changes propagate automatically. If `cfg.COLOR_CRITICAL` is defined to something OTHER than "FFFFCCCC" in src/config.py — STOP and report; the production constant value is the source of truth and src/config.py is locked per D-01 scope.

      Production lockdown reminder: src/config.py must NOT be modified. The value "FFFFCCCC" is correct and current; tests/docs that previously cited "00FFCCCC" were incorrect (07-01 Task 3 fixes the CLAUDE.md instance).

    Step C — Fix any other test_output_generator.py failures listed in 07-01-BASELINE.md.

    Do NOT modify src/output_generator.py or src/config.py. If write_outputs() is producing files in unexpected locations or with missing keys, that is a production bug to report, not a test concern to mask.
  </action>
  <verify>
    <automated>py -3.12 -m pytest tests/test_output_generator.py::test_all_6_output_files_exist -v ; py -3.12 -m pytest tests/test_output_generator.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_output_generator.py` contains the literal string `def test_all_6_output_files_exist(`
    - The body of test_all_6_output_files_exist contains the literal string `write_outputs(` (calls the orchestrator)
    - The body of test_all_6_output_files_exist contains the literal string `tmp_path` (uses the pytest tmp_path fixture, not cfg.OUTPUT_DIR)
    - The body of test_all_6_output_files_exist contains the literal string `minimal_enriched_df` (consumes the fixture from conftest.py)
    - The body of test_all_6_output_files_exist contains assertions for the literal strings `"priority_list"`, `"whatsapp"`, `"run_log"`, `"dashboard"`, and `"report"` (each non-campus key checked individually)
    - `tests/test_output_generator.py` contains at least one assertion comparing `.fgColor.rgb` to `cfg.COLOR_CRITICAL`
    - `tests/test_output_generator.py` does NOT contain the literal string `"00FFCCCC"` (the incorrect prior value must be removed if present)
    - `src/config.py` defines `COLOR_CRITICAL = "FFFFCCCC"` (exact 8-character value, verified — `cfg.COLOR_CRITICAL == "FFFFCCCC"` is the production truth; src/config.py NOT modified by this task)
    - `py -3.12 -m pytest tests/test_output_generator.py::test_all_6_output_files_exist -v` exits 0
    - `py -3.12 -m pytest tests/test_output_generator.py -v` exits 0
    - `git diff --name-only src/` returns no output (no production source touched)
  </acceptance_criteria>
  <done>test_all_6_output_files_exist added, PatternFill check uses cfg.COLOR_CRITICAL (production value "FFFFCCCC"), full test_output_generator.py file passes.</done>
</task>

<task type="auto">
  <name>Task 3: Verify combined Wave 1 full-suite gate exits 0</name>
  <files>(verification-only — no file modifications expected)</files>
  <read_first>
    - .planning/phases/07-test-suite/07-01-BASELINE.md (the failure inventory this Wave was scoped to close)
  </read_first>
  <action>
    This is the Wave 1 closing gate. It runs ONLY AFTER:
      - 07-02 Task 1 (risk engine boundary tests) is done
      - 07-02 Task 2 (ingestion D-03 renames) is done
      - 07-02 Task 3 (auxiliary baseline failures: test_config, test_generate_data, test_package_structure, test_no_hardcoded_paths) is done
      - 07-03 Task 1 (LLM tests with MAX_STUDENTS pin and caplog) is done
      - 07-03 Task 2 (output generator integration test + PatternFill check) is done

    Step A — Full-suite pytest run:
      Run `py -3.12 -m pytest tests/ -v` from project root. Capture the summary line.

    Step B — Acceptance:
      The exit code must be 0. The summary line must show 0 failed AND 0 errors. The collected count must be ≥ the count seen in 07-01-BASELINE.md (no tests deleted; only added/renamed/repaired).

    Step C — Residual handling:
      If any test still fails:
        1. Cross-reference 07-01-BASELINE.md Section 5 to determine ownership (07-02 vs 07-03 vs out-of-scope test_doc_generator.py).
        2. If the failing test is in test_doc_generator.py, that's out of scope per CONTEXT.md Phase Boundary — note it in the SUMMARY but do NOT fix it here.
        3. If the failing test is in any other file, the owning task (in 07-02 or 07-03) failed to close it. Re-open the owning task with the specific failure as additional acceptance criterion, fix, re-run this gate.

    Do NOT modify any test file in this task. Do NOT modify any source file. This task is a pure
    verification gate; its only output is the pass/fail decision recorded in the plan summary.
  </action>
  <verify>
    <automated>py -3.12 -m pytest tests/ -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.12 -m pytest tests/ -v` exits 0 (the FULL test suite passes — this is ROADMAP success criterion #1)
    - The pytest summary line shows `0 failed` and `0 errors`
    - The collected test count is ≥ the baseline count from 07-01-BASELINE.md (no test deletion regressions)
    - test_doc_generator.py failures (if any) are explicitly called out as out-of-scope per CONTEXT.md, not silently ignored — but they do NOT count against this gate UNLESS they would cause a collection error blocking the overall exit code. If test_doc_generator.py causes collection failures, this gate fails until they are isolated (e.g. via pytest --ignore=tests/test_doc_generator.py captured separately, then noted in summary).
    - `git diff --name-only src/` returns no output (no production source touched by Wave 1)
  </acceptance_criteria>
  <done>Wave 1 closes with the full pytest suite exiting 0 (or with documented out-of-scope test_doc_generator.py exclusions); Phase 7 ROADMAP success criterion #1 satisfied.</done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `py -3.12 -m pytest tests/test_llm_engine.py tests/test_output_generator.py -v` exits 0
- `py -3.12 -m pytest tests/ -v` exits 0 (full-suite gate per Task 3)
- All TEST-03 and TEST-04 entries previously marked MISSING or FAIL in 07-01-BASELINE.md are now OK
- test_campus_batching pins cfg.MAX_STUDENTS_PER_LLM_CALL via monkeypatch (WARNING-5 fix)
- All color assertions reference cfg.COLOR_CRITICAL whose production value is "FFFFCCCC"
- Zero modifications under src/
</verification>

<success_criteria>
- test_fallback_to_template, test_campus_batching (with MAX_STUDENTS pin), test_token_logging all satisfy D-08/D-09/TEST-03 and pass
- test_all_6_output_files_exist present, uses tmp_path + minimal_enriched_df, passes
- PatternFill color assertion uses cfg.COLOR_CRITICAL (production "FFFFCCCC")
- Phase 7 ROADMAP success criterion #1 confirmed: full `py -3.12 -m pytest tests/ -v` exits 0 at end of Wave 1
</success_criteria>

<output>
Create `.planning/phases/07-test-suite/07-03-SUMMARY.md` when done.
</output>
